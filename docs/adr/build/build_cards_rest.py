import sys, pathlib
sys.path.insert(0,"/home/daidaiiro/workspace/waffle/docs/adr/build")
from _common import CSS, sec, fold, tbl, extra
from card_kit import OUT, zone, pair, head, two, note, page

def ul(xs, cls="post"):
    return f'<ul class="{cls}">' + "".join(f"<li>{x}</li>" for x in xs) + "</ul>"

# ════════ 1 事業領域
page("business-domain", 1, "事業領域", "Business Domain",
 "抽象のいちばん上。<b>顧客に何を提供している事業か</b>を書く。"
 "そこから業務領域の分類（中核・一般・補完）の基準が決まる。",
 head("事業領域","bd-waffle","Waffle","Waffle",
   "スキーマという型で文書を焼き上げ、構造の検証と意味の案内を内蔵するドキュメント基盤の事業。")
 + zone("この事業が顧客へ提供しているもの", None,
    two("誰に",
        '<p class="pr-v">仕様を書き、それを実装へ運ぶ人。<br>およびその作業を代行する AI</p>',
        "何を",
        '<p class="pr-v"><b>仕様と実装がずれていないことを、機械が言える状態。</b><br>'
        '宣言した単位が実装に在ること。宣言した振る舞いを実装がすること。'
        '宣言していない振る舞いが動いていないこと。</p>', arrow=False)
    + note("この欄が<b>業務領域の分類の基準</b>になる。"
           "中核・一般・補完は「差別化を生むか」で決まるが、"
           "<b>何に対する差別化かを決めるのはこの事業領域</b>である。")),
 tbl(["欄","必須か","何を書くか"],[
  ("",["題","必須","この事業を業務の言葉で。<b>識別子を繰り返さない</b>"]),
  ("",["名前の英語表記","必須","その語を英語でどう言うか"]),
  ("",["概要","必須","顧客に提供するサービスの大枠を1〜2文で"]),
  ("keyrow",["この事業が顧客へ提供しているもの","<b>必須</b>",
    "<b>誰に</b>と<b>何を</b>。空だと業務領域の分類に根拠が無くなる。"
    "書くのは<b>いまの事業</b>であって、社名でも沿革でもない"]),
 ]))

# ════════ 2 業務領域
page("subdomain", 2, "業務領域", "Subdomain",
 "事業活動を細分化した1単位。<b>中核・一般・補完のどれか</b>を分類し、その根拠を書く。"
 "<b>境界は一覧として持たず、指しているユースケースから導く。</b>",
 head("業務領域","sd-docstring-linting","docstring の適合を見る","Docstring Linting",
   "実装の docstring が定めた流儀に従っているかを、既存の道具に判定させて返す。",
   foot=("この業務領域が属する事業領域","bd-waffle"))
 + zone("事業のどこに位置づくか", None,
    two("分類", '<p class="big">一般</p>',
        "そう分類した根拠",
        '<p class="pr-v">既存の linter で足りる。'
        'この事業が提供している「ずれを機械が言える状態」に直接効かないため、'
        '<b>差別化を生まない</b>。</p>', arrow=False)
    + pair("使う既製品", "既存の docstring linter"))
 + zone("この業務領域の境界", None,
    note("<b>一覧としては持たない。導く。</b>"
         "この業務領域を指しているユースケースを数えれば、境界はそこから出る ── "
         "書き起こしが「業務領域の境界＝<b>強く関連するユースケースの集まり</b>」と"
         "定めているためである。<b>集まりが空のものは、業務領域ではない。</b>")),
 tbl(["欄","必須か","何を書くか"],[
  ("",["題／名前の英語表記／概要","必須","この業務活動を業務の言葉で"]),
  ("keyrow",["属する事業領域","<b>必須</b>",
    "1つだけ。分類は<b>事業領域を基準にした相対評価</b>なので、基準が決まらないと分類できない"]),
  ("",["分類","必須","中核／一般／補完のいずれか"]),
  ("keyrow",["そう分類した根拠","<b>必須</b>",
    "<b>この事業が提供しているものに照らして</b>、差別化を生むかを書く。"
    "<b>どれだけ作り込むかは書かない</b> ── それは規約が持つ"]),
  ("",["使う既製品","任意","一般に分類したとき、何で置き換えるか"]),
 ]))

# ════════ 3 区切られた文脈
page("bounded-context", 3, "区切られた文脈", "Bounded Context",
 "1つの言葉が一貫した意味を持ち続けられる範囲。"
 "<b>持つのは、その範囲で通じる言葉の定義だけ</b>である。",
 head("区切られた文脈","bc-waffle","スキーマ駆動の文書基盤","Schema-Driven Document Platform",
   "型に照らして文書を検証し、問い合わせ、描き、雛形を作る範囲。この中で言葉が一貫する。")
 + zone("この文脈で通じる言葉", "89件",
    tbl(["語","定義"],[
     ("",["ドメイン駆動設計",
       "業務の言葉でモデルを作り、その言葉のまま実装へ運ぶ設計の考え方。この文脈では土台として置く"]),
     ("",["区切られた文脈",
       "ある言葉が一貫した意味を持ち続けられる範囲であり、そのモデルが実装として独立している範囲"]),
     ("",["型","文書が適合すべき構造の宣言。文書を検証・描画するときの唯一の基準"]),
     ("",["<span class='sub'>ほか86件</span>","<span class='sub'>—</span>"]),
    ])
    + note("<b>同じ語が、別の文脈で別の意味を持ってよい。</b>それが文脈を区切る理由である。"))
 + zone("この文脈の内側", None,
    note("<b>一覧としては持たない。</b>集約・エンティティ・値オブジェクト・"
         "業務ユースケース・業務サービスは、この文脈の配下に置かれる ── "
         "<b>どれも文脈を1つしか持たない</b>ので、置き場所が所属を運んでよい。")),
 tbl(["欄","必須か","何を書くか"],[
  ("",["題／名前の英語表記／概要","必須","この範囲を業務の言葉で"]),
  ("keyrow",["この文脈で通じる言葉","<b>必須</b>",
    "語と、その定義。<b>この文脈の中に閉じる</b>"]),
 ]))
print("1-3 ok")

# ════════ 4 集約
page("aggregate", 4, "集約", "Aggregate",
 "1つのトランザクションで一緒に変わる範囲。<b>いちばん変わる種別</b>である ── "
 "エンティティと値オブジェクトの定義が出て、状態と遷移が消え、"
 "操作が受け入れ基準とシナリオを持つ。",
 head("集約","agg-document","文書","Document",
   "型に照らして検証・描画される、構造化された成果物の一貫性単位。")
 + zone("境界", None,
    two("内側 ── 実体で持つ",
        '<div class="pr-v"><p><b>ルート</b>　<code>ent-document</code></p>'
        '<p class="sub" style="margin-top:.3rem">値オブジェクト5件<br>'
        '<code>vo-document-id</code> <code>vo-document-type</code><br>'
        '<code>vo-discriminator-value</code> <code>vo-schema-ref</code> <code>vo-status</code></p>'
        '<p class="sub" style="margin-top:.3rem">内側のエンティティ <b>0件</b> ── 階層を持たない</p></div>',
        "外 ── 識別子で指す",
        '<div class="pr-v"><p><code>agg-schema</code></p>'
        '<p class="sub" style="margin-top:.3rem">実体は持たない</p></div>', arrow=False)
    + note("<b>ここへ入れてよいのは、1つのトランザクションで一緒に変わらなければならないものだけ。</b>"
           "別々に変えてよいなら、それは別の集約であり、境界の外として鍵で指す。"))
 + zone("常に満たすこと", "3件",
    tbl(["規則","担保"],[
     ("",["適合していない文書からは、成果物を描き出せない","検査"]),
     ("",["パス解決は、どの操作からも常にプロジェクトルート内に閉じる","検査"]),
     ("",["schemaRef を持たない文書への型の解決は、どの操作からも拒否される","検査"]),
    ])
    + note("<b>境界をまたぐものだけを持つ。</b>"
           "その型ひとつを見れば足りる規則は、その型が持つ。"))
 + zone("外へ公開する操作", "4件 ／ 変えるもの 1件",
    '<div class="crit">' + tbl(["操作","この集約を変えるか","常に満たすことを覆う義務","基準","シナリオ"],[
     ("",["値を埋める","<b>変える</b>","<b>ある</b>","3件","3件"]),
     ("",["値を取り出す","変えない","無い","2件","2件"]),
     ("",["逸脱していないか判定する","変えない","無い","3件","3件"]),
     ("",["描く","変えない","無い","2件","2件"]),
    ]) + '</div>'
    + note("<b>変える操作にだけ、覆う義務が掛かる。</b>"
           "変えない操作は、常に満たすことを破りようがないためである。"
           "<span class='sub'>（変えるものを、ドメイン駆動設計ではコマンドと呼ぶ）</span>")
    + pair("出す出来事",
      "<code>DocumentValidated</code> <code>DocumentRendered</code> "
      "<code>DocumentDeployed</code> <code>DocumentSuperseded</code>")),
 tbl(["欄","必須か","何を書くか"],[
  ("",["題／名前の英語表記／概要","必須","この一貫性の単位を業務の言葉で"]),
  ("keyrow",["境界の内側","<b>必須</b>",
    "ルートのエンティティ1つと、内側のエンティティ・値オブジェクトへの鍵"]),
  ("",["識別子で指す、外の集約","任意","実体は持たない"]),
  ("keyrow",["常に満たすこと","<b>必須</b>","「常に〜である」。<b>境界をまたぐものだけ</b>"]),
  ("keyrow",["外へ公開する操作","<b>必須</b>",
    "操作ごとに<b>この集約を変えるか</b>／基準／シナリオ"]),
  ("",["変える内側のエンティティ","任意","階層を持つ集約でだけ書く"]),
  ("",["出す出来事","任意","空なら「出さない」という決定である"]),
 ]),
 sec("03","状態と遷移を持たない",
  "<b>導けるものは持たない</b>（段1 の決定）。",
  '<p class="dp" style="font-size:.93rem;line-height:1.85;color:var(--ink-soft)">'
  '「適合したか・描いたか・中身に何があるかは、<b>文書と型と成果物から導ける</b>。'
  '導けるものを保存すると古くなる」── そのまま当てはまる。'
  '連鎖して、状態に依存する不変条件（前進のみ・終端）も消え、'
  '<b>7件が3件になる</b>。</p>'))

# ════════ 5 エンティティ
page("entity", 5, "エンティティ", "Entity",
 "<b>新設。</b>同一性を持ち、状態が変わるもの。"
 "集約の内側にあった定義が、そのまま1本の文書になる。<b>ルートも例外にしない。</b>",
 head("エンティティ","ent-document","文書","Document",
   "型に照らして検証・描画される、構造化された成果物。中身が入れ替わっても、同じ文書である。",
   callout=("documentId","<b>これが同じなら、同じ文書である。</b>"
     "中身が入れ替わり、状態が進んでも変わらない。"))
 + zone("持つ属性", "9件",
    tbl(["属性","型"],[
     ("",["<code>documentId</code> <span class='sub'>同一性</span>","<code>vo-document-id</code>"]),
     ("",["<code>schemaRef</code>","<code>vo-schema-ref</code>"]),
     ("",["<code>content</code>","構造化データ（型に準拠）"]),
     ("",["<span class='sub'>ほか6件</span>","<span class='sub'>—</span>"]),
    ])
    + note("<b>型は、値オブジェクトの文書への鍵で指す。</b>"
           "裸の文字列にしない ── 綴りを間違えたときに、誰も気づけなくなる。"))
 + zone("この型だけで常に満たすこと", "2件",
    tbl(["規則","担保"],[
     ("",["schemaRef は常に存在する","型"]),
     ("",["documentId は、文書が在るあいだ変わらない","型"]),
    ])
    + note("<b>その型ひとつを見れば確かめられる規則だけ。</b>"
           "他を見る必要があるなら集約が持つ。"))
 + zone("どの集約に属するか", None,
    note("<b>ここには書かない。</b><code>agg-document</code> の一覧が、これをルートとして指している。"
         "両側に書くと、食い違ったときに<b>どちらが正かを言えなくなる</b>。")),
 tbl(["欄","必須か","何を書くか"],[
  ("",["題／名前の英語表記／概要","必須","このものを業務の言葉で"]),
  ("keyrow",["同じものと見なす根拠","<b>必須</b>",
    "<b>何が変わっても同じものだと言える根拠。</b>値オブジェクトとの違いはここだけ"]),
  ("",["持つ属性","必須","名前と型。型は<b>値オブジェクトへの鍵</b>"]),
  ("",["この型だけで常に満たすこと","任意","その型ひとつを見れば確かめられる規則"]),
  ("",["どの集約に属するか","<b>書かない</b>","集約の側の一覧が指す"]),
 ]))
print("4-5 ok")

# ════════ 6 値オブジェクト
page("value-object", 6, "値オブジェクト", "Value Object",
 "<b>新設。</b>値で等しさが決まり、変わらないもの。"
 "bc-waffle では10本出る ── いままで集約の内側にしか置けなかったものである。",
 head("値オブジェクト","vo-schema-ref","型を特定する鍵","Schema Reference",
   "文書が適合する型を、名前と版の組で一意に指す鍵。",
   callout=("鍵であって、写しではない",
     "適合する型そのものではなく、<b>それを特定できる鍵</b>を表す。"
     "どの型かが決まればよく、その型が何を宣言しているかは持たない。"))
 + zone("持つ属性と、振る舞い", None,
    two("属性",
        '<div class="pr-v"><p><code>value</code>　<span class="sub">文字列</span></p>'
        '<p class="sub" style="margin-top:.3rem">'
        '<code>&lt;型の名前&gt;/&lt;版&gt;</code> の形</p></div>',
        "振る舞い",
        '<div class="pr-v"><p><b>変わらない。値が等しければ等しい。</b></p>'
        '<p class="sub" style="margin-top:.3rem">'
        '指し先の型が新しい版を持っても、この鍵は変わらない</p></div>', arrow=False)
    + note("<b>変わるものは、値ではなくエンティティである。</b>"))
 + zone("この値だけで常に満たすこと", "2件",
    tbl(["規則","担保"],[
     ("",["値は、実在する型の名前と版を指す","検査"]),
     ("",["名前と版を分けて持たない。分解は、指し先を解決するときに行う","型"]),
    ]))
 + zone("どの集約のものか", None,
    note("<b>問いが立たない。</b>同一性を持たないので、"
         "<b>複数の集約が同じ値オブジェクトを使ってよい</b>。"
         "だから置き場所も、集約の下ではなく文脈の直下になる。")),
 tbl(["欄","必須か","何を書くか"],[
  ("",["題／名前の英語表記／概要","必須","この値を業務の言葉で"]),
  ("keyrow",["この値が表すもの","<b>必須</b>",
    "何の代わりに立っているか。<b>指し先の写しではないこと</b>を書く"]),
  ("",["持つ属性","必須","名前と型。1つのこともある"]),
  ("keyrow",["振る舞い","<b>必須</b>",
    "<b>変わらないこと</b>と、<b>値が等しければ等しいこと</b>"]),
  ("",["この値だけで常に満たすこと","任意","その値ひとつを見れば確かめられる規則"]),
 ]))

# ════════ 8 業務サービス
page("domain-service", 8, "業務サービス", "Domain Service",
 "どの集約にも属さない、<b>集約をまたぐ判断</b>。"
 "8種別のうち<b>唯一、欄が1つも変わらない</b>。",
 head("業務サービス","ds-collect-key-references",
   "宣言をたどって、鍵を指している場所を集める","Collect Key References",
   "参照関係の宣言と文書を受け取り、ある鍵を指している場所を集めて返す。"
   "宣言されていない参照は集めない ── どこが指しうるかは宣言だけが決める。",
   callout=("集約の内側に置けない理由",
     "<b>2つの集約にまたがる。</b>走査する相手は文書の中身だが、"
     "どこをたどるべきかという宣言は型が持っている。"
     "文書は自分がどこから指されるかを知らず、型は中身を知らない。"))
 + zone("受け取るものと、返すもの", None,
    two("受け取る",
        '<ul class="post"><li>鍵</li><li>参照関係の宣言</li><li>文書の集まり</li></ul>',
        "返す",
        '<ul class="post"><li>その鍵を指している場所の一覧</li></ul>')
    + pair("触れる集約", "<code>agg-document</code>　<code>agg-schema</code>")
    + note("<b>1つしか並んでいなければ、その集約の内側に置けたはずである。</b>"
           "2つ以上あることが、外に居る根拠になる。"))
 + zone("約束すること", "基準 6 ／ シナリオ 6 ／ 覆えていない基準 0",
    '<div class="crit">' + tbl(["","いつ","何が","どうなる","覆うシナリオ"],[
     ("",["<b>1</b>","たどるとき","宣言に無い経路","<b>たどらない</b>","1件"]),
     ("",["<b>2</b>","返すとき","同じ場所","<b>二度返さない</b>","1件"]),
     ("",["<span class='sub'>3〜6</span>","<span class='sub'>ほか4件</span>",
       "<span class='sub'>—</span>","<span class='sub'>—</span>","<span class='sub'>4件</span>"]),
    ]) + '</div>'
    + note("シナリオ6件は<b>いずれも「確かめる単位」に "
           "<code>ds-collect-key-references</code> を指す</b>。")),
 tbl(["欄","必須か","何を書くか"],[
  ("",["題／名前の英語表記／概要","必須","この計算を業務の言葉で"]),
  ("keyrow",["集約の内側に置けない理由","<b>必須</b>",
    "<b>理由が同じことの言い換えになっていたら、集約の境界を引き直せる可能性がある</b>"]),
  ("",["触れる集約","任意","1つしか並ばないときは、内側に置けたはずかを疑う"]),
  ("",["受け取るものと、返すもの","必須","入口と出口"]),
  ("",["受け入れ基準","必須","<b>いつ</b>・<b>何が</b>・<b>どうなる</b>の3つ組"]),
  ("keyrow",["シナリオ","<b>必須</b>","Given / When / Then と、<b>確かめる単位</b>"]),
 ]))
print("6,8 ok")
