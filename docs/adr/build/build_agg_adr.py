import sys, pathlib
sys.path.insert(0,"/home/daidaiiro/workspace/waffle/docs/adr/build")
from _common import CSS, sec, fold, tbl, step, joint, ok, ng, extra

K = "<code>domain-model</code>"
SRC = "『ドメイン駆動設計をはじめよう』第6章の書き起こし"

axis = f'''<div class="scroll"><table>
<thead><tr><th>案</th>
<th class="keycol">書き起こしと食い違わない<br><span class="sub">決め手</span></th>
<th>1つのトランザクションで変わる範囲が読める</th>
<th>またがる概念を1か所に置ける</th>
<th>文書の数が増えない</th></tr></thead>
<tbody>
<tr class="pickrow"><td class="cond">所有の一節だけを直す<br><span class="sub">採る</span></td>
<td>{ok}</td><td>{ok}</td><td>{ok}</td><td>{ng}</td></tr>
<tr><td class="cond">決定ごと取り下げ、集約の中へ埋め戻す</td><td>{ok}</td><td>{ok}</td><td>{ng}</td><td>{ok}</td></tr>
<tr><td class="cond">決定を保ち、Waffle 独自の作りだと明示する</td><td>{ng}</td><td>{ng}</td><td>{ok}</td><td>{ng}</td></tr>
</tbody></table></div>'''

axis_fold = fold("条件の内訳を開く ── なぜその条件が要るか、なぜ × なのか（4件）",
  tbl(["条件（全文）","なぜこの条件が要るか","× の理由"],[
    ("keyrow",['書き起こしと食い違わない<span class="keytag">決め手</span>',
      "Waffle はドメイン駆動設計を根幹に置く。"
      "根幹と食い違う作りにすると、同じ言葉が Waffle の中と外で別の意味を持ち、"
      "外から来た人の理解がそのまま誤りになる",
      "<b>Waffle 独自と明示する</b> 食い違いが残る。"
      "明示しても、同じ言葉が2つの意味を持つことは変わらない"]),
    ("",["1つのトランザクションで変わる範囲が読める",
      "書き起こしは「1つの集約のインスタンスが1つのトランザクションの単位になる」と定める。"
      "境界の内と外が読めなければ、この単位が決まらない",
      "<b>Waffle 独自と明示する</b> 内側も外側も同じ鍵1つで指すため、"
      "どこまでが1つのトランザクションかが文書から読めない"]),
    ("",["またがる概念を1か所に置ける",
      "置き場所が集約の内側しかないと、集約をまたぐ概念は参照する側が写しを持つ。"
      "写しは相手が変わったときに古くなる（前の決定が測った症状）",
      "<b>埋め戻す</b> 前の決定が測った症状がそのまま戻る"]),
    ("",["文書の数が増えない",
      "単位が増えれば、書く文書も辿る文書も増える",
      "<b>一節だけ直す（採る）</b>／<b>Waffle 独自と明示する</b> "
      "どちらも独立して書ける単位を保つので、文書は増えたままである"]),
  ]))

reason = '<div class="chain">' + "".join([
  step("evidence","測った（前の決定から引き継ぐ）",
    "Document が持つ参照は7つ。うち6つは鍵1つで相手を特定し、"
    "<code>schemaRef</code> だけが相手の名前と版を写して持っていた"),
  joint("だから"),
  step("conclude","言えること",
    "<b>集約をまたぐ</b>参照は、相手の値を写さず、鍵1つへ寄せる"),
  joint("ところが前の決定は、これを内側にも当てた"),
  step("counter","行き過ぎ",
    "前の決定は「集約は値オブジェクトとエンティティを<b>所有しない。参照する</b>」と書いた。"
    "測ったのは境界の外の7件であり、<b>境界の内側については何も測っていない</b>"),
  joint("書き起こしを読んだ"),
  step("evidence","出典",
    "「他の集約は識別子で参照し、オブジェクトそのものを内部に保持しない」── "
    "<b>他の集約は</b>、と書いてある。"
    "同じ節が内側についても述べる。"
    "「同じ集約の一部として一貫性を守るものだけを内側に持ち、それ以外は識別子で指す」"),
  joint("さらに"),
  step("evidence","出典",
    "「集約は<b>エンティティの階層構造</b>であり、データの一貫性を保証する境界である」／"
    "「集約は複数のエンティティと値オブジェクトの階層構造を持てる」／"
    "「エンティティは<b>単独で実装せず</b>、必ず集約の実装の一部になる」／"
    "「1つの集約のインスタンスが1つのトランザクションの単位になる」"),
  joint("だから"),
  step("conclude","言えること",
    "内側まで鍵で切ると、<b>内と外の区別が消える</b>。"
    "区別が消えると、1つのトランザクションで変わる範囲がどこまでかを、文書から誰も言えなくなる"),
  joint("どこで行き過ぎたかを見た"),
  step("conclude","言えること",
    "<b>2つの参照を1つの言葉で言っていた。</b>"
    "値オブジェクトやエンティティを別の文書として書き、集約の文書がそれを指すのは"
    "<b>書き方</b>の話である。集約が内側を実体で持つか鍵で持つかは<b>実装の形</b>の話である。"
    "別々の文書に書いても、実装では集約が持つ ── この2つは両立する"),
  joint("抽象の高さを当てると"),
  step("evidence","拠り所",
    "仕様の下限は、そこから具体が一意に導けるかで測る。"
    "仕様は実装の写しではなく、<b>具体が導ける高さ</b>まで抽象する"),
  joint("合わせると"),
  step("conclude","結論",
    "独立して書ける単位は保つ。集約は自分の境界の内側を持ち、"
    "鍵1つで指すのは境界の外の集約だけにする"),
  joint("ただし"),
  step("counter","反証",
    "独立した文書にすると、<b>どの集約にも属さないエンティティの文書が書ける</b>。"
    "書き起こしがアンチパターンと呼ぶ形が、書けるようになる。"
    "集約の側に境界の内側の一覧を必須で持たせ、"
    "どの一覧にも現れないエンティティを違反として検出することでしか防げない"),
]) + "</div>"

reason_fold = fold("段の裏付けと、崩れるときを開く（5件）",
  tbl(["段","裏付けの種別","参照先","崩れるとき"],[
    ("keyrow",["集約は内側を実体で持ち、外は識別子で指す",'<span class="kindtag unv">knowledge</span>',
      f"{K} ── 「他の集約を参照する」「エンティティの階層構造」","—"]),
    ("keyrow",["エンティティは単独で実装しない",'<span class="kindtag unv">knowledge</span>',
      f"{K} ── 「エンティティ」の定義","—"]),
    ("",["1集約が1トランザクションの単位",'<span class="kindtag unv">knowledge</span>',
      f"{K} ── 「トランザクションの境界」","—"]),
    ("",["参照7件のうち例外は1件",'<span class="kindtag unv">実測</span>',
      "前の決定が数えた。この決定はその実測を引き継ぎ、当てる先だけを直す","—"]),
    ("",["仕様の下限は、具体が一意に導けるかで測る",'<span class="kindtag unv">先立つ決定</span>',
      '<a href="https://claude.ai/code/artifact/e8397ab8-292a-4b7a-89f8-0ff1d1dee18e">仕様の下限を、具体が一意に導けるかで測る</a>（承認済み）',"—"]),
  ])
  + '<p class="foldnote">この決定に「Waffle の設計判断」の段が無い理由 ── '
    'ここで決めているのは<b>書き方と実装の形の対応</b>だけで、'
    'ドメイン駆動設計が定めていることを足しても引いてもいない。'
    f'足しているのは<b>1つ</b>、「境界の内側の一覧を、集約の文書が持つ」である。'
    '書き起こしは実装が何を持つかを述べるが、'
    'それを別々の文書に分けて書くときに<b>どちらの側が境界を言うか</b>は持たない。</p>')

body = "".join([
 '<header><p class="eyebrow">Architecture Decision Record</p>'
 '<h1>集約は、自分の境界の内側を持つ</h1>'
 '<p class="lede">前の決定は、集約をまたぐ参照7件を測って「参照は鍵1つへ寄せる」と結論した。'
 '測ったことは正しい。しかしその結論を、境界の内側にも当てた。'
 '<b>内側まで鍵で切ると、1つのトランザクションで変わる範囲を、文書から誰も言えなくなる。</b></p></header>',

 sec("01","決定",None,
  '<div class="decision"><p class="main">Waffle は、'
  '値オブジェクトとエンティティを独立して書ける単位にする ── '
  '<b>これは前の決定から引き継ぐ</b>。そのうえで、'
  '<b>集約は自分の境界の内側のエンティティと値オブジェクトを持つ</b>。'
  '鍵1つで指すのは、境界の外の集約だけである。'
  '集約の文書は、境界の内側の一覧を必ず持つ。</p>'
  '<div class="key"><span class="lbl">決め手</span>'
  '<span class="txt">前の決定は<b>2つの参照を1つの言葉で言っていた</b>。'
  '「別の文書として書き、集約の文書が指す」は書き方の話であり、'
  '「集約が内側を実体で持つか鍵で持つか」は実装の形の話である。'
  '別々の文書に書いても、実装では集約が持つ ── <b>この2つは両立する</b>。'
  '1つの言葉で言ったために、書き方の決定が実装の形まで決めてしまった。</span></div></div>'),

 sec("02","変更前と変更後",
  "変わるのは<b>集約が内側に何を持つか</b>だけである。独立して書ける単位は変えない。",
  tbl(["","変更前 ── 置き換えられた決定","変更後"],[
    ("",["独立して書ける単位","4部品すべて","<b>変えない</b>"]),
    ("keyrow",["集約が持つもの","一貫性の境界だけ","一貫性の境界と、<b>その内側のエンティティ・値オブジェクト</b>"]),
    ("keyrow",["集約から内側への参照","鍵1つで指す","<b>実体で持つ。</b>内側の状態を変えるコマンドはルートを経由する"]),
    ("",["集約から外の集約への参照","鍵1つで指す","<b>変えない</b>"]),
    ("",["集約の文書",
      "境界の内側の一覧を持たない",
      "<b>持つ。</b>ルートのエンティティ1つ、内側のエンティティ、内側の値オブジェクト"]),
    ("",["どの集約にも属さないエンティティ",
      "書けてしまう。誰も違反と言えない",
      "<b>どの一覧にも現れないエンティティ</b>として検出できる"]),
    ("",["「参照する」が指すもの",
      "文書のあいだの参照と実装の参照、どちらとも読める",
      "<b>文書のあいだの参照だけ</b>を指す"]),
  ])),

 sec("03","理由",
  "測ったのは境界の外の7件で、内側については何も測っていなかった。"
  "<b>書き起こしは「他の集約は識別子で参照し」と、外に限って述べている。</b>",
  reason, reason_fold),

 sec("04","判断を分けた軸",
  "採る案は4つの条件のうち3つを満たす。落とすのは1つで、<b>文書の数は増えたままである</b>。",
  axis, axis_fold),

 sec("05","付随して決めたこと",
  "境界の内側を集約が持つと決めると、3つの文書が互いをどう指すかが決まる。",
  tbl(["論点","決定"],[
    ("keyrow",["境界を言うのはどちらの側か",
      "<b>集約の側。</b>集約の文書が、境界の内側の一覧を持つ"]),
    ("",["エンティティの文書",
      "所属する集約を書かない。どの集約の一覧にも現れないことで、単独のエンティティを検出する"]),
    ("",["値オブジェクトの文書",
      "所属を持たない。<b>複数の集約の一覧に現れてよい</b>"]),
    ("",["ルートのエンティティ",
      "集約の文書が1つだけ指す。内側の状態を変えるコマンドは、すべてルートに付く"]),
  ]),
  fold("それぞれの根拠を開く（4件）",
    tbl(["論点","根拠"],[
      ("keyrow",["境界を言うのはどちらの側か",
        "書き起こしは「集約は内部と外部の間に明確な境界を定義する」と述べる。"
        "境界は集約自身の主張であり、集約の側が言う。"
        "両側に書くと同じことが2か所になり、食い違ったときにどちらが正かを言えない"]),
      ("",["エンティティの文書",
        "所属を両側に書かないための帰結である。"
        "検出したいのは「どの集約にも属さないエンティティ」であり、"
        "これは一覧の側だけを見れば分かる"]),
      ("",["値オブジェクトの文書",
        "書き起こしは値オブジェクトを「欄の値の組み合わせで識別し、明示的な識別子を持たない」"
        "「不変として実装する」と定める。"
        "同一性を持たないので、<b>どの集約のものかという問いが立たない</b>。"
        "重さや金額のような値は、複数の集約が同じ型を使う"]),
      ("",["ルートのエンティティ",
        "書き起こしは「外部に公開するインターフェース役のエンティティは1つにする」"
        "「内部の他のエンティティの状態を変えるコマンドも、必ずルートを経由して実行する」と定める。"
        "ルートが指されていなければ、コマンドの付け先が決まらない"]),
    ]))),

 sec("06","答えないこと",
  "欄の形と、個別の判定は、この決定では扱わない。",
  tbl(["項目","種別","行き先"],[
    ("",["境界の内側の一覧を、どういう欄として書くか",'<span class="kindtag out">範囲外</span>',
      "段2 の Domain の型を起こすときに決める"]),
    ("",["いま集約として書かれている2件が本当に集約か",'<span class="kindtag out">範囲外</span>',
      "移す作業で1件ずつ判定する"]),
    ("",["集約をまたぐ整合をどう保つか",'<span class="kindtag out">範囲外</span>',
      "業務サービスと業務イベントの扱いとして、別に決める"]),
    ("",["受け入れ基準と振る舞いが、どの要素に付くか",'<span class="kindtag out">範囲外</span>',
      "別の決定で扱う。この決定は、部品の持ち方だけを決める"]),
  ])),

 '<section><h2><span class="num">07</span>承認</h2>'
 '<div class="approve"><span class="k">状態</span><span class="v">未承認</span></div></section>',

 sec("08","関連","この決定は、段2 の Domain の型を起こすための前提になる。","",
  fold("縛る対象と、前後の決定を開く（4件）",
    tbl(["種類","対象"],[
      ("keyrow",["縛る対象",
        "集約・エンティティ・値オブジェクトの文書が、互いをどう指すか。"
        "以後、境界の内側は集約の文書だけが言う"]),
      ("",["引く knowledge",f"{K} ── {SRC}"]),
      ("",["置き換える決定",
        '<a href="https://claude.ai/code/artifact/1a0663a7-5c19-433f-a623-899e83d23643">値オブジェクトとエンティティを、独立して書ける単位にする</a>'
        "（2026-08-15 承認）── "
        "判断は引き継ぎ、「所有しない。参照する」の一節だけを直す"]),
      ("",["この決定を使う予定",
        "段2 の Domain の型を起こす。種別ごとの必須ブロックが、この決定で決まる"]),
    ]))),
])

html = ("<title>集約は、自分の境界の内側を持つ</title>"
        f"<style>{CSS}\n{extra}</style>"
        f'<div class="wrap">{body}</div>')
pathlib.Path("/home/daidaiiro/workspace/waffle/docs/adr/adr-aggregate-holds-its-inside.html").write_text(html, encoding="utf-8")
print("書いた", len(html), "bytes")
