import sys, pathlib
sys.path.insert(0,"/home/daidaiiro/workspace/waffle/docs/adr/build")
from _common import CSS, sec, fold, tbl, step, joint, ok, ng, extra

K = "<code>spec-implementation-abstraction</code>"

axis = f'''<div class="scroll"><table>
<thead><tr><th>案</th>
<th class="keycol">またがる概念を1か所に置ける<br><span class="sub">決め手</span></th>
<th>ドメインモデルの4部品が同じ粒度で書ける</th>
<th>集約が一貫性の境界だけを持つ</th>
<th>文書の数が増えない</th></tr></thead>
<tbody>
<tr class="pickrow"><td class="cond">値オブジェクトとエンティティを独立の単位にする<br><span class="sub">採る</span></td>
<td>{ok}</td><td>{ok}</td><td>{ok}</td><td>{ng}</td></tr>
<tr><td class="cond">いまのまま集約の中に埋める</td><td>{ng}</td><td>{ng}</td><td>{ng}</td><td>{ok}</td></tr>
<tr><td class="cond">またがるものだけを共通の置き場へ出す</td><td>{ok}</td><td>{ng}</td><td>{ng}</td><td>{ok}</td></tr>
</tbody></table></div>'''

axis_fold = fold("条件の内訳を開く ── なぜその条件が要るか、なぜ × なのか（4件）",
  tbl(["条件（全文）","なぜこの条件が要るか","× の理由"],[
    ("keyrow",['またがる概念を1か所に置ける<span class="keytag">決め手</span>',
      "置き場所が集約の内側しかないと、集約をまたぐ概念は参照する側が写しを持つことになる。"
      "写しは、相手が変わったときに古くなる。実際に相手の振る舞いが失われている",
      "<b>いまのまま</b> 集約ごとに定義することになる"]),
    ("",["ドメインモデルの4部品が同じ粒度で書ける",
      "値オブジェクト・エンティティ・集約・業務サービスは、ドメイン駆動設計が同じ格で置いている部品である。"
      "書ける粒度が違うと、置き場所の都合で部品の選び方が歪む",
      "<b>いまのまま</b>／<b>またがるものだけ出す</b> どちらも4部品は揃わない"]),
    ("",["集約が一貫性の境界だけを持つ",
      "集約は一貫性の境界である。所有していないものまで抱えると、境界がどこまでかが読めなくなる",
      "<b>いまのまま</b>／<b>またがるものだけ出す</b> 集約が値オブジェクトを持ち続ける"]),
    ("",["文書の数が増えない",
      "単位が増えれば、書く文書も辿る文書も増える",
      "<b>独立の単位にする（採る）</b> いま集約の中にある10件が、それぞれ文書になる"]),
  ]))

reason = '<div class="chain">' + "".join([
  step("evidence","測った",
    "ドメインモデルの部品は4つ（値オブジェクト・エンティティ・集約・業務サービス）だが、"
    "仕様として独立に書けるのは<b>集約と業務サービスの2つだけ</b>だった"),
  joint("だから"),
  step("conclude","言えること",
    "値オブジェクトとエンティティは、<b>集約の内側にしか置けない</b>"),
  joint("そうすると何が起きるかを測った"),
  step("evidence","測った",
    "Document が持つ参照は7つ。うち6つは <code>DocumentId</code> 型で、属性を1つ（値）だけ持つ<b>鍵</b>である。"
    "<code>schemaRef</code> だけが <code>SchemaRef</code> 型で、<b>名前と版という2つの属性を持つ</b>。"
    "しかも両方が裸の文字列で、Schema 側が持つ <code>SchemaId</code>・<code>Version</code> の型を使っていない"),
  joint("だから"),
  step("conclude","言えること",
    "集約をまたいで型を参照できないため、<b>参照が相手の値を写して持つことになった</b>。"
    "写しなので、Schema 側の振る舞い（版は上げる方向にのみ進む）は失われている。"
    "例外が1つだけ在ることは、この形が意図ではなく置き場所が無かった結果であることを示す"),
  joint("症状も出ていた"),
  step("evidence","測った",
    "置き場所が無かった Schema が、集約として書かれている。"
    "38個の型定義を1つに束ね、<b>コマンドは0件</b>、"
    "不変条件は12件のうち11件が静的な書き方の規則で、時間をまたいで守るものではない"),
  joint("合わせると"),
  step("conclude","結論",
    "値オブジェクトとエンティティを、仕様として独立に書ける単位にする。"
    "集約は一貫性の境界だけを持ち、それらを所有しない"),
  joint("ただし"),
  step("counter","反証",
    "単位が増えると、書く文書も辿る文書も増える。"
    "<b>集約1つを読めば済んでいたものが、複数を辿ることになる</b>"),
]) + "</div>"

reason_fold = fold("段の裏付けと、崩れるときを開く（5件）",
  tbl(["段","裏付けの種別","参照先","崩れるとき"],[
    ("",["4部品のうち2つしか書けない",'<span class="kindtag unv">実測</span>',
      "仕様の種別に指定できる候補を数えた","候補が増えたとき"]),
    ("keyrow",["参照7つのうち1つだけが例外",'<span class="kindtag unv">実測</span>',
      "Document が持つ参照の型と、その属性の数を数えた"
      "（6つは属性1つの鍵、<code>schemaRef</code> だけが2つ）",
      "他の参照にも複数属性のものが見つかったとき"]),
    ("",["Schema が集約として説明できない",'<span class="kindtag unv">実測</span>',
      "コマンドの数と、不変条件の担保のされ方を数えた","コマンドを持たない集約が正しいと分かったとき"]),
    ("",["潰した境界は症状を出す",'<span class="kindtag unv">knowledge</span>',
      f"{K} ── 「a.b が必要なのに a まで圧縮すると、分ける必要がある境界を無視することになる」","—"]),
    ("",["4部品すべてを独立の単位にする",'<span class="kindtag out">Waffle の設計判断</span>',
      "<b>knowledge に該当なし</b>","—"]),
  ])
  + f'<p class="foldnote">最後の段に knowledge の裏付けが無い理由 ── {K} は'
    '「潰すと境界が消える」とまでは言うが、<b>どの単位まで分けて書けるようにするかは持たない</b>。'
    'ドメイン駆動設計が4部品を同じ格で置いていることを根拠に、'
    '仕様の側でも同じ格にするという判断は、この製品の側で置いている。</p>')

body = "".join([
 '<header><p class="eyebrow">Architecture Decision Record</p>'
 '<h1>値オブジェクトとエンティティを、独立して書ける単位にする</h1>'
 '<p class="lede">ドメインモデルの部品は4つあるが、仕様として独立に書けるのは2つだけだった。'
 '残る2つは集約の内側にしか置けない。'
 'その結果、<b>集約をまたぐ参照が、相手を特定する鍵ではなく相手の値を写して持っている</b>。</p></header>',

 sec("01","決定",None,
  '<div class="decision"><p class="main">Waffle は、'
  '<b>値オブジェクトとエンティティを、集約や業務サービスと同じ粒度で書ける単位にする</b>。'
  '集約は自分の一貫性の境界だけを持ち、値オブジェクトとエンティティを所有しない。参照する。</p>'
  '<div class="key"><span class="lbl">決め手</span>'
  '<span class="txt">Document が持つ参照7つのうち6つは、相手を特定する<b>1つの鍵</b>である。'
  '<code>schemaRef</code> だけが例外で、相手の名前と版を<b>写して持っている</b>。'
  '集約をまたいで型を参照できないことの帰結であり、'
  '<b>例外が1つだけ在ることは、その形が意図ではなかったことを示す</b>。</span></div></div>'),

 sec("02","変更前と変更後",
  "この決定が変えるのは、<b>仕様として何を独立に書けるか</b>である。ドメインモデルの部品そのものは変えない。",
  tbl(["","変更前 ── 集約が3つを兼ねる","変更後 ── 4部品が同じ格になる"],[
    ("keyrow",["仕様として独立に書ける部品","集約と業務サービスの<b>2つ</b>","<b>4つすべて</b>"]),
    ("",["集約をまたぐ参照",
      "相手の値を写して持つ。<code>SchemaRef</code> が名前と版を裸の文字列で抱え、"
      "Schema 側の振る舞いを失っている",
      "<b>相手を特定する鍵だけを持つ。</b>値は相手の側にある"]),
    ("",["集約が持つもの","一貫性の境界に加えて、エンティティと値オブジェクト","<b>一貫性の境界だけ</b>"]),
    ("",["置き場所が無い概念","他に段が無いので集約として書く（Schema がそうなった）","<b>本来の段へ置ける</b>"]),
  ])),

 sec("03","理由",
  "書ける単位が2つしかないため、集約をまたぐ参照が相手の値を写して持っている。"
  "<b>7つの参照のうち、例外は1つだけである。</b>",
  reason, reason_fold),

 sec("04","判断を分けた軸",
  "採る案は4つの条件のうち3つを満たす。落とすのは1つで、<b>いま集約の中にある10件が、それぞれ文書になる</b>。",
  axis, axis_fold),

 sec("05","付随して決めたこと",
  "分けると、集約と値オブジェクトの関係が決まる。3点を同時に決めた。",
  tbl(["論点","決定"],[
    ("keyrow",["集約と値オブジェクトの関係",
      "<b>所有せず、参照する。</b>同じ値オブジェクトを複数の集約が参照してよい"]),
    ("",["いま集約の中にある10件","独立した単位へ移す。移すのは別の作業として行う"]),
    ("",["写しになっている参照","移すときに<b>鍵1つへ寄せる</b>。相手の値は相手の側に置く"]),
  ]),
  fold("それぞれの根拠を開く（3件）",
    tbl(["論点","根拠"],[
      ("keyrow",["集約と値オブジェクトの関係",
        "所有のままだと、またがる概念をどちらの集約が持つかを決められない。"
        "参照にすれば、値オブジェクトは1つで、参照する側が何個あってもよい"]),
      ("",["いま集約の中にある10件",
        "移す作業には、写しになっているものを鍵へ寄せる判断が含まれる。"
        "決定と同じ場でやると、決定の可否が個別の判断に引きずられる"]),
      ("",["写しになっている参照",
        "参照は相手を特定できればよく、相手の値を持つ必要がない。持つと、相手が変わったときに写しが古くなる。"
        "<code>SchemaRef</code> は鍵1つへ寄せ、名前と版は Schema の側に置く"]),
    ]))),

 sec("06","答えないこと",
  "個別の移し方と、他の食い違いは、この決定では扱わない。",
  tbl(["項目","種別","行き先"],[
    ("keyrow",["Schema が最終的にどの単位になるか",'<span class="kindtag out">範囲外</span>',
      "<b>この決定は書ける単位を増やすだけ。</b>Schema をどこへ置くかは、移す作業で判定する"]),
    ("",["集約の中の10件をどう移すか",'<span class="kindtag out">範囲外</span>',
      "移す作業として別に行う"]),
    ("",["仕様の種別をどう増やすか",'<span class="kindtag out">範囲外</span>',
      "欄の形は、実装に着手する段で決める"]),
    ("",["業務サービスが仕様5件・実装24件で開いていること",'<span class="kindtag out">範囲外</span>',
      "この決定とは別の食い違い。別に測る"]),
  ])),

 '<section><h2><span class="num">07</span>承認</h2>'
 '<div class="approve"><span class="k">状態</span><span class="v">置き換えられた</span>'
 '<span class="m">2026-08-15 に承認（daidaiiro）。'
 '2026-08-17、<a href="https://claude.ai/code/artifact/2e293712-115f-48f0-8a3d-b54858e8b1c8">集約は、自分の境界の内側を持つ</a> が置き換えた。'
 '<b>「独立して書ける単位にする」は引き継がれている。</b>'
 '置き換わったのは「所有しない。参照する」の一節で、'
 '集約をまたぐ参照について測ったことを、境界の内側にも当ててしまっていた</span></div></section>',

 sec("08","関連","この決定は、Schema を説明する仕様を書けるようにするための前提になる。","",
  fold("縛る対象と、前後の決定を開く（4件）",
    tbl(["種類","対象"],[
      ("keyrow",["縛る対象","仕様として独立に書ける単位。以後、値オブジェクトとエンティティは集約に埋めない"]),
      ("",["引く knowledge",f"{K} ── 組み合わせを潰すと、分ける必要がある境界が消える"]),
      ("",["この決定を使う予定",
        "Schema を説明する仕様を起こす。"
        "いま schema が持つ10種の宣言のうち、図の宣言だけが値オブジェクトになっており、残りは仕様に存在しない"]),
      ("",["同じ形の先行する決定",
        "knowledge は、自分を読む相手を知らない（承認済み）── "
        "同じ対応が2か所にあって食い違っていた件。今回は値オブジェクトの定義で同じことが起きている"]),
    ]))),
])

html = ("<title>値オブジェクトとエンティティを、独立して書ける単位にする</title>"
        f"<style>{CSS}\n{extra}</style>"
        f'<div class="wrap">{body}</div>')
pathlib.Path("/home/daidaiiro/workspace/waffle/docs/adr/adr-value-object-as-a-unit.html").write_text(html, encoding="utf-8")
print("書いた", len(html), "bytes")
