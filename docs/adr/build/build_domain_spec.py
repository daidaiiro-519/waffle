import sys, pathlib
sys.path.insert(0,"/home/daidaiiro/workspace/waffle/docs/adr/build")
from _common import CSS, sec, fold, tbl, step, joint, ok, ng, extra

# ── 01 型の名前と版
name_tbl = tbl(["","いま","これから"],[
 ("keyrow",["型の名前","<code>DomainSpecSchema</code>","<code>DomainSchema</code>"]),
 ("",["版","<code>v11</code>","<code>v1</code>"]),
 ("",["どちらが正か","—","<b>新しいほうだけ。</b>v11 は移し終えるまで併存し、その後たたむ"]),
])

# ── 02 種別
kinds = tbl(["種別","いま何本","変更後","1本が何を言うか"],[
 ("keyrow",["事業領域","<b>0</b>","<b>1</b>","<b>業務領域を分類するときの基準になる、事業のまとまり</b>"]),
 ("",["業務領域","7","7","事業活動の1単位。中核・一般・補完の分類と、その根拠"]),
 ("",["区切られた文脈","1","1","この文脈で言葉が一貫する範囲"]),
 ("keyrow",["集約","2","2","<b>一貫性の境界。</b>1つのトランザクションで一緒に変わる範囲"]),
 ("keyrow",["エンティティ","<b>0</b>","<b>2</b>","<b>同一性を持ち、状態が変わるもの</b>"]),
 ("keyrow",["値オブジェクト","<b>0</b>","<b>10</b>","<b>値で等しさが決まり、変わらないもの</b>"]),
 ("",["業務ユースケース","30","30","外から呼ばれる、業務としてひとまとまりの用事"]),
 ("",["業務サービス","4","4","どの集約にも属さない、集約をまたぐ判断"]),
 ("",["<b>合計</b>","<b>44</b>","<b>57</b>","—"]),
])

# ── 03 共有する組み立て
defs = tbl(["組み立て","持つもの","どの種別が使うか"],[
 ("keyrow",["受け入れ基準","鍵／「〜のとき、〜となる」の1文",
   "集約が公開する操作・業務ユースケース・業務サービス"]),
 ("keyrow",["シナリオ","名前／分類（正常・異常）／観点／本文（Given・When・Then）／"
   "<b>確かめる単位</b>／覆う「常に満たすこと」（任意）","同上"]),
 ("",["属性","名前／型（値オブジェクトを指す鍵）／任意か","エンティティ・値オブジェクト"]),
 ("",["常に満たすこと","鍵／規則（「常に〜である」）／担保（型・検査）／なぜそう決めたか",
   "集約・エンティティ・値オブジェクト"]),
 ("",["文書への参照","鍵1つだけ。相手の値を写さない","集約・業務サービス・業務ユースケース"]),
 ("",["図の宣言","主張1つ／items／links／frame。図の中に図を入れられる",
   "<b>全種別</b>（段1 が持つ。Domain 固有ではない）"]),
])

# ── 04 ブロックの定義
def bl(kind, blocks):
    return ("keyrow" if kind in ("集約","エンティティ","値オブジェクト") else "",
            [f"<b>{kind}</b>", f"<b>{len(blocks)}</b>", "／".join(blocks)])

blocks_tbl = tbl(["種別","数","ブロック"],[
 bl("事業領域",["題","概要","名前の英語表記","この事業が顧客へ提供しているもの"]),
 bl("業務領域",["題","概要","名前の英語表記","この業務領域が属する事業領域",
   "分類","そう分類した根拠","使う既製品"]),
 bl("区切られた文脈",["題","概要","名前の英語表記","この文脈で通じる言葉"]),
 bl("集約",["題","概要","名前の英語表記","この集約の境界の内側","この集約が識別子で指す、外の集約",
   "この集約が常に満たすこと","この集約が外へ公開する操作","この集約が出す出来事"]),
 bl("エンティティ",["題","概要","名前の英語表記","同じものと見なす根拠","この文書が持つ属性",
   "この型だけで常に満たすこと"]),
 bl("値オブジェクト",["題","概要","名前の英語表記","この値が表すもの","この値が持つ属性",
   "この値の振る舞い","この値だけで常に満たすこと"]),
 bl("業務ユースケース",["題","概要","名前の英語表記","このユースケースが属する業務領域",
   "誰が何をしたいか","このユースケースが受け取る入力","基本の流れ",
   "このユースケースの受け入れ基準","基準を確かめるシナリオ","失敗の仕方",
   "終わったときに成り立つこと"]),
 bl("業務サービス",["題","概要","名前の英語表記","この計算が集約の内側に置けない理由",
   "この計算が触れる集約","受け取るものと、返すもの","この計算の受け入れ基準",
   "基準を確かめるシナリオ"]),
])

title_why = fold("題から識別子を落とす理由を開く",
 '<p class="foldnote">いまの題は「<b>Document の一貫性を守る集約：agg-document</b>」のように'
 '<b>説明と識別子を「：」でつないだ形</b>である。'
 '識別子は表紙の <code>documentId</code> が持っているので、'
 '題に繰り返すのは<b>導けるものを保存する</b>ことになる ── '
 '段1 が「導けるものは持たない」と決めた、そのものである。'
 'あわせて、<code>boundedContext</code>／<code>subdomain</code>／<code>usecase</code> という'
 '<b>名前だけを持つブロックが3つ</b>あったが、'
 '題を業務の言葉の名前にすれば要らなくなる。'
 '<b>3種別からブロックが1つずつ消える。</b></p>')

# ── 05 枝ごとの必須ブロック
req = tbl(["","必須","任意"],[
 ("keyrow",["全種別","題／概要／<b>名前の英語表記</b>","—"]),
 ("keyrow",["事業領域","<b>この事業が顧客へ提供しているもの</b>","—"]),
 ("keyrow",["業務領域","<b>属する事業領域</b>／分類／<b>そう分類した根拠</b>","使う既製品"]),
 ("",["区切られた文脈","この文脈で通じる言葉","—"]),
 ("",["集約","境界の内側／常に満たすこと／外へ公開する操作",
   "識別子で指す外の集約／出す出来事／操作ごとの<b>変える内側のエンティティ</b>"]),
 ("",["エンティティ","同じものと見なす根拠／持つ属性","この型だけで常に満たすこと"]),
 ("",["値オブジェクト","表すもの／持つ属性／振る舞い","この値だけで常に満たすこと"]),
 ("keyrow",["業務ユースケース",
   "<b>属する業務領域</b>／誰が何をしたいか／基本の流れ／終わったときに成り立つこと／"
   "受け入れ基準／<b>基準を確かめるシナリオ</b>","受け取る入力／失敗の仕方"]),
 ("",["業務サービス","集約の内側に置けない理由／受け取るものと返すもの／受け入れ基準／シナリオ",
   "触れる集約"]),
])

# ── 06 各所の指針
guide = tbl(["どこに置く指針か","何を書くか"],[
 ("keyrow",["すべての種別：名前の英語表記",
   "その語を<b>英語でどう言うか</b>だけを書く。"
   "<b>実装の型名でもファイル名でもない</b> ── 綴りの流儀は規約が導く。"
   "集約とそのルートのエンティティは<b>同じ英語表記になる</b>（名前は1つだから）"]),
 ("keyrow",["事業領域：この事業が顧客へ提供しているもの",
   "<b>誰に、何を</b>提供しているかを書く。これが<b>業務領域の分類の基準</b>になる ── "
   "中核・一般・補完は「差別化を生むか」で決まるが、"
   "<b>何に対する差別化かはこの事業領域が決める</b>。"
   "書くのは<b>いまの事業</b>であって、社名でも沿革でもない"]),
 ("keyrow",["業務領域：そう分類した根拠",
   "<b>この事業が提供しているものに照らして</b>、差別化を生むかを書く。"
   "<b>どれだけ作り込むかは書かない</b> ── それは技術者の判断で、規約が持つ"]),
 ("keyrow",["業務領域：境界は、ユースケースの集まりである",
   "<b>境界は一覧として持たない。導く。</b>"
   "その業務領域を指しているユースケースを数えれば出る。"
   "書き起こしは「業務領域の境界＝強く関連するユースケースの集まり。"
   "<b>同じアクター・同じ外部システムとつながり、密接に関係するデータを操作する</b>"
   "ユースケース群がひとつの業務領域を形成する」と定める。"
   "だから<b>集まりが空のものは業務領域ではない</b>"]),
 ("",["業務ユースケース：属する業務領域",
   "<b>必ず1つ書く。</b>書かないと、その用事は<b>どの業務活動にも紐づかず</b>、"
   "投資判断（中核・一般・補完）の対象から漏れる。"
   "<b>どの集約にも属さないエンティティと同じ形</b>である"]),
 ("keyrow",["集約：この集約の境界の内側",
   "ここへ入れてよいのは、<b>1つのトランザクションで一緒に変わらなければならないもの</b>だけ。"
   "別々に変えてよいなら、それは別の集約であり、境界の外として鍵で指す。"
   "<b>集約はできるだけ小さく</b>"]),
 ("keyrow",["集約：この集約が外へ公開する操作",
   "操作ごとに<b>この集約を変えるか</b>を書く。"
   "変えるものには、<b>常に満たすことを覆うシナリオ</b>が要る。"
   "変えないものには要らない ── 破りようがないため。"
   "<span class='sub'>（変えるものを、ドメイン駆動設計ではコマンドと呼ぶ）</span>"]),
 ("",["集約：変える内側のエンティティ",
   "階層を持つ集約でだけ書く。<b>必須にしない</b> ── "
   "どの内側のエンティティが変わるかを知らなくても、"
   "シナリオが結果を言っているので<b>実装は一意に導ける</b>。"
   "つまり<b>仕様の下限より上</b>の情報である"]),
 ("",["エンティティ：同じものと見なす根拠",
   "<b>何が変わっても同じものだと言える根拠</b>を書く。"
   "どの集約に属するかは<b>書かない</b> ── 集約の側の一覧が指す"]),
 ("",["値オブジェクト：この値の振る舞い",
   "<b>変わらないこと</b>と、<b>値が等しければ等しいこと</b>を書く。"
   "変わるものは値ではなくエンティティである"]),
 ("keyrow",["シナリオ：確かめる単位",
   "そのシナリオが<b>どの単位の振る舞いを確かめているか</b>を、<b>仕様の識別子で指す</b>。"
   "実装がどんな形（クラス・関数・手続き）で実現するかは書かない ── "
   "形は規約と実装の判断である。<b>これが、仕様と実装を結ぶ三角形の1辺になる</b>"]),
 ("",["一覧を持つ側か、属する側が宣言するか",
   "その集まりが<b>閉じていて、閉じていること自体が主張</b>なら、持つ側が一覧を持つ"
   "（集約の境界の内側）。<b>増え続ける集まり</b>なら、属する側が宣言する"
   "（業務ユースケース→業務領域、業務領域→事業領域）。<b>両側に書かない</b>"]),
 ("",["常に満たすこと（置き場所）",
   "その規則を確かめるのに<b>その型ひとつを見れば足りる</b>なら、その型が持つ。"
   "他を見る必要があるなら集約が持つ"]),
 ("",["業務ユースケース：基本の流れ",
   "誰と誰の間で、どの順に何が行き来したかを書く。"
   "<b>「やり取り」の主張</b>で宣言する ── 順序・戻り・繰り返し・分かれはその中に入る"]),
 ("",["図：どの主張を選ぶか",
   "<b>16の主張の定義文をそのまま置く。</b>書き手は主張を選ぶだけで、<b>描き方は選ばない</b>"]),
])

# ── 07 描き方
draw = tbl(["","決めたこと","理由"],[
 ("keyrow",["形式","<b>HTML</b>",
   "<b>人には畳み、AI には全部見せられる。</b>畳んだ中身はファイルには最初から在るので、"
   "AI は畳まれているかを意識しない。人だけが選んで開く"]),
 ("",["図","16の主張。図の中に図を入れられる",
   "描き方は主張から従属して決まる。書き手は選ばない"]),
 ("",["畳むもの","根拠・裏付け・全件の一覧",
   "読み手が判断するのに要らないものを、既定で外へ出す"]),
 ("",["畳まないもの","題・概要・決めたこと・受け入れ基準",
   "これを畳むと、開かないと何も分からない頁になる"]),
])

draw_why = fold("Markdown を採らない理由を開く",
 '<p class="foldnote">Skill・Knowledge・Agent が Markdown なのは<b>読みやすさで選んだのではなく</b>、'
 '<code>SKILL.md</code> のように<b>ツールがそのファイル名でしか読まないから</b>である。'
 'Domain にはそれが無いので、初めて選べる。'
 'Markdown の側に残る利点は「git の差分が読める」だけだが、'
 '<b>正本は document.json で、成果物は毎回描き直される投影</b>なので、'
 'その差分を読む必要が無い ── 段1 の「導けるものは持たない」と同じ筋である。'
 'また突き合わせの機械が読むのは document.json であって成果物ではないので、'
 '<b>形式は判定に一切効かない</b>。成果物は人が読むためだけのものである。</p>')

# ── 08 宛先
dest = tbl(["種別","置き場所"],[
 ("keyrow",["事業領域","<code>.waffle/specs/business-domain/&lt;名&gt;.html</code>"]),
 ("keyrow",["業務領域","<code>.waffle/specs/subdomain/&lt;名&gt;.html</code> "
   "<b>── 文脈の配下ではない</b>"]),
 ("",["区切られた文脈","<code>.waffle/specs/&lt;文脈&gt;/&lt;文脈&gt;.html</code>"]),
 ("",["集約","<code>.waffle/specs/&lt;文脈&gt;/aggregate/&lt;名&gt;.html</code>"]),
 ("",["エンティティ","<code>.waffle/specs/&lt;文脈&gt;/entity/&lt;名&gt;.html</code>"]),
 ("",["値オブジェクト","<code>.waffle/specs/&lt;文脈&gt;/value-object/&lt;名&gt;.html</code>"]),
 ("keyrow",["業務ユースケース","<code>.waffle/specs/&lt;文脈&gt;/usecase/&lt;名&gt;.html</code> "
   "<b>── 業務領域の配下ではない。文脈の下に集まる</b>"]),
 ("",["業務サービス","<code>.waffle/specs/&lt;文脈&gt;/domain-service/&lt;名&gt;.html</code>"]),
])

open_tbl = tbl(["論点","いまの状態","要る判断"],[
 ("keyrow",["業務領域と業務ユースケースの置き場所",
   "<b>承認待ちの記録「業務領域を、区切られた文脈の外へ出す」（2026-08-11）</b>を引き継いだ。"
   "knowledge 19本を書き起こしから作り直して測り直し、助言役2者が当初の反対を撤回している",
   "<b>この型を承認すると、その記録も一緒に承認したことになる。</b>"
   "切り離したいなら言ってほしい"]),
])

closed_tbl = tbl(["論点","決めたこと","何から導いたか"],[
 ("keyrow",["業務領域の <code>実装の指針</code>",
   "<b>落とす。規約へ寄せる</b>",
   "中身は「一般ゆえ既存ツールを薄く呼び出すだけ…」で、"
   "<b>実装をどう作るかの話</b>だった。"
   "「差別化を生むか」は事業が決め、「ならどれだけ作り込むか」は技術者が決める ── "
   "<b>決める人が違うので別の文書にある</b>"]),
 ("keyrow",["名前の英語表記",
   "<b>全種別が持つ</b>",
   "規約が<b>綴りの流儀</b>（大文字小文字・接尾辞・置き場所）を導くための元になる。"
   "英訳は機械的に導けないので、宣言が持つ。"
   "<b>実装の型名は書かない</b>ので、承認済みの「仕様は実装の型名を持たない」は保たれる"]),
 ("",["操作に、変える内側のエンティティを書かせるか",
   "<b>書ける。必須にしない</b>",
   "<b>仕様の下限は、具体が一意に導けるかで測る</b>（承認済み）── "
   "シナリオが結果を言うので実装は導ける。つまり下限より上。"
   "それでも型が表せないと、階層を持つ集約を書けなくなる"]),
 ("keyrow",["エンティティ・値オブジェクトの置き場所",
   "<b>文脈の直下</b>",
   "<b>所属はディレクトリではなく宣言が運ぶ</b>＋<b>集約が境界の内側の一覧を持つ</b>。"
   "集約の下へ置くとディレクトリが所属を運び、"
   "<b>値オブジェクトが複数の集約から使われたときに1親制約が出る</b> ── "
   "業務領域で直したのと同じ病"]),
 ("keyrow",["業務ユースケースの <code>operationIndex</code>",
   "<b>落とす</b>",
   "1つのユースケースが複数の操作へ分岐するのは<b>受け口の形</b>であって、"
   "業務の用事の数ではない。<b>受け口の都合が仕様に漏れていた</b>。"
   "1つの用事＝1本。まとめ方は規約の管轄"]),
 ("",["<code>DomainSchema/v1</code> という名前",
   "<b><code>Spec</code> を落とす</b>",
   "型の名前で「これは仕様である」という分類を表すと、"
   "<b>分類が変わったときに名前を変える</b>ことになる。"
   "分類は名前ではなく欄が言う。8つ全部が <code>〜Schema</code> で揃う"]),
])

body = "".join([
 '<header><p class="eyebrow">段2 の仕様 ── 1つめ</p>'
 '<h1>Domain の型</h1>'
 '<p class="lede">段1 が用意した部品で、Domain を1本の型として宣言する。'
 '種別は<b>8つ</b>、文書は<b>44本から57本</b>になる。'
 '増えるのは、集約の内側から出てくる<b>エンティティ2本と値オブジェクト10本</b>と、'
 '業務領域を受けとめる<b>事業領域1本</b>である。'
 '<b>この頁は bc-waffle だけを見ている。</b></p></header>',

 sec("01","型の名前と版",
  "既存へ肉付けせず、新しい名前で立てる。移し終えるまでは併存する。",
  name_tbl),

 sec("02","種別",
  "<b>描かれる成果物の単位</b>で切る。それ以外の切り方はしない。",
  kinds,
  fold("なぜこの8つかを開く",
    '<p class="foldnote">種別は<b>1本の文書として描かれるかどうか</b>だけで決める。'
    'ドメイン駆動設計が同じ格で置いている4部品（値オブジェクト・エンティティ・集約・業務サービス）に、'
    '事業領域・業務領域・区切られた文脈・業務ユースケースを足した形である。'
    '<b>エンティティと値オブジェクトが新設</b>なのは、'
    'いままで集約の内側にしか置けなかったためで、'
    'そのせいで文書どうしの参照が相手の値を写して持っていた。'
    '<b>事業領域が新設</b>なのは、業務領域を文脈の外へ出したときに'
    '置き場所だけが宙に浮くのを避けるためである ── '
    '器を切るだけでは、宣言の無い置き場所がまた意味を運ぶ。'
    '<b>状態や工程を種別にはしない</b> ── 導けるものだからである。</p>')),

 sec("03","共有する組み立て",
  "同じものを種別ごとに別の名前で持たない。<b>6つを共有する。</b>"
  "シナリオの<b>確かめる単位</b>は、仕様と実装の乖離を捕まえるために足す"
  "（<a href=\"https://claude.ai/code/artifact/1ebe3dff-e9dd-4967-b736-ca725fd59f59\">結び目の設計</a>／"
  "<a href=\"https://claude.ai/code/artifact/fc3626b7-6755-4848-810a-a2c7799dd3bf\">ずれたとき何が赤くなるか</a>）。",
  defs,
  fold("いま3つの名前で呼ばれているものを開く",
    tbl(["いまの名前","付く場所","これから"],[
      ("keyrow",["<code>invariantScenarios</code>","集約","<b>シナリオ</b>"]),
      ("keyrow",["<code>acceptanceScenarios</code>","ユースケース／業務サービス","<b>シナリオ</b>"]),
      ("",["<code>acceptanceCriteria</code>","ユースケース／業務サービス","<b>受け入れ基準</b>"]),
      ("",["（公開する操作には無い）","—","<b>両方を持たせる</b>"]),
    ]))),

 sec("04","ブロックの定義",
  "種別ごとに、書くブロックを並べる。<b>題と概要は全種別が持つ。</b>",
  blocks_tbl, title_why),

 sec("05","枝ごとの必須ブロック",
  "必須と任意を分け、<b>宣言に無いブロックは書けないように閉じる</b>。",
  req),

 sec("06","各所の指針",
  "書き手が迷う場所にだけ置く。<b>迷わない場所には置かない。</b>",
  guide),

 sec("07","描き方",
  "<b>HTML</b>。読み手のツールが形式を決めていない型なので、初めて選べる。",
  draw, draw_why),

 sec("08","成果物の宛先",
  "<b>業務領域と業務ユースケースが、文脈の配下から出る。</b>"
  "あわせてエンティティと値オブジェクトの置き場所を足す。",
  dest),

 '<section><h2><span class="num">09</span>承認</h2>'
 '<div class="approve"><span class="k">状態</span><span class="v">未承認</span></div></section>',

 sec("10","閉じた論点",
  "4点を、<b>承認済みの決定と原理から閉じた</b>。実物の件数は根拠にしていない。",
  closed_tbl),

 sec("11","まだ決めていないこと",
  "<b>1点だけ。</b>判断をもらうしかないものである。",
  open_tbl),
])

extra2 = extra + """
.figbox{border:1px solid var(--rule);border-radius:2px;background:#FFF;padding:1rem}
.wf-fig{display:block;max-width:100%;height:auto;overflow:visible}
.sub-h{font-family:var(--serif);font-size:1rem;font-weight:600;margin:.5rem 0 -.3rem;color:var(--ink-soft)}
"""
html = ("<title>Domain の型</title>"
        f"<style>{CSS}\n{extra2}</style>"
        f'<div class="wrap">{body}</div>')
pathlib.Path("/home/daidaiiro/workspace/waffle/docs/adr/tier2-domain.html").write_text(html, encoding="utf-8")
print("書いた", len(html), "bytes")
