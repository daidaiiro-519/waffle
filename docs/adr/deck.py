import sys
sys.path.insert(0,'.waffle/skills/brainstorm/references')
sys.path.insert(0,'/tmp/claude-1000/-home-daidaiiro-workspace-waffle/9e3fe882-051f-45f2-be7c-bc7e812aea94/scratchpad')
from build_board import Topic, Option, Table, deck, write, _table
from figs import FIG1, FIG2
from recfigs import REC
from linefig import LINE
from concrete import CONCRETE
from projfig import PROJ
from projtbl import PROJTBL, GROUPPROJ
from difffig import DIFF
from gatefig import GATE, WHERE
from figs2 import PROMPT, MIGRATE, QUERY
from migtime import MIGTIME
from adrfig import ADRFIG
from codfig import CODCUT
from codlayer import CODLAYER
from speclayer import SPECLAYER
from bucketfig import BUCKETS
from corefig import RULECORE
from toolfig import TOOLS
from onewayfig import ONEWAY
from sizefig import SIZE
from treefig import TREE
from jslist import JSLIST
from composefig import COMPOSE

t1 = Topic(1, "三層", "schema は厚い一枚か、薄い核＋複数の契約か", "決着",
  answer="どちらでもなく、<b>メタレベルの三層</b>",
  note="厚い／薄いは同じ階層での責務の配分の話で、実際に起きているのは<b>階層が1つ増えること</b>である。厚さを比べても、この構造は出てこない。",
  figures=[REC[0]],
  extras=[("「値だけを推論させる」は再帰する", _table(
    ["何を書くとき", "構造を縛るもの", "推論に載るもの"],
    [["文書エンティティ", "concrete schema", "値だけ"],
     ["<b>concrete schema</b>", "<b>abstract schema</b>", "<b>欄の設計だけ</b>"]])
    + "<p class='lead'><b>concrete schema を書く行為そのものが、構造の推論である。</b>"
      "domain schema を新しく起こすとき、いま人とAIが「どんな欄が要るか」を推論している。"
      "それを縛るのが abstract schema であり、<b>これが段1の存在理由になる</b> ── "
      "共通部分をくくり出したのではなく、同じ原理をもう1段上に当てている。</p>")],
  decision=[("決定", "schema は、abstract／concrete／文書エンティティの三層で捉える"),
            ("理由", "concrete schema は abstract schema のインスタンスであり、同時に文書の型でもある。この二重の役が核心で、JSON Schema 自身が meta-schema を持つのと同じ形である"),
            ("効くこと", "段1 は「共通部分」ではなく「もう1段上の同じ原理」として定義される")])

t2 = Topic(2, "単位", "世界の単位は、文書か1つのグラフか", "決着",
  answer="<b>単位は読み手に従属する。</b>人＝文書／AI＝節",
  note="偽の二択だった ── <b>「単位」という語が4つの別物を指していた</b>（人が読む単位／検証と状態の単位／取得と参照の単位／保存の単位）。",
  figures=[REC[3], REC[4]],
  extras=[("足したい条件が2つある",
    "<p><b>① 構造が意味的空間になるのは、命名が守られているときだけである。</b>"
    "構造は型を保証するが、意味は保証しない。<code>invariants[]</code> に不変条件でないものが書かれても検証は通る。"
    "だから書き方の指示とセットで初めて成り立つ。帰結として<b>意味は検証できない</b> ── "
    "最後に人の承認が残る理由がここで説明できる。</p>"
    "<p><b>② 構造化の利得は「無いことが分かる」ことである。</b>"
    "grep は「見つからなかった」と「無い」を区別できない。構造で引けば「その欄が空である」と言い切れる。</p>"),
   ("代償", "<p><b>文書をまたぐ規則の置き場所が無くなる。</b>"
    "「この仕様が指しているユースケースは実在するか」は、どの1つの文書のスキーマでも検証できない ── "
    "範囲が文書を超えているためである。<b>これが論点6の入口になる。</b></p>")],
  decision=[("決定", "人間が読む検証・状態・承認は文書単位、AI が行う取得・参照は節単位に置く"),
            ("理由", "形式は読み手の認知に従属し、単位は形式に従属する。人間はビジュアライズされていないと認知できず、AI は形式を問わない"),
            ("次", "論点3 へ進む。ただし問いが変わった ── 「名前か印か」ではなく「節に別の同一性が要るか」になる")])

t5 = Topic(5, "型の増え方", "型は縦に増えるか、横に増えるか", "決着",
  answer="<b>横。</b>動詞は abstract が固定する",
  note="三層に置き直した時点で、この論点は自動的に閉じた。<b>ただし論点6を前提にしている</b> ── 規則をコマンドで持つなら、結局は縦に増える。",
  decision=[("決定", "型は横に増える。動詞は abstract schema が固定する"),
            ("崩れる条件", "論点6 が案A（規則をコマンドで持つ）に倒れた場合。そのときは型を足すたびに動詞が増え、縦に増えることになる")])

t3 = Topic(3, "同一性", "AI が引く節に、人が読む文書とは別の同一性が要るか", "決着",
  answer="<b>A ── 節に、変わらない印を持たせる</b>",
  note="論点2 の決着で<b>問いが変わった</b> ── 「名前か印か」ではなく、節が文書とは別の同一性を持つかどうかになった。この論点が決まると、論点6 の前提も動く。",
  figures=[(FIG1, "<b>3案の差は、参照が何を鍵にするかである。</b>同じ「名前を直す／別の親へ移す」を3案に通した。"),
           (FIG2, "<b>同じ問いが、どちらの段に荷を置くかの選択でもある。</b>段2 に何も置かない案は A だけである。"),
           REC[6]],
  kept=[
   Option("節に、変わらない印を持たせる",
          "機械が発番する印を鍵にする。人が読む射影では出さない",
          "節を手で書き足せない。印の重複と散逸を検査する責務が増える。印は意味を運ばない",
          before="同一性は名前か、変わらない印か（節と文書を区別せずに問うていた）",
          why="論点2 が「取得と参照は節単位」で決着したことで、指される側が節に定まった。名前と印の比較ではなく、節が文書とは別の同一性を持つかどうかが問いになった"),
   Option("位置を同一性とし、動かすときは宣言する",
          "〈文書の識別子＋アドレス〉を鍵にし、改名・移動は移行の操作として書く",
          "宣言の書き忘れが観測できない。忘れたこと自体が、不在として現れない",
          before="改名を移行操作の宣言で運ぶ（独立の案として立てていた）",
          why="位置を鍵にする案と中身が同じだった。位置を鍵にする限り、位置が変わったことを別途宣言する以外に追跡手段が無い。2案ではなく、1案の必然の帰結である"),
   Option("参照の側に、解決状態を持たせる",
          "鍵は名前のまま。指す先へ届いたかを、参照が申告する",
          "「移った」と「消えた」を区別できない。直し方は毎回、人が決める"),
  ],
  tables=[Table("この選択が、段1と段2に置くもの", ["段1　全型が継ぐ契約", "段2　8つの型"],
    {"A": ["印を持つ／採番と不変／参照は印を指す／射影で落とす　<b>＝3つ増える</b>",
           "<b>増えない。</b>既にある document への後付けの発番だけ"],
     "B": ["移行の操作を動詞として固定。<b>型が増えるほど太る</b>",
           "各型が位置の安定を守る。アドレスが動くと、指す参照が全部書き換わる"],
     "C": ["参照の語彙に、型と解決状態。<b>同一性は増えない</b>",
           "各型が、参照の欄で状態を申告する"]},
    lead="いま再定義しているのは<b>段2</b> である。")],
  dropped=[
   ("中身のハッシュを鍵にする", "中身を1文字直すと別物になる。エンティティの定義と衝突する"),
   ("外から指される節にだけ印を与える", "指す側は後から現れる。後で発番すると、それより前の突き合わせが切れる"),
   ("人が手で印を書く", "人が読む単位に機械の鍵が現れる。しかも手書きの印は重複する"),
  ],
  found=[
   "問いが「要るか」から<b>「宣言で持つか、位置から導くか」</b>へ変わった",
   "「要らない」に当たるのは <b>C だけ</b>である。A と B は持ち方の違いにすぎない",
   "<b>C は A と両立する。</b>排他なのは「指される側に同一性を置くか」だけで、「指す側が届いたかを申告するか」は別に決められる",
   "JSON Schema に足りない6つのうち、<b>「同一性」と「実体どうしの参照」は同じ1つの穴の両側</b>である",
  ],
  pick=("A", "節に、変わらない印を持たせる。参照と突き合わせの鍵を、名前から印へ移す"),
  grounds=[
   ("印を鍵にすること", "節を別の親へ移すと、名前を鍵にした差分は <code>removed 1 / added 1</code>、印を鍵にすると <code>moved 1</code>", "実測", "原型（`docs/brainstorm/kata-prototype`）での実測"),
   ("指される側を実体にすること", "AI生成仕様628本で、参照の <b>33%</b> が指す先に届いていない", "実測", "`spec-correspondence/02-dataset` の実測（アンカー1,205件）"),
   ("位置を鍵にしないこと", "いま段1・段2 を再定義していて、構造そのものが動いている", "決まり", "この作業の現在地（論点1・2・5 が決着し、段2 は未定）"),
  ],
  costs=["人が読む射影で印を落とす。印を採番する道具を通さずには、節を書けなくする"],
  weaknesses=["印は意味を運ばない。人が印だけを見ても、それが何のことかは分からない"],
  decision=[
   ("決定", "<b>節に、変わらない印を持たせる。</b>参照と突き合わせの鍵を、名前から印へ移す"),
   ("理由", "参照が実体と実体を結ぶ以上、指される側は実体でなければならない。位置を鍵にすると、参照の妥当性がいま最も動いているもの（構造そのもの）に依存する"),
   ("引き受ける", "人が読む射影では印を落とす。印を採番する道具を通さずには、節を書けなくする。印の重複と散逸は、検査で見る"),
   ("重ねて採れる", "案C（参照の側の解決状態）は排他ではない。指す側が届いたかを申告するかは、別に決められる"),
   ("次", "論点6 へ進む。<b>印が決まったことで、宣言が指せる先が実体になった</b>"),
  ])

t6 = Topic(6, "規則の置き場所", "規則を、どこまで宣言で持つか", "決着",
  answer="<b>B ── 述語 ＋ 検めることの名前。名簿は2段</b>",
  note="<b>問いを作り直した。</b>もとは「コマンドで持つか宣言で持つか」だったが、"
       "案A（コマンド）は論点5 の決まりと衝突して落ちた ── 型が増えるたび abstract へ動詞を足しに行くことになる。"
       "残ったのは<b>宣言の中のどこに線を引くか</b>である。"
       "論点3 が A で決まり、述語の<b>対象</b>は書けるようになった。開いているのは述語の側だった。"
       "<b>ここでの「検査」は、宣言した規則が守られているかを確かめる操作を指す</b> ── "
       "構造が型に合うかを見る JSON Schema の検証とは別で、<b>構造は合っているのに内容が違反している</b>状態を見る。",
  figures=[LINE],
  tables=[Table("検査の実例 ── いま実際に走っているもの",
    ["何を確かめるか", "どちら側か", "いまどこに在るか"],
    {"構造が型に合う": ["節の欄が、宣言された形をしているか", "<b>そもそも規則ではない</b>", "JSON Schema そのもの（<code>validate</code>）"],
     "対が揃っている": ["受け入れ基準を持たない操作が無いか", "<b>述語</b>（1つの節で決まる）", "まだ規則として書けていない"],
     "印が指す先が在る": ["参照が、実在する節に届いているか", "<b>述語</b>（1つの参照の解決）", "<code>check-spec-integrity</code>（走査で実装）"],
     "版がそろっている": ["schema の版と、文書の版がずれていないか", "能力を要する（全文書を舐める）", "<code>check-schema-version-drift</code>"],
     "実装とずれていない": ["シナリオと、実装したコードがずれていないか", "能力を要する（構文解析）", "<code>check-scenario-drift</code> ・ <code>scan-source-code</code>"],
     "原文に在る": ["出典として書いた文字列が、落とした原典に実在するか", "能力を要する（外部ファイルを読む）", "CodingSkills の <code>check.py</code>"],
     "雛形の節を持つ": ["規約が、その種類の雛形の節をすべて持つか", "能力を要する（別ファイルと突き合わせる）", "同上"]},
    lead="<b>いま「検査」と呼んでいるものは、この7つのような操作である。</b>上2つは1つの節を見れば決まり、下4つは外を舐めないと決まらない。", plain=True),
   Table("名簿を、どこに置くか",
    ["型が1つ増えたとき", "spec だけに要る検査（scenario-drift）", "未登録の名前を規則が指せるか"],
    {"abstract に全部置く": ["<b>abstract に名前が積もる</b>", "abstract が持つ（他の型には無用）", "指せない（schema が閉じる）"],
     "2段に分ける": ["<b>abstract は変わらない</b>", "<b>その concrete が自分で宣言する</b>", "指せない（schema が閉じる）"],
     "実装の登録簿に置く": ["abstract は変わらない", "実装が持つ", "<b>指せる ── 名前の実在を schema が保証できない</b>"]},
    lead="<b>abstract が持つのは、全ての型が共通して要る検査だけである。</b>"
         "型ごとの検査を abstract へ置くと、案A（コマンドで持つ）と同じ太り方をする。", plain=True),
   Table("この線が置くもの",
    ["規則の一覧を読んで分かること", "書けない検査の行き先", "検査を1つ足すとき", "段1（abstract）が持つもの"],
    {"A": ["違反の条件そのもの", "<b>規則の外。一覧から消える</b>", "abstract の検査器を直す",
           "型契約 ＋ 動詞 ＋ 述語の語彙"],
     "B": ["違反の条件と、検めることの名前（中身は名簿）", "名前で指され、<b>一覧に残る</b>",
           "その型の concrete へ宣言を足す（共通なら abstract）",
           "型契約 ＋ 動詞 ＋ 述語の語彙 ＋ <b>検査の宣言の仕方</b> ＋ 共通の検査"]},
    lead="どちらも「宣言で持つ」。違うのは、宣言に収まらない検査を指せるかどうかである。")],
  kept=[
   Option("述語だけを宣言に持つ",
          "宣言できるのは、節の印が指す先について言える述語（在る・等しい・含む・対が揃う）だけ。"
          "走査や循環検出は規則として持たない",
          "<b>規則の一覧に穴が空き、その穴が見えない。</b>「何を検めていないか」を誰も言えない"),
   Option("述語 ＋ 検めることの名前を持つ。<b>名簿は2段に分かれる</b>",
          "述語に加えて「誰が検めるか（機械／人）」と「登録された検査の名前」を宣言が持つ。"
          "<b>全ての型に共通の検査は abstract が、その型だけの検査は concrete が自分で宣言する</b>",
          "<b>宣言だけを読んでも、その検査が何を見るかは分からない</b> ── 名簿を引かないと閉じない。"
          "さらに<b>同じ検査が別の型で別名で宣言されうる</b>",
          before="述語 ＋ 検めることの名前を持つ（名簿は abstract が持つ）",
          why="名簿を abstract に置くと、<code>scenario-drift</code> のような<b>特定の型にしか無い検査の名前が"
              "abstract へ積もる</b> ── 案A（コマンドで持つ）を落とした理由と同じ壊れ方をする。"
              "名簿を2段に分け、共通の検査だけを abstract に残した"),
  ],
  dropped=[
   ("規則をコマンドで持つ",
    "型が増えるたび abstract へ動詞を足しに行くことになり、論点5 の決まり（型は横に増える。動詞は abstract が固定する）と衝突する"),
   ("式の言語を宣言に埋める（JMESPath ・ CEL）",
    "式が書ける範囲が、そのまま abstract の契約になる。しかも式は1文書に閉じるので、走査も循環検出も結局書けない ── 線は動かず、言語が1つ増えるだけ"),
   ("任意の命令文字列を宣言に書ける",
    "宣言が任意コードの実行口になり、schema が何を保証するかを言えなくなる。他の実装へ持って行くこともできない"),
  ],
  found=["<b>線は「宣言で書けるか」ではなく、「abstract が名前を配れるか」で引ける</b> ── 検査の実体が外にあっても、名前が abstract の語彙なら、規則は宣言のまま閉じる",
         "<b>同じ線を、すでに1度引いていた。</b>CodingSkills の規約は「検証方法（機械／人）」と「実行コマンド」だけを持ち、走査と原文照合の実体は <code>check.py</code> が持つ ── 規約側は名前と命令で閉じている",
         "<b>落ちたのは案Aだけではない。</b>式の言語（案C）も、線を動かさずに語彙を1つ増やすだけだった ── 表現力を上げても、1文書の外は見えない",
         "<b>名簿を abstract に置くと、案Aと同じ壊れ方をする。</b><code>scenario-drift</code> は spec 系の型にしか無く、"
         "それを abstract が持てば型が増えるたび名前が積もる ── <b>abstract が持てるのは、全ての型が共通して要る検査だけである</b>"],
  pick=("B", "述語に加えて「誰が検めるか」と「登録された検査の名前」を宣言に持つ。"
             "abstract は<b>検査の宣言の仕方</b>と、全ての型に共通の検査だけを持ち、"
             "<b>その型だけの検査は concrete が自分で宣言する</b>"),
  grounds=[
   ("名前で指させること", "無いことが見えなければ、足りないことに気づけない", "決まり", "CodingSkills 論点11 の決まり（索引は導出し、不在を観測できるようにする）"),
   ("検査の実体を宣言の外へ置くこと", "規約37本が「検証方法 ＋ 実行コマンド」だけを持ち、検査は <code>check.py</code> が担って回っている", "実測", "CodingSkills（規約37本を検査16件で検証）── <b>原文の文字列照合は、内容を機械で裁こうとして誤検知したため、2026-09-06 に落とされた</b>"),
   ("名簿を2段に分けること", "型が増えるたび abstract に名前が積もるなら、動詞が積もるのと同じことが起きる", "決まり", "論点5 の決まり（型は横に増える。abstract は太らない）"),
   ("abstract に語彙だけを置くこと", "abstract は全ての型が継ぐ契約なので、そこに置けるのは全ての型に共通するものだけである", "決まり", "論点1 の決まり（三層のメタ構造）"),
   ("任意の命令を書かせないこと", "宣言が任意コードの実行口になると、schema が何を保証するか言えなくなる", "前提", "落とした案Dの構造からの帰結"),
  ],
  extras=[("具体で見る ── 同じ4つの規則を、案Aと案Bで書く", CONCRETE),
   ("名前の実体は、どこに置くか", """<p class="lead">宣言が指すのは名前であり、<b>その名前が何に解決されるかは実装側の話である。</b>
ただし置き場所は2つに分かれる ── <b>検査が集約の境界の内側で閉じるかどうか</b>で決まる。</p>
<table><tr><th>検査</th><th>答えを出すのに要るもの</th><th>名前の実体を置く場所</th></tr>
<tr><td>VALIDATED なら検証結果の置き場所を持つ</td><td>その文書だけ</td><td><b>集約</b>（不変条件）</td></tr>
<tr><td>節の対が揃っている</td><td>その文書だけ</td><td><b>集約</b>（不変条件）</td></tr>
<tr><td>参照が実在する節に届く</td><td>他の文書</td><td><b>ドメインサービス</b></td></tr>
<tr><td>schema の版がそろう</td><td>他の文書 ＋ schema</td><td><b>ドメインサービス</b></td></tr>
<tr><td>シナリオと実装がずれていない</td><td>ソースの木ぜんぶ</td><td><b>ドメインサービス</b></td></tr>
<tr><td>出典の文字列が原典に在る</td><td>落とした原文</td><td><b>ドメインサービス</b></td></tr></table>
<p class="lead"><b>集約にまたぐ検査を持たせると、集約が内側から他の集約を引くことになる</b> ──
そこで集約は一貫性の境界ではなくなり、何でも入る箱になる。</p>
<table><tr><th>置き方</th><th>そう置く理由</th></tr>
<tr><td>検査は「コマンド」と呼ばない</td><td>コマンドは状態を変える意図を指す。検査は状態を変えず判定を返すので、<b>仕様か問い合わせのサービス</b>である</td></tr>
<tr><td>モデルが「自分の構造契約はこの schema」と持つ</td><td>schema がモデルを知ると、構造契約が実装に依存する。<b>向きはモデル → 仕様</b>である</td></tr>
<tr><td>名前の実在は、schema 自身が閉じる</td><td>未実装を落とすコンパイラがいない。<b>名簿に無い名前を規則が指せない</b>ことで閉じる</td></tr></table>""")],
  costs=["宣言だけでは、その検査が何を見るかは分からない ── 名簿を引く手間が要る",
         "同じ検査が、別の型で別名で宣言されうる。共通だと分かった時点で abstract へ引き上げる操作が要る"],
  decision=[
   ("決定", "<b>述語に加えて「誰が検めるか」と「登録された検査の名前」を宣言に持つ。</b>"
            "abstract は検査の宣言の仕方と、全ての型に共通の検査だけを持ち、"
            "<b>その型だけの検査は concrete が自分で宣言する</b>"),
   ("理由", "検査の中身が宣言の外にあることは、どちらの案でも変わらない。違うのは<b>「まだ検めていない」を数えられるか</b>である ── "
            "案Aでは、その規則が一覧に無いので穴が穴として現れない"),
   ("引き受ける", "宣言だけでは、その検査が何を見るかは分からない（名簿を引く）。"
                "同じ検査が別の型で別名で宣言されうるので、共通と分かった時点で abstract へ引き上げる"),
   ("実装の置き場所", "名前の実体は、<b>境界の内側で閉じる検査なら集約、またぐならドメインサービス</b>に置く。"
                   "検査は状態を変えないので「コマンド」とは呼ばない"),
   ("次", "論点8（表現の型と射影）へ進む。<b>論点6・8 は互いの前提になっていない</b>"),
  ],
  weaknesses=["<b>どの検査が「全ての型に共通」かは、まだ引いていない。</b>参照の解決と版のずれは共通に見えるが、確かめていない",
              "<b>宣言された名前に、結ばれた実体が在るか</b>をいつ確かめるかは決めていない ── 落とす相手がコンパイラではないので、確かめる時点を別に決める必要がある",
              "人が検める規則は、名前を持っても走らせられない ── <b>「検めた」という記録をどこに置くかは、この論点では決めていない</b>（論点7 に落ちる）"])

t7 = Topic(7, "書く単位と承認の単位", "どこまで書いたら、人に見せるか", "決着",
  answer="<b>B ── 意思決定の境目（ブレスト→spec、spec→実装）でだけ承認する</b>",
  note="<b>書くのは節、承認するのは文書</b> ── このずれが Human On The Loop の境界そのものである。"
       "論点6・8 の決着が、ここへ 材料を落とした ── "
       "<b>規則は「まだ検めていない」を数えられるようになり、差分は意味として持てるようになった。</b>",
  figures=[GATE],
  tables=[Table("ずれの中身",
    ["単位", "なぜ"],
    {"書く": ["<b>節</b>", "AI が欄を埋める"],
     "承認する": ["<b>文書</b>", "人が1本として読む"]},
    lead="「どこまで書いたら人に見せるか」は、節が何個埋まったかでは決まらない。", plain=True),
   Table("この選択が置くもの",
    ["人が見る回数", "見るもの", "境目の間の誤り", "承認が意味するもの"],
    {"A": ["文書の本数だけ", "文書1本ぜんぶ", "文書が閉じるまで残る", "この文書は読んだ"],
     "B": ["<b>境目の数だけ（2回）</b>", "①全文　②<b>差分と、未検の一覧</b>", "<b>次の境目まで残る</b>", "<b>ここから先は、この決定に従う</b>"],
     "C": ["節の数だけ", "節1つ", "すぐ出る", "この節は読んだ"]})],
  kept=[
   Option("文書が閉じたら承認する",
          "文書1本ぶんの欄が埋まった時点で、人に見せる",
          "<b>意思決定の無い文書まで、承認の列に並ぶ。</b>埋まっただけの文書を毎回読むことになり、承認が儀式になる"),
   Option("意思決定の境目でだけ承認する",
          "ブレスト→spec と spec→実装 の2か所で承認する。その間は決められた構造・knowledge・規約が値を埋め、"
          "<b>検査が守る</b>",
          "<b>境目の間に入った誤りは、次の境目まで出てこない。</b>だから間を検査で守り、"
          "承認のときに<b>未検の一覧を必ず見せる</b>必要がある"),
   Option("節ごとに承認する",
          "AI が節を1つ埋めるたびに、人が見る",
          "<b>人が工程の番人になる。</b>承認の回数が節の数だけ要り、Human On The Loop が Human In The Loop に戻る"),
  ],
  dropped=[
   ("承認しない（全部を自動で通す）",
    "承認した決定に従う、という契約が消える。<b>ADR で決めた意味が無くなる</b>"),
   ("状態が VALIDATED になったら自動で承認する",
    "検査が見るのは規則であって、意思決定ではない。<b>構造が正しいことと、その決定でよいことは別である</b>"),
  ],
  found=["<b>2つの境目で、見るものが違う。</b>①ブレスト→spec は新しく立つので全文、②spec→実装 は前の版からの差分である ── <b>同じ「承認」でも、材料が違う</b>",
         "<b>差分を見せられるかどうかが、この案の成立条件である。</b>変わった箇所と変えた理由が本文の隣で開かないと、②の承認は結局『全文を読む』に戻る",
         "<b>Markdown ではこの体験を作れない。</b>同じ差分の意味を、Markdown では変更点の一覧に、HTML では本文の中の印に出す ── <b>論点8 の決まり（型は意味、形は射影）が、そのまま効く場面である</b>"],
  pick=("B", "承認は、意思決定が起きる境目（ブレスト→spec、spec→実装）でだけ行う。"
             "その間は決められた構造と規約が値を埋め、検査が守る。"
             "②で人が見るのは<b>差分と、未検の一覧</b>である"),
  grounds=[
   ("境目に絞ること", "承認は意思決定に対して行うものであり、値が埋まったことに対して行うものではない", "前提", "承認点をブレスト→spec と spec→実装 に置く、という置き方"),
   ("間を人が見ないこと", "その間は決められた構造と knowledge と規約に従って値が埋まるだけである", "前提", "同上"),
   ("間を検査で守ること", "宣言された規則は「まだ検めていない」を数えられる", "決まり", "論点6 の決まり（述語 ＋ 検めることの名前）"),
   ("差分を HTML で見せること", "同じ差分の意味を、Markdown では一覧に、HTML では本文の中の印に出せる", "決まり", "論点8 の決まり（型は意味、形は射影）"),
  ],
  costs=["境目の間に入った誤りは、次の境目まで出てこない",
         "承認の材料（差分と、未検の一覧）を作る仕組みが要る ── これが無いと、②は全文読みに戻る"],
  decision=[
   ("決定", "<b>承認は、意思決定が起きる境目でだけ行う</b> ── ブレスト→spec と spec→実装 の2か所"),
   ("理由", "承認は意思決定に対して行うものであり、値が埋まったことに対して行うものではない。"
            "その間は決められた構造と knowledge と規約に従って値が埋まるだけである"),
   ("間の守り", "人が見ない区間は<b>検査が守る</b>。承認のときに<b>未検の一覧を必ず見せる</b> ── "
              "これが無いと、境目の間に入った誤りが黙って通る"),
   ("見るもの", "①ブレスト→spec は<b>全文</b>（新しく立つので）。"
              "②spec→実装 は<b>差分</b>（変わった箇所・変えた理由・未検の一覧）"),
   ("成立条件", "②が成り立つのは、差分を本文の隣で開けるからである ── <b>論点9 の決着が前提になる</b>"),
   ("次", "<b>全論点が決着した。</b>次は、この決まりを spec の形へ落とす"),
  ],
  weaknesses=["<b>承認した契約が、どこまで下流を担保するかは書いていない。</b>「ADR で承認したら中身は担保される」と言うには、担保の範囲を宣言する必要がある",
              "<b>ブレスト→spec の境目で見るものは、まだ形になっていない。</b>差分ではないので、②とは別の材料が要る"])

t9 = Topic(9, "変更の記録", "変更前と、変えた理由を、どこに持つか", "決着",
  answer="<b>B ── 承認済みの値を、もう1つだけ持つ。承認したら畳む</b>",
  note="<b>変わった箇所は計算で出せるが、変えた理由は計算で出せない。</b>"
       "理由は人が書くものなので、保存する場所が要る ── どこに置くかが、この論点である。"
       "論点7 の案Bは、ここが決まらないと成立しない。"
       "<b>問いは「履歴を持つか」ではない</b> ── 承認の材料に要るのは、<b>最後に承認された値1つ</b>だけである。",
  figures=[WHERE],
  tables=[Table("この選択が置くもの",
    ["文書が持つ状態の数", "承認したとき", "理由の残り方", "遡れるか"],
    {"A": ["1（節の中に履歴を抱える）", "節が太ったまま残る", "節の中に積もる", "節を読めば遡れる"],
     "B": ["<b>2（承認済み ＋ いま）</b>", "<b>1つに畳んで上書きする</b>", "<b>畳んだ時点で消える</b>", "遡れない"],
     "C": ["版の数だけ", "版が1つ増える", "全部残る", "いつでも遡れる"]}),
   Table("版を積むと、何が手に入るか",
    ["それが要るか", "いま誰が持っているか"],
    {"承認の材料（前との差分）": ["<b>要る</b>", "<b>承認済みの値1つで足りる</b> ── 版は要らない"],
     "なぜその形にしたか": ["要る", "<b>ADR</b>（決定の記録）が持つ"],
     "いつ ・ 誰が変えたか": ["あれば助かる", "<b>git</b> が持つ（節の粒度は落ちる）"],
     "過去の版へ巻き戻す": ["<b>要件になっていない</b>", "要件になったら、そのとき案C へ移る"],
     "監査（第三者に経過を示す）": ["<b>要件になっていない</b>", "同上"]},
    lead="<b>版を積む理由は、承認の側からは出てこない。</b>出てくるのは巻き戻しと監査で、どちらもいまは要件になっていない。", plain=True)],
  kept=[
   Option("節が、変更前の値と理由を持つ",
          "節の中に「いまの値」「変更前の値」「変えた理由」を並べて持つ",
          "<b>版が進むほど節が太り、いまの正とかつての値が同じ境界に同居する</b> ── 集約の内側に履歴が溜まる"),
   Option("承認済みの値を、もう1つだけ持つ",
          "文書は「承認済みの値」と「いまの値」の2状態だけを持ち、"
          "<b>承認したら1つに畳んで上書きする</b>。変わった節の印と理由は、畳むまでのあいだ在る",
          "<b>畳んだ時点で「なぜ変えたか」は消える。</b>残したい理由は、決定として ADR に書くことになる",
          before="版と版の間が、変わった箇所と理由を持つ",
          why="「版管理をしたい理由は何か」と問われて、<b>承認の側からは出てこない</b>と分かった ── "
              "承認に要るのは最後に承認された値1つで、履歴ではない。"
              "版が要るのは巻き戻しと監査のときで、どちらもいまは要件になっていない"),
   Option("版を積み続ける",
          "変更のたびに版を1つ残し、いつでも遡れるようにする",
          "<b>溜まり続ける。</b>捨てる判断を別に決めることになり、承認には使わないものを運び続ける"),
  ],
  dropped=[
   ("差分は描くときに計算し、保存しない",
    "<b>「なぜ変えたか」の置き場所が無い。</b>変わった箇所は計算で出せるが、理由は計算では出てこない"),
   ("変更の記録を、版管理の履歴だけに任せる",
    "行の差分は出るが、<b>どの節が変わったかは出ない</b>。印で結ぶ相手が無く、理由も節に結び付かない"),
  ],
  found=["<b>「差分を見る」と「変更を記録する」は別の操作である。</b>前者は状態を変えないので問い合わせ、後者と承認は状態を変えるので命令になる ── <b>検査を命令と呼ばないのと同じ区別である</b>",
         "<b>保存の形と、見せ方は別である。</b>版と版の間に置いても、本文のすぐ隣に理由を開くことはできる ── それは射影の仕事だからである",
         "<b>この盤面自身が、案Bの実物になっている。</b>案が変わった箇所に印が付き、押すと変更前と理由が開く ── 印と理由は、いまの盤面の外に置かれている"],
  pick=("B", "文書は「承認済みの値」と「いまの値」の2状態だけを持ち、変わった節の印と理由はそのあいだに在る。"
             "<b>承認したら1つに畳んで上書きし、履歴は積まない。</b>本文の隣に開いて見せるのは、射影が行う"),
  grounds=[
   ("履歴を積まないこと", "承認に要るのは最後に承認された値1つで、版を積む理由は承認の側からは出てこない", "前提", "承認点をブレスト→spec と spec→実装 に置く、という論点7 の置き方"),
   ("節に持たせないこと", "集約の内側に履歴を持つと、いまの正とかつての値が同じ境界に同居する", "決まり", "論点6 の決まり（集約は一貫性の境界であり、またぐものは外に置く）"),
   ("理由を保存すること", "変わった箇所は計算で出せるが、変えた理由は計算で出せない", "前提", "理由は人が書くものである、という性質"),
   ("見せ方を分けること", "本文の隣に理由を開くのは形の話であり、保存の形とは別である", "決まり", "論点8 の決まり（型は意味、形は射影）"),
   ("命令と問い合わせを分けること", "差分の取得は状態を変えず、記録と承認は変える", "決まり", "論点6 の決まり（検査はコマンドと呼ばない）"),
  ],
  costs=["<b>畳んだ時点で、変えた理由は消える。</b>残したい理由は、決定として ADR に書く",
         "承認済みの値をもう1つ持つので、文書が抱える状態は2つになる",
         "理由を書く手間が、変更のたびに要る。書かせない限り、空のまま畳まれる"],
  decision=[
   ("決定", "<b>文書は「承認済みの値」と「いまの値」の2状態だけを持つ。</b>"
            "変わった節の印と変えた理由はそのあいだに在り、<b>承認したら1つに畳んで上書きする</b>"),
   ("理由", "<b>版を積む理由は、承認の側からは出てこない。</b>承認に要るのは最後に承認された値1つである ── "
            "版が要るのは巻き戻しと監査のときで、どちらもいまは要件になっていない"),
   ("理由の残り方", "<b>畳んだ時点で「なぜ変えたか」は消える。</b>残したい理由は、決定として ADR が持つ"),
   ("見せ方", "変わった箇所のすぐ隣で理由を開くのは<b>射影の仕事</b>である ── 保存の形とは別に決まる（論点8 の決まり）"),
   ("命令と問い合わせ", "<b>差分の取得は問い合わせ、記録と承認は命令</b>。状態を変えるのは後者だけである"),
   ("次", "論点7 へ戻る ── これで承認の材料がそろった"),
  ],
  weaknesses=["<b>承認を1度も通っていない文書には、承認済みの値が無い。</b>そのとき差分は「全部が新しい」になるが、それでよいかは決めていない",
              "<b>理由が空のまま畳まれたときに何が起きるかは決めていない</b> ── 検査で落とすのか、承認の材料として「理由が無い変更 N 件」と見せるのか",
              "<b>巻き戻しか監査が要件になったら、案C へ移ることになる。</b>そのとき、畳んで捨てたものはもう戻らない"])

overview_extra = ("JSON Schema に足りないもの（6つ）",
  _table(["足りないもの", "JSON Schema が持つもの", "なぜ足りないか"],
   [["同一性", "型の判定", "「この節が<b>何であるか</b>」は言えるが「<b>どの個体か</b>」を持たない"],
    ["書き方の指示", "<code>description</code>", "<code>x-</code> に載せるしかない。そして構造化出力は <code>x-</code> を拒む（実測）"],
    ["操作", "検証だけ", "引き方・書き方のコマンドが無い"],
    ["射影", "──", "出力先・出力形の概念が無い"],
    ["<b>実体どうしの参照</b>", "<code>$ref</code>", "<b><code>$ref</code> は型の再利用であって、インスタンス間の参照ではない</b>"],
    ["版と移行", "<code>$id</code>", "版は言えるが、実体を移す意味論が無い"]])
  + f'<figure>{REC[1][0]}<figcaption>{REC[1][1]}</figcaption></figure>')

t8 = Topic(8, "表現の型と射影", "表現の型と、出力の形をどう分けるか", "決着",
  answer="<b>B ── 型は意味、形は射影。既定を持ち、差分だけ書く</b>",
  note="<b>schema の構造が、そのまま骨格にも描画にもなる</b> ── ここまでは実際にそう動いている。"
       "開いているのは<b>その先</b>で、同じ節を人には表で、AI には節の列で出したいときに、"
       "型を増やすのか、出し方を分けるのかである。"
       "<b>JSON Schema に足りないもの6つのうち「射影」が、ここに当たる。</b>",
  figures=[PROJ, DIFF, PROJTBL, GROUPPROJ],
  tables=[Table("同じ「表」の節を、どこへどう出すか",
    ["Markdown（人 ・ Skill ・ Agent）", "HTML（人）", "射影なし（AI が直に読む）"],
    {"A　型＝形": ["罫線の表", "<b>罫線の表（変えたければ型を増やす）</b>", "<b>構造にも形が混ざる</b>（型が形を決めているため）"],
     "B　型 × 射影": ["罫線の表", "札の並び、あるいは図", "意味の型のまま読める"],
     "C　文書ごとの雛形": ["その文書の雛形しだい", "その文書の雛形しだい", "雛形は効かない（構造は文書のまま）"]},
    lead="<b>AI は射影を必要としない</b> ── <code>document.json</code> の構造を直に読む。"
         "射影が要るのは、<b>人が読むときと、Skill ・ Agent として Markdown で読ませるとき</b>である。"
         "読み手ではなく<b>出力先</b>が形を決め、読み手はその出力先に従属する。", plain=True),
   Table("この選択が置くもの",
    ["出力先を1つ足すとき", "型を1つ足すとき", "同じ意味が2つの形で要るとき", "描けない文書が出たとき"],
    {"A": ["全ての型に、その出力先の出し方を書き足す", "1つ足す", "<b>型を2つに割る（意味は1つなのに）</b>", "<b>専用の雛形へ落ちる（汎用から外れる）</b>"],
     "B": ["射影を1つ足す（型は触らない）", "全ての射影に、その型の出し方が要る", "射影を分ける（型は1つのまま）", "射影の欄が欠けている、と言える"],
     "C": ["文書ごとに書く", "1つ足す", "文書ごとに書く", "起きない（最初から専用）"]})],
  kept=[
   Option("型が、そのまま出力の形になる",
          "表の型は表として出る。出力先が違っても同じ形で出る",
          "<b>出したい形が増えるたびに、意味の型が増える</b>。汎用に載らない文書は専用の雛形へ落ちる"),
   Option("型は意味だけを言い、形は射影が決める",
          "型は「並べて比べる情報である」としか言わない。出力先ごとに、その型をどう出すかを射影が持つ",
          "<b>射影の対応表が要る。</b>型を足したら、全ての射影に「その型をどう出すか」の欄が増える"),
   Option("文書ごとに、出力の雛形を持つ",
          "その文書だけの雛形を書き、そこへ値を流し込む",
          "<b>同じ型が、文書ごとに違う形で出る。</b>読み手は形から意味を読めなくなり、雛形は文書の数だけ増える"),
  ],
  dropped=[
   ("形を持たず、常に生の構造を出す",
    "人は形が無いと認知できない ── 論点2 の決まり（人＝ビジュアライズされていないと認知できない）と正面から衝突する"),
   ("人向けと AI 向けで、schema そのものを2本持つ",
    "同じ意味の節が2か所に在ることになり、どちらが正かを言う仕組みがまた要る。論点2 は<b>1つの構造に2つの単位</b>で答えていた"),
  ],
  found=["<b>Skill ・ Agent へ渡す Markdown は、人向けと同じでよい。</b>人の認知負荷が低い文書は、構造が整理されているという意味で AI にとっても読みやすい ── <b>Markdown の射影は1つで足りる</b>",
         "<b>型＝形は、すでに1度破れている。</b>Handoff は <code>x-render-target</code> を持たず、汎用の描画では出したい形にならなかったので、専用テンプレートでしか描けない ── 案Aの代償が、実物として先に出ていた",
         "<b>骨格と描画は、同じ型から出るが、同じものではない。</b>scaffold が作る骨格は型そのままでよく、分かれるのは出力の側だけである",
         "<b>読み手ではなく、出力先が形を決める。</b>AI は射影を通さず構造を直に読むが、Skill ・ Agent として読ませるときは Markdown へ射影する ── <b>同じ AI でも、出力先が違えば形が違う</b>",
         "<b>射影を足しても abstract は太らない。</b>型の語彙は abstract が持つので、射影は「既に在る型の出し方」を書くだけで済む ── 論点6 の名簿とは、太り方が違う"],
  pick=("B", "型は意味だけを言い、出力の形は射影が決める。射影は出力先ごとに、型の出し方を持つ"),
  extras=[
   ("語をそろえる", """<p class="lead"><b>「部品」は、型とは別のものではない。組から見た型の呼び名である。</b>
この論点で使う語を、次の意味に固定する。</p>
<table><tr><th>語</th><th>何を指すか</th><th>例</th></tr>
<tr><td><b>型</b></td><td>節が<b>何であるか</b>の分類。schema が持つ語彙。1つの節は1つの型を持つ</td><td>表 ／ 文 ／ 図＋読み方 ／ 並び</td></tr>
<tr><td><b>節</b></td><td>型を1つ持った実体。文書は節の集まりである</td><td>この論点の「案の比較表」という節</td></tr>
<tr><td><b>組</b></td><td>複数の型が集まって<b>1つの意味になったもの</b>。組そのものが型である</td><td>「案の比較 ＋ 結論」</td></tr>
<tr><td><b>部品</b></td><td><b>組から見たときの、中の型の呼び名。</b>新しい種類のものではない</td><td>組「案の比較 ＋ 結論」の部品は、表と文</td></tr>
<tr><td><b>射影</b></td><td>型を、その出力先でどう出すかの対応</td><td>表 → Markdown なら罫線の表</td></tr>
<tr><td><b>既定</b></td><td>abstract が全ての型について持つ、標準の出し方</td><td>表 → どの出力先でも罫線の表</td></tr>
<tr><td><b>差分</b></td><td><b>既定と違うマスを1つ上書きすること。</b>組み合わせではない</td><td>表 × HTML だけ、札の並びにする</td></tr>
<tr><td><b>並べ方</b></td><td>組が持つ、部品を置く順序。<b>中の出し方は書かない</b></td><td>結論を先に、表を後に</td></tr></table>
<p class="lead"><b>差分は、部品を当てて組み立てることではない。</b>射影の表の1マスに
「この型を、この出力先では、こう出す」と書くだけである ── 組み合わせの話は、組の並べ方の側にある。</p>
<p class="lead"><b>通してみる。</b>この論点のタブ自体を例にすると、こうなる。</p>
<table><tr><th>段</th><th>実際にあるもの</th></tr>
<tr><td>節</td><td>「案の比較表」（型＝表）　／　「結論の文」（型＝文）</td></tr>
<tr><td>組</td><td>「案の比較 ＋ 結論」── 表だけでは何を選んだか言っていないので、<b>型として登録する</b></td></tr>
<tr><td>型の射影</td><td>表 → Markdown は罫線の表（既定）／ HTML は札の並び（<b>差分</b>）</td></tr>
<tr><td>組の並べ方</td><td>Markdown も HTML も「結論を先、表を後」。<b>中の表の出し方は書かない</b></td></tr>
<tr><td>射影なし</td><td>AI は <code>document.json</code> をそのまま読む。型も組もそのままの構造で見える</td></tr></table>"""),
   ("部品で持つか、組で持つか", """<p class="lead"><b>表と図のように、複数の部品が繰り返し組で現れることがある。</b>
その組を1つの意味として登録し、組に射影を持たせるか ── 判定は<b>片方を落としたときに何が起きるか</b>で付く。</p>
<table><tr><th>順</th><th>問い</th><th>Yes なら</th></tr>
<tr><td>1</td><td><b>片方を落としたら、言っていることが変わるか</b></td><td><b>型にする</b>（組そのものが主張を持つ）</td></tr>
<tr><td>2</td><td><b>片方を落としたら、その主張を疑う手段が失われるか</b></td><td><b>型にする</b>（落とした案の一覧が、これに当たる）</td></tr>
<tr><td>3</td><td>1も2も No で、よく一緒に現れるか</td><td><b>型にしない。</b>射影の側に「並べ方」として名前を付ける</td></tr>
<tr><td>4</td><td>どれも No</td><td>何もしない。部品のまま置く</td></tr></table>
<p class="lead"><b>問2 は、当ててみて足した。</b>「反証を通過した案」と「落とした案」は、
落としても<b>結論は変わらない</b>が、選択肢を潰したことに誰も気づけなくなる ──
問1 だけだと、ここで境目が動いた。<b>型にした組は、射影として並べ方だけを持つ。</b></p>
<p class="lead"><b>頻度は、型を作る理由にならない。</b>よく一緒に現れるのは、体裁が繰り返されているだけかもしれない。
頻度で型を作ると、<b>意味は1つなのに名前が増える</b> ── 案Aと同じ壊れ方である。
ただし頻度は「いつ切り出すか」の合図にはなる。切り出す先が、型ではなく射影だというだけである。</p>
<table><tr><th>現れ方</th><th>片方を落とすと</th><th>どちらか</th></tr>
<tr><td>表 ＋ その表を線で示した図</td><td>読みにくくなるが、主張は通る</td><td><b>体裁</b> → 射影の並べ方</td></tr>
<tr><td>図 ＋ その図の読み方の一文</td><td><b>図が何を言っているか決まらない</b></td><td><b>意味の型</b>（図と読み方で1つ）</td></tr>
<tr><td>案の比較表 ＋ 選んだ結論</td><td><b>何を選んだかを言っていない</b></td><td><b>意味の型</b>（この盤面の結論カードがこれ）</td></tr></table>
<p class="lead"><b>組の射影は、部品の射影を呼ぶ形にする。</b>組の中の表をどう出すかまで組が書くと、
組の数 × 出力先の数だけ欄が増える ── 組が書くのは<b>並べ方だけ</b>である。</p>
<p class="lead"><b>図に描いた主張を、図の中へ閉じ込めない。</b>SVG の中の文字は構造を持たないので、
射影を通さず読む AI からは見えない。<b>主張は意味として持ち、図はその射影の1つ</b>にする
── この盤面が、図の読み方を必ず一文で持っているのがそれである。</p>"""),],

  grounds=[
   ("型と形を分けること", "同じ節を、人には形のある表で、AI には節の列で出す必要がある", "決まり", "論点2 の決まり（単位は読み手に従属する）"),
   ("射影という段を置くこと", "Handoff は汎用の描画に載らず、専用テンプレートでしか描けない ── 型＝形では足りなかった", "実測", "Waffle の HandoffSchema（<code>x-render-target</code> を持たず、<code>render-handoff-template</code> のみ）"),
   ("欄の欠けを落とすこと", "無いことが見えなければ、足りないことに気づけない", "決まり", "CodingSkills 論点11 の決まり（不在が観測できるようにする）"),
   ("型を増やさないこと", "意味が1つなら型も1つである。形の違いは意味の違いではない", "前提", "型は意味の分類である、という置き方からの帰結"),
  ],
  costs=["射影の対応表が要る。型を1つ足すと、全ての射影にその型の出し方が要る",
         "「この型は、この出力先では出さない」も宣言しないと、欠けと区別が付かない"],
  decision=[
   ("決定", "<b>型は意味だけを言い、出力の形は射影が決める。</b>射影は出力先ごとに、型の出し方を持つ"),
   ("理由", "同じ節を、人には形のある表で、Skill には Markdown で、AI には構造のまま渡す必要がある。"
            "型と形を1対1にすると、<b>出したい形が増えるたびに意味の型が増える</b> ── "
            "Handoff が汎用の描画に載らず専用テンプレートへ落ちたのが、その実物である"),
   ("既定と差分", "<b>abstract が全ての型に既定の出し方を持ち、人が書くのは既定と違う1マスだけ。</b>"
                "既定も差分も無いマスは描画時に落とし、欄が無いことを見せる"),
   ("組の扱い", "<b>組が持つ射影は並べ方だけ</b>で、中の型の出し方は部品の射影に任せる ── "
              "組が中身まで書くと、組 × 出力先 × 部品ぶん欄が要り、同じ出し方が組の数だけ写される"),
   ("組にするかの判定", "①落とすと言っていることが変わるか ②<b>落とすとその主張を疑う手段が失われるか</b> ── "
                    "どちらかが Yes なら型にする。よく一緒に現れるだけなら、射影の側に並べ方として名前を付ける。"
                    "<b>頻度は型を作る理由にならない</b>"),
   ("Markdown の射影", "<b>人向けと、Skill ・ Agent 向けで同じものを使う。</b>"
                    "人の認知負荷が低い文書は、構造が整理されているという意味で AI にとっても読みやすい"),
   ("次", "論点7（書く単位と承認の単位）へ進む。<b>これが最後の論点である</b>"),
  ],
  weaknesses=["<b>既定の射影が「無難だが読みにくい形」に落ち着きやすい。</b>差分を書かせない力が働くので、既定の質がそのまま出力の質になる",
              "<b>問2（疑う手段が失われるか）は、この盤面の節にしか当てていない。</b>別の種類の文書では、また境目が動きうる",
              "<b>組の並べ方が、出力先をまたいで同じでよいかは確かめていない</b> ── Markdown では結論が先でよくても、HTML では図が先かもしれない"])

t10 = Topic(10, "書き方の指示", "節ごとの書き方の指示を、schema のどこに置くか", "決着",
  answer="<b>C ── x- に構造で保存し、渡すときに description へ合成する</b>",
  note="<b>JSON Schema は「どう書くか」を持たない。</b>載せられるのは <code>description</code> か <code>x-</code> だけで、"
       "<b>構造化出力は <code>x-</code> を拒む（実測）</b> ── この2つの板挟みが、この論点である。",
  figures=[PROMPT, COMPOSE],
  tables=[Table("この選択が置くもの",
    ["構造化出力へ渡したとき", "説明と指示を別々に直せるか", "schema を人が読んだとき", "指示を長く書けるか"],
    {"A": ["<b>指示が落ちる</b>", "直せる", "分かれていて読みやすい", "書ける"],
     "B": ["そのまま載る", "<b>直せない（同じ文字列）</b>", "説明と指示が混ざる", "<b>常時トークンに載る</b>"],
     "C": ["<b>合成して載る</b>", "直せる", "分かれていて読みやすい", "書ける（渡すときだけ載る）"],
     "D": ["合成して載せられる", "直せる", "<b>指示が schema に無い</b>", "書ける"]})],
  kept=[
   Option("<code>x-prompt-*</code> のまま持つ（いまの形）",
          "書き方の指示を <code>x-</code> の欄に置く。構造化出力へはそのまま渡す",
          "<b>構造化出力では指示が落ちる。</b>落ちたことは静かなので、"
          "「指示に従わない」という形でしか気づけない"),
   Option("<code>description</code> に書く",
          "標準の欄に、説明と書き方の指示を1つの文字列として書く",
          "<b>説明と指示が混ざり、片方だけを直せない。</b>読む側の説明を直すと、書く側の指示も動く"),
   Option("<code>x-</code> に保存し、渡すときに <code>description</code> へ合成する",
          "保存は分けたまま、構造化出力へ渡す瞬間だけ「何であるか ＋ どう書くか」を1つにする",
          "<b>合成する仕組みが要る。</b>人が読む schema と、AI が受け取る schema が別物になる"),
   Option("指示を schema の外に置き、印で結ぶ",
          "型の印から指示を引く表を、別のファイルに持つ",
          "<b>2つがずれる。</b>ずれても schema 側は正しいので、検査を別に立てないと気づけない"),
  ],
  dropped=[
   ("書き方の指示を持たない",
    "指示が無ければ、同じ欄でも書くたびに形が変わる。<b>Waffle が解こうとしている問題そのものが残る</b>"),
  ],
  found=["<b>これは論点8 の決まりの、そのままの適用である</b> ── 保存の形と渡す形は別で、渡す形は射影が決める。"
         "<b>合成は、構造化出力という出力先への射影である</b>",
         "<b>案Bの代償は、読み手が2種類いることから来る。</b>人向けの説明と AI 向けの指示は、"
         "同じ欄に置くと片方を直すたびにもう片方が動く ── 論点2 の決まり（単位は読み手に従属する）と同じ形の問題である"],
  pick=("C", "書き方の指示は <code>x-</code> に分けて保存し、"
             "<b>構造化出力へ渡すときだけ <code>description</code> へ合成する</b>"),
  grounds=[
   ("合成にすること", "構造化出力は <code>x-</code> を拒む", "実測", "構造化出力に schema を渡したときの挙動（この盤面の前提として既に測ってある）"),
   ("保存を分けること", "説明と指示を同じ文字列にすると、片方だけを直せない", "前提", "案Bの構造からの帰結"),
   ("射影として扱うこと", "保存の形と、出力先ごとの出し方は別である", "決まり", "論点8 の決まり（型は意味、形は射影）"),
  ],
  extras=[("JSON Schema が持つ語の一覧（57）", JSLIST),
   ("合成の形 ── 散文にしない", """<p class="lead"><b>合成した <code>description</code> が散文になると、指示は効かなくなる。</b>
だから合成は「つなげる」ではなく、<b>決まった形に入れる</b>ものにする。</p>
<table><tr><th></th><th>合成のしかた</th><th>渡ったときに何が起きるか</th></tr>
<tr><td><b>×</b></td><td>説明と指示を、1つの文につないで並べる</td><td><b>どこまでが説明で、どこからが指示か分からない</b>。長いほど効かなくなる</td></tr>
<tr><td><b>○</b></td><td>ラベルの付いた短い行を、決まった順で並べる</td><td>行の頭が役割を言うので、<b>読み飛ばしても形が保つ</b></td></tr></table>
<p class="lead"><b>もう1つ、読むときと書くときで、渡すものを変える。</b>
射影は出力先ごとに持つので、<b>「書くための構造化出力」と「読むための構造化出力」を別の出力先として扱う</b> ──
<code>x-prompt-write</code> は書く側にだけ、<code>x-prompt-query</code> は読む側にだけ載せる。
両方を1つの <code>description</code> に載せると、<b>いま何をしているのかを AI が判断することになる</b>。</p>
<table><tr><th>出力先</th><th>description に載せるもの</th><th>載せないもの</th></tr>
<tr><td>人が読む schema</td><td>この欄が何であるか</td><td>指示（別の欄にある）</td></tr>
<tr><td><b>書くための構造化出力</b></td><td>何であるか　＋　<b>どう書くか</b></td><td>どう読むか</td></tr>
<tr><td><b>読むための構造化出力</b></td><td>何であるか　＋　<b>どう読むか</b></td><td>どう書くか</td></tr></table>
<h3>合成した中身を、どの形で持つか</h3>
<table><tr><th>形</th><th>渡る中身</th><th>分解できるか</th><th>かさ</th></tr>
<tr><td><b>×</b>　散文</td><td>文をつないだもの</td><td><b>できない</b></td><td>小</td></tr>
<tr><td><b>○</b>　ラベル行</td><td><code>書き方：振る舞いを述べる一文にする</code></td><td>行で切れる</td><td>小</td></tr>
<tr><td><b>○</b>　JSON 文字列</td><td><code>{"書き方": "振る舞いを述べる一文にする"}</code></td><td><b>機械で確実に切れる</b></td><td>中</td></tr></table>
<p class="lead"><b>合成した文字列は保存されない。</b>渡すときに作られ、渡し終われば消える ──
だから<b>どの形を選んでも、正本の schema は変わらない</b>。
人が読むのは保存の形（説明と指示が別の欄に分かれたもの）のままである。</p>
<p class="lead"><b>だから3つの差は2つしかない</b> ── 分解できるかどうかと、かさである。
<b>読みにくさが問題になるのは案B（<code>description</code> に直接書く）を採ったときで、案Cには当たらない。</b></p>
<h3>どの階層に置けるか</h3>
<p class="lead"><b><code>description</code> は「その schema が記述するインスタンスの目的の説明」である</b>（原典の言い方）──
だから<b>欄だけでなく、塊（オブジェクト）の schema にも付く</b>。配列の要素にも、<code>$defs</code> の中にも付く。</p>
<table><tr><th>階層</th><th>置くもの</th><th>いつ渡すか</th></tr>
<tr><td><b>節（意味の塊）</b></td><td>この塊が何であるか　＋　<b>どう読むか</b>（<code>x-prompt-query</code>）</td>
<td><b>塊で引くとき</b> ── AI が引く単位は節である（論点2）</td></tr>
<tr><td>塊の中の欄</td><td>この欄が何であるか　＋　<b>どう書くか</b>（<code>x-prompt-write</code>）</td><td>その欄を埋めるとき</td></tr>
<tr><td>並びの要素</td><td>1件が何であるか　＋　書き方</td><td>要素を足すとき</td></tr>
<tr><td><code>$defs</code> の型</td><td>その型が何であるか</td><td>その型を使うすべての場所</td></tr>
<tr><td><b><code>$ref</code> の隣</b></td><td><b>その場所でだけの説明</b></td><td>同じ型を、違う意味で使っているとき</td></tr></table>
<p class="lead"><b><code>$ref</code> の隣に書けるのは、原典が「他のキーワードを <code>$ref</code> と並べてよい」と言っているからである。</b>
同じ型を2か所で使っていて、意味が場所によって違うとき ── 型の説明はそのままに、<b>その場所の説明だけを足せる。</b></p>
<p class="lead"><b>塊で引くときは、塊の <code>x-prompt-query</code> を渡す。</b>
欄ごとの指示を全部並べるのではなく、<b>引いた単位の階層に在る指示だけを載せる</b> ──
これも射影の選択であって、保存の形は変わらない。</p>
<h3>指示そのものを、構造で持つ</h3>
<p class="lead"><b>指示の中に意味の境界があるなら、それは文字列の中ではなく欄で表す。</b>
いまは「振る舞いを述べる一文。実装名を含めない。例：…」のように句読点が境界を兼ねているが、
<b>句点の打ち方が変われば、境界も動いてしまう。</b></p>
<table><tr><th>欄</th><th>持つもの</th><th>何が言えるようになるか</th></tr>
<tr><td><code>x-prompt-write.how</code></td><td>どう書くか（一文）</td><td>指示が在るかを、規則で数えられる</td></tr>
<tr><td><code>x-prompt-write.forbidden</code></td><td>してはならないこと（並び）</td><td><b>禁じ手ごとに、違反を照らせる</b></td></tr>
<tr><td><code>x-prompt-write.example</code></td><td>適合する例</td><td><b>「例が無い欄」を数えられる</b></td></tr>
<tr><td><code>x-prompt-query.read</code></td><td>どう読むか</td><td>読む射影に載せる欄が決まる</td></tr>
<tr><td><code>x-prompt-query.related</code></td><td>併せて見る欄</td><td>読み違えやすい対を、印で結べる</td></tr>
</table>
<p class="lead"><b>こうすると、合成は「並べ替えるだけ」になる。</b>
ラベル行にするなら欄名を表示名へ置き換え、JSON 文字列にするなら<b>宣言された欄名をそのまま鍵にする</b> ──
どちらも<b>合成のときに文を切り分けない</b>。切り分けが要るのは、指示を1つの文字列に押し込んだときだけである。</p>
<p class="lead"><b>これは論点6 の決まりを、指示の側へ広げることでもある。</b>
「例が無い」「禁じ手が無い」は<b>述語で書ける規則</b>になり、いま数えられないものが数えられるようになる。</p>
<p class="lead"><b>これは壊す変更だが、いまは受け入れる。</b>
<code>x-prompt-write</code> を文字列からオブジェクトへ変えると、既存の schema はそのままでは通らない ──
<b>いまが段1・段2 の再定義そのものなので、ここは一度で変える。</b>
新しい欄を足して古い文字列を残す、という迂回はしない ── 迂回すると、
<b>再定義が終わった時点で、使わない欄が最初から残っていることになる。</b></p>
<p class="lead"><b>論点11 の決まりが効き始めるのは、再定義が終わってからである。</b>
定常状態に入ったあとは、必須の欄を後から足すことを禁じ、壊すなら型を分ける ──
<b>いまはその定常状態の手前にいる。</b></p>
<p class="lead"><b>残る問いは1つ ── どの形がいちばん効くか。</b>
同じ節を、<b>合成なし ／ 散文 ／ ラベル行 ／ JSON 文字列</b>の4通りで埋めさせ、
<b>規則に違反した件数を比べる</b>。Waffle は自分自身が対象なので、この測定はいつでもできる。</p>""")],
  costs=["合成する仕組みが要る ── 渡す経路すべてに通す必要がある",
         "人が読む schema と、AI が受け取る schema が別物になる。突き合わせるときは合成後を見る"],
  decision=[
   ("決定", "<b>書き方と読み方の指示は <code>x-</code> に保存し、渡すときだけ <code>description</code> へ合成する。</b>"
            "保存の形と渡す形は別であり、合成は構造化出力という出力先への射影である"),
   ("指示の形", "<b>指示そのものを構造で持つ</b> ── <code>x-prompt-write</code> は "
              "<code>how</code> ・ <code>forbidden</code> ・ <code>example</code>、"
              "<code>x-prompt-query</code> は <code>read</code> ・ <code>related</code>。"
              "句読点を意味の境界にしない"),
   ("合成の鍵", "<b>合成のために語彙を新しく作らない。</b>JSON 文字列にするなら"
              "<b>宣言された欄名をそのまま鍵にし</b>、ラベル行にするなら欄名を表示名へ置き換える"),
   ("置く階層", "<b>塊にも欄にも置ける。</b>引いた単位の階層に在る指示だけを渡す ── "
              "塊で引いたときは塊の <code>x-prompt-query</code>、欄を埋めるときはその欄の <code>x-prompt-write</code>"),
   ("いつ変えるか", "<b>再定義の一部として、一度で変える。</b>"
                "古い文字列の欄を残す迂回はしない ── 論点11 の決まりが効くのは、再定義が終わってからである"),
   ("測ること", "<b>どの形がいちばん効くかは、測って決める</b> ── 合成なし ／ 散文 ／ ラベル行 ／ JSON 文字列 の4通りで、"
              "規則に違反した件数を比べる"),
   ("次", "論点13（引く仕様）へ進む ── <b>塊で引くときに何を渡すかが、そこで決まる</b>"),
  ],
  weaknesses=["<b>合成した description が長くなりすぎたときの扱いは決めていない。</b>渡すたびにトークンへ載る",
              "<b>合成した指示が実際に効くかは、まだ測っていない。</b>形を決めても、読まれる保証にはならない ── 合成なし／散文／ラベル行／JSON 文字列の4通りで、違反の件数を比べる",
              "<b>語彙を <code>$vocabulary</code> で宣言しても、その意味は実装が持つ。</b>宣言だけでは、他の処理系は述語を理解しない",
              "<b>指示の欄をどこまで分けるかは決めていない。</b>how ・ forbidden ・ example の3つで足りるのか、判断の根拠や参照先も要るのかは、実際に書いてみないと分からない"])

t11 = Topic(11, "版と移行", "型や欄が増えたとき、既存の document をどう運ぶか", "決着",
  answer="<b>D ── schema の版は Waffle の版に従う。破壊は認め、静かに壊さない</b>",
  note="<b>論点9 で決めたのは文書の承認状態であって、schema 自身の版ではない。</b>"
       "<b>これはデータベースの移行と同じ形の問題である</b> ── 足すのは楽で、"
       "<b>名前を変えることと消すことが厄介</b>。そして直すのは状態を変える操作なので、人が通すことになる。"
       "<b>この決まりが効き始めるのは、いまの再定義が終わってからである。</b>"
       "<b>ずれが起きるのは、Waffle を更新した一瞬だけにする</b> ── "
       "更新しない限り、schema と document は必ず同じ版である。",
  figures=[MIGRATE, MIGTIME],
  tables=[Table("変更の種類ごとに、何が要るか",
    ["既存の document", "宣言で書けるか", "人の承認"],
    {"任意の欄を足す": ["そのまま通る", "書くことが無い", "要らない"],
     "必須の欄を足す": ["<b>落ちる</b>", "既定値を宣言すれば書ける", "要る"],
     "欄の名前を変える": ["<b>読めなくなる</b>", "<b>旧名→新名で書ける</b>", "要る"],
     "欄を消す": ["残ったまま宙に浮く", "<b>消すと宣言すれば書ける</b>", "要る"],
     "欄の意味を変える（値の作り直し）": ["<b>静かに間違う</b>", "<b>書けない</b>", "── そもそも禁じる"]},
    lead="<b>厄介なのは名前の変更と削除だが、どちらも宣言で書ける。</b>"
         "書けないのは、値そのものを作り直す変更である。", plain=True),
   Table("この選択が置くもの",
    ["版を上げたとき", "AI が直せるか", "いつ人に見せるか", "宣言に無い壊し方"],
    {"A": ["移行の手続きを書く", "手続きしだい", "移行のとき", "手続きで何でも書ける"],
     "B": ["何もしない", "── 直さない", "見せない", "古いまま残る"],
     "C": ["任意の欄が増えるだけ", "直す必要が無い", "見せない", "<b>型を分ける</b>"],
     "D": ["<b>宣言を1行足す</b>", "<b>宣言どおりに当てる（推測しない）</b>", "<b>更新のとき、まとめて</b>", "<b>禁じる（案C へ）</b>"]})],
  kept=[
   Option("版を上げたら、全ての document を一括で移す",
          "移行の手続きを書き、古い版の document を新しい版へ移す",
          "<b>手続きには何でも書ける。</b>だから何が起きるかは、書いた人にしか分からない"),
   Option("document は古い版のままでよく、読むときに補う",
          "足りない欄は、読むときに既定値で埋める",
          "<b>古い版がいつまでも残る。</b>読むたびに「どの版か」を判定することになる"),
   Option("必須の欄を後から足すことを禁じ、壊すなら型を分ける",
          "足せるのは任意の欄だけ。壊す変更をしたくなったら、新しい型を別の名前で立てる",
          "<b>名前の変更と削除ができない。</b>実際には起きるので、これだけでは足りない"),
   Option("移行を宣言で持ち、<b>Waffle の更新のときにまとめて当てる</b>",
          "schema の版は Waffle の版に従う。更新しない限りずれは起きず、"
          "更新のときに<b>合わない document を列挙し、宣言された移行（旧名→新名 ・ 削除 ・ 既定値）を当てる</b>",
          "<b>宣言で書ける変更しか運べない</b>（値の作り直しは禁じる）。"
          "また<b>更新を何段も飛ばすと、段を順に当てることになる</b>"),
  ],
  dropped=[
   ("版を持たない",
    "schema と document のずれを、誰も検知できなくなる。<b>いま在る schema 版ドリフトの検査が、指す先を失う</b>"),
   ("ずれを見つけたら、AI がその場で直す",
    "<b>直し方が宣言に無ければ、AI は推測することになる</b>。推測は静かに間違い、間違ったことも記録に残らない"),
  ],
  found=["<b>版を Waffle の版に従わせると、後方互換を実行時に持ち回らなくてよい。</b>"
         "更新しない限り schema と document は同じ版なので、<b>ずれるのは更新した一瞬だけ</b>である ── "
         "担保するのは「1段ぶんの更新」だけになる",
         "<b>これは論点6 で引いた線と、同じ線である</b> ── 宣言で書ける移行（名前の変更・削除・既定値）と、"
         "宣言に収まらない移行（値の作り直し）。<b>収まらない側は、規則と同じく禁じる側に置く</b>",
         "<b>厄介なのは名前の変更と削除だが、どちらも宣言で書ける。</b>"
         "書けないのは値を作り直す変更で、それは型を分けるべき変更である",
         "<b>直すのは状態を変える操作なので、人が通す</b>（論点9 の区別）── "
         "更新のときにまとめて出せば、<b>仕事の途中で承認を求めることにならない</b>",
         "<b>schema が公開契約そのものなので、版の変更は破壊的である。</b>"
         "実行時に後方互換を持つと、古い形と新しい形の両方を通る道ができ、<b>その分岐がバグの住処になる</b> ── "
         "隠すより、破壊であると宣言するほうが安全である",
         "<b>後方互換を捨てることと、移行を捨てることは別である。</b>"
         "捨てるのは実行時の互換だけで、<b>移行の宣言と、当てる道具は残す</b>"],
  pick=("D", "<b>schema の版は Waffle の版に従う。</b>更新しない限り、前の版のまま使い続けられる。"
             "更新のときに合わない document を列挙し、schema が宣言した移行を当て、"
             "<b>宣言に無いものだけを人へ回す</b>。宣言で書けない壊し方は禁じ、型を分ける（案C）"),
  grounds=[
   ("宣言で持つこと", "手続きで書くと、何が起きるかは書いた人にしか分からない。宣言なら、何が当たるかを読める", "決まり", "論点6 の決まり（規則は宣言で持ち、能力を要するものは名前で指す）"),
   ("書けない移行を禁じること", "宣言に無い直し方が要るなら、それは値を作り直す変更であり、型を分けるべきものである", "決まり", "論点5 の決まり（型は横に増える）"),
   ("AI に推測させないこと", "推測した直しは静かに間違い、間違ったことも残らない", "前提", "落とした案の構造からの帰結"),
   ("版を Waffle に従わせること", "更新しない限り schema と document は同じ版なので、ずれる瞬間が1つに決まる", "前提", "更新を、ずれが起きる唯一の契機として置く"),
   ("当てるのを更新に寄せること", "当てるのは状態を変える操作なので人が通すが、仕事の途中で割り込ませない", "決まり", "論点7・9 の決まり（承認は境目で。記録と承認は命令）"),
  ],
  costs=["宣言で書ける移行しか運べない。値を作り直す変更は、型を分けることになる",
         "<b>更新の直後に、当てる作業が要る</b> ── 更新と作業再開のあいだに一手挟まる",
         "更新を何段も飛ばすと、段を順に当てることになる"],
  decision=[
   ("決定", "<b>schema の版は Waffle の版に従う。</b>更新しない限り前の版のまま使い続けられ、"
            "更新のときに合わない document を列挙して、<b>schema が宣言した移行を当てる</b>"),
   ("破壊の扱い", "<b>版の変更は破壊的な変更である。それを隠さない。</b>"
                "schema が公開契約そのものなので、実行時に後方互換を持つと分岐がバグの住処になる"),
   ("静かに壊さない", "<b>破壊してよいが、気づかれない破壊は禁じる。</b>"
                  "更新のときに必ず列挙され、当たらないものは人へ回ること ── "
                  "値の意味を変える変更は宣言で書けないので、型を分ける"),
   ("まとめて上げる", "<b>破壊してよいことと、頻繁に破壊してよいことは別である。</b>"
                  "上げる回数がそのまま利用者の手間になるので、小刻みに壊さず、区切って一度に上げる"),
   ("移行は捨てない", "捨てるのは実行時の互換だけである ── <b>移行の宣言（旧名→新名 ・ 削除 ・ 既定値）と、"
                  "当てる道具は残す。</b>AI は推測で直さない"),
   ("次", "論点12（決定と変更の記録）へ進む"),
  ],
  weaknesses=["<b>「再定義が終わった」と誰が言うのかは決めていない。</b>そこを境に、壊す変更の可否が入れ替わる",
              "<b>当てないまま使い続けられるかは決めていない</b> ── 更新の条件にするのか、後回しを許すのか",
              "<b>宣言した移行が、実際に当たったかを確かめる方法は決めていない</b> ── 当て損ねは、静かなずれとして残る",
              "<b>複数のリポジトリで Waffle の版が違うと、混在は起こりうる</b> ── リポジトリの中で揃っていれば問題にならないが、揃っているかは誰も見ていない"])

t12 = Topic(12, "決定と変更の記録", "決定の記録と、変更の記録は、それぞれどの層に属するか", "決着",
  answer="<b>B ── 変更の記録は abstract の共通契約、ADR は concrete の型（3種）</b>",
  note="<b>問いを立て直した。</b>もとは「ADR と変更の記録を、concrete の型としてどう置くか」だったが、"
       "<b>変更の記録は型ごとに置くものではない</b> ── どの型の文書にも要るので、"
       "<b>abstract が全ての型に課す共通の契約</b>である。"
       "ADR はその契約に従う<b>文書の型の1つ</b>で、spec などと同列に並ぶ。"
       "<b>そして ADR は、特定の場面のためのものではない</b> ── <b>あらゆる意思決定を残せる型</b>である"
       "（名前の由来としては Architectural Decision Record が通りだが、"
       "ここでは<b>任意の決定を残す型</b>として定義する）。",
  figures=[ADRFIG],
  tables=[Table("2つの記録は、何が違うか",
    ["何であるか", "属する層", "単位", "寿命"],
    {"変更の記録": ["<b>すべての文書が持つ仕組み</b>", "<b>abstract（共通契約）</b>",
                "文書1本の、承認と承認のあいだ", "<b>承認で畳まれて消える</b>"],
     "決定の記録（ADR）": ["<b>1つの文書の型</b>", "<b>concrete（spec などと同列）</b>",
                      "決定1つ", "<b>残り続ける</b>"]},
    lead="<b>層が違うので、そもそも並べて置くものではなかった。</b>"
         "片方は仕組み、もう片方は文書である。", plain=True),
   Table("ADR は、どの決定にも使える ── 種を3つ持つ",
    ["何を決めた記録か", "いつ書くか", "その種だけが持つもの"],
    {"汎用": ["<b>あらゆる意思決定</b>", "境目に関わらず、決めたとき",
            "── 共通の欄だけで足りる"],
     "ブレスト → spec": ["<b>何が正しいか</b>を決めた記録", "承認①（論点7）",
                     "反証を通過した案 ・ 落とした案と理由 ・ 代償"],
     "spec → 実装": ["<b>どう作るか</b>を決めた記録", "承認②（論点7）",
                  "構成と技術の選択 ・ その決定が縛る節の印"]},
    lead="<b>ADR は特定の場面に特化した型ではない。</b>"
         "承認の境目で書かれるものが多いだけで、<b>境目の外の決定も同じ型で残せる</b>。", plain=True),
   Table("この選択が置くもの",
    ["新しい型を足したとき", "ADR 自身の変更", "同じ仕組みが何か所に現れるか"],
    {"A": ["<b>その型にも、変更の記録を書き足す</b>", "型ごとの書き方しだい", "<b>型の数だけ</b>"],
     "B": ["<b>書くことが無い（契約に従う）</b>", "<b>同じ仕組みで差分が出て、承認される</b>", "<b>1か所</b>"],
     "C": ["書くことが無い", "決定も契約になる", "1か所（ただし文書が持てない）"]})],
  kept=[
   Option("変更の記録も ADR も、concrete の型として置く",
          "型ごとに、変更をどう記録するかを決める",
          "<b>同じ仕組みが型の数だけ現れる。</b>新しい型を足すたびに書き足すことになり、"
          "書き方が型ごとにずれる",
          before="ADR を型にし、変更の記録はその中の節にする",
          why="<b>変更の記録は型ごとに置くものではない</b>と分かったので、案の並べ方そのものを立て直した ── "
              "問いは「どちらの型に入れるか」ではなく「<b>どの層に属するか</b>」だった"),
   Option("変更の記録は abstract の共通契約、ADR は concrete の型の1つ",
          "どの型の文書も、承認済みの値といまの値の2状態を持つ（論点9 の決まり）。"
          "<b>ADR はその契約に従う文書で、spec などと同列に並ぶ</b>",
          "<b>理由が2か所に散る。</b>畳む前は変更の記録、残すものは ADR ── どちらに書くかを、書くたびに判断する"),
   Option("決定も abstract の共通契約にする",
          "どの文書にも「なぜこの形か」を書く欄を、契約として持たせる",
          "<b>決定が文書として立たない。</b>1つの決定が複数の文書にまたがるとき、置き場所が決まらない"),
  ],
  dropped=[
   ("ADR を持たず、決定の理由も変更の記録に書く",
    "承認で畳まれた瞬間に、<b>なぜその形にしたかが消える</b>。次に同じ判断をするとき、また一から考えることになる"),
   ("変更の記録を、型ごとに違う形で持つ",
    "同じものを型の数だけ書くことになり、<b>書き方が型ごとにずれる</b>。ずれても、比べる相手がいないので気づけない"),
  ],
  found=["<b>変更の記録は仕組みで、ADR は文書である。</b>層が違うので、並べて「どちらに入れるか」を問うこと自体が誤りだった",
         "<b>ADR 自身も、変更の記録を持つ。</b>共通契約なので例外が無い ── "
         "ADR を書き換えれば差分が出て、承認を通ることになる",
         "<b>論点9 の決まりは、abstract に属していた。</b>「承認済みの値といまの値の2状態」は特定の型の話ではなく、"
         "<b>すべての文書に課される契約</b>である",
         "<b>版を上げる判断も、ADR に残る側である</b>（論点11）── いつ・何を壊したかは、決定として残る",
         "<b>ADR は場面に特化しない。</b>承認の境目で書かれることが多いだけで、"
         "<b>境目の外の決定（運用の取り決め、規約の変更）も同じ型で残せる</b> ── "
         "種を3つ持ち、境目に結びつく2つと、どこでも使える汎用に分かれる"],
  pick=("B", "<b>変更の記録は abstract の共通契約</b>として全ての型に課し、"
             "<b>ADR は concrete の型の1つ</b>として spec などと同列に置く。"
             "<b>ADR は場面に特化せず、汎用 ／ ブレスト→spec ／ spec→実装 の3種を持つ</b>"),
  grounds=[
   ("変更の記録を abstract に置くこと", "どの型の文書にも要る仕組みであり、型ごとに書き分けるものではない", "決まり", "論点1 の決まり（abstract は全ての型が継ぐ契約である）"),
   ("2状態を契約にすること", "承認済みの値といまの値を持ち、承認で畳む ── これは型を問わず同じである", "決まり", "論点9 の決まり"),
   ("ADR を concrete に置くこと", "ADR は決定を残す文書であり、欄を持つ1つの型である", "前提", "ADR の目的（意思決定を残すこと）"),
   ("場面に特化させないこと", "決定は承認の境目でだけ起きるものではない ── 運用や規約の決定も、同じ形で残せる", "前提", "ADR を「任意の決定の記録」として置く、という定義"),
   ("共通にすると1か所で済むこと", "型が増えても、変更の記録について書くことは増えない", "決まり", "論点5 の決まり（型は横に増える。abstract は太らない）"),
  ],
  costs=["理由を書くとき、決定として残すのか、変更として畳むのかを判断することになる",
         "ADR へ移す操作を、承認の手順の中に置く必要がある"],
  decision=[
   ("決定", "<b>変更の記録は abstract の共通契約</b>としてすべての型に課し、"
            "<b>ADR は concrete の型の1つ</b>として spec などと同列に置く"),
   ("理由", "<b>層が違うので、並べて「どちらに入れるか」を問うこと自体が誤りだった</b> ── "
            "変更の記録は<b>すべての文書が持つ仕組み</b>で、ADR は<b>決定を残す文書</b>である"),
   ("ADR の広さ", "<b>ADR は場面に特化しない。</b>汎用 ／ ブレスト→spec ／ spec→実装 の3種を持ち、"
                "<b>承認の境目の外の決定も同じ型で残せる</b>。境目が増えたら、種を横に足す"),
   ("例外が無いこと", "<b>ADR 自身も共通契約に従う</b> ── ADR を書き換えれば差分が出て、承認を通る"),
   ("何が残るか", "畳まれて消えるのは変更の理由、残るのは決定の理由。"
                "<b>版を上げる判断（破壊的変更を通す判断）も、残る側である</b>（論点11）"),
   ("次", "論点13（引く仕様）へ進む ── <b>これが最後の論点である</b>"),
  ],
  weaknesses=["<b>3つの種が、本当に3つで足りるかは確かめていない。</b>境目が増えれば種も増える ── そのとき横に増やせる形になっているか",
              "<b>どの変更が ADR に値するかの線は引いていない。</b>引かないと、全部を ADR に書くか、何も書かないかに倒れる",
              "<b>ADR が持つ欄は決めていない</b> ── 決定・理由・落とした案・引き受けること、まではこの盤面の形が使えそうだが、確かめていない",
              "<b>1つの決定が複数の文書にまたがるとき、ADR がどう指すかは決めていない</b> ── 指す先は節の印になるはずだが、まだ書いていない"])

t13 = Topic(13, "引く仕様", "節を引く仕様を、どう決めるか", "決着",
  answer="<b>C ── 述語の語彙は abstract、引き方は concrete。どの引き方でも読み方を添える</b>",
  note="<b>いまは JMESPath の式1つで引いている。</b>式は JSON の構造に対するもので、"
       "schema が持つ意味（印・型・軸）を知らない ── そこが、この論点である。"
       "<b>そして schema が形を固定している以上、その型をどう引くかも型ごとに決まっているはずである</b> ── "
       "引き方は、取得する側が毎回考えるものではない。",
  figures=[QUERY],
  tables=[Table("この選択が置くもの",
    ["引く側が知っている必要のあるもの", "構造が変わったとき", "同じ問いの書き方", "規則との関係"],
    {"A　構造の式": ["<b>document の構造</b>", "<b>式を直す</b>", "何通りもある", "別の語彙になる"],
     "B　引き方ごとの動詞": ["動詞の名前", "動詞の中で吸収できる", "1通り", "別の語彙になる"],
     "C　意味の述語": ["<b>意味（印・型・軸）だけ</b>", "述語は変わらない", "1通り", "<b>規則と同じ語彙</b>"]})],
  kept=[
   Option("構造の式で引く（いまの形）",
          "JMESPath の式を渡し、document の構造を辿って取り出す",
          "<b>引く側が構造を知ることになる。</b>構造が変われば式も直り、"
          "印・型・軸といった意味は式の中に埋もれる"),
   Option("引き方ごとに動詞を持つ",
          "「節を取る」「印で探す」「型で絞る」…を、それぞれ別の操作として固定する",
          "<b>引き方が増えるたびに動詞が増える</b> ── 論点5 の決まり（動詞は abstract が固定する）と衝突する"),
   Option("意味の述語で引く。<b>引き方は concrete が宣言する</b>",
          "「引く」1つの動詞に述語を渡す。<b>述語の語彙は abstract が持ち（規則と同じもの）、"
          "その型でよく引く形は concrete が名前を付けて宣言する</b> ── 構造の式は、契約の外の逃げ道として残す",
          "<b>語彙が判定と取得の両方に効くので、変えると両方に響く。</b>"
          "式で引いたときは、当たった節から読み方を引くことになる ── "
          "<b>1つの節に解決しない式では、それができない</b>",
          before="意味の述語で引く（述語の語彙は、規則が使うものと同じにする）",
          why="<b>schema が形を固定しているなら、その型をどう引くかも型ごとに決まっている</b> ── "
              "語彙だけを共通にして引き方を毎回組み立てるのではなく、"
              "<b>引き方そのものを concrete が宣言する</b>（論点6 の名簿と同じ2段になる）"),
  ],
  dropped=[
   ("式と述語の両方を持てるようにする",
    "同じ問いに2通りの書き方ができ、<b>どちらで書くかの判断が毎回増える</b>。しかも式の側は構造に依存し続ける"),
  ],
  found=["<b>「読み方が付かない取得を作らない」と決めると、式で引くことも許せる。</b>"
         "式が1つの節に解決するなら、その節の読み方を引いて添えられる ── "
         "<b>式そのものが悪いのではなく、何を引いたか分からないまま返すことが悪い</b>",
         "<b>これは論点6 の名簿と、そっくり同じ2段である</b> ── "
         "<b>述語の語彙は abstract</b>（全ての型に共通）、<b>その型の引き方は concrete</b>（型ごとに違う）。"
         "検査の名簿で引いた線が、そのまま取得にも引ける",
         "<b>引く単位が決まると、渡すものも決まる。</b>論点10 で「塊で引いたら、その塊の読み方を渡す」と決めたので、"
         "<b>引き方の選択は取得だけの話ではなくなった</b> ── 式で引くと、何を引いたのかが返り値から決まらない",
         "<b>述語の語彙は、もう決まっている。</b>論点6 で abstract が持つと決めたものを、取得にも使えばよい ── "
         "<b>規則では「この述語を満たすか」を判定し、取得では「満たすものを出せ」と引く</b>",
         "<b>述語を1つ足すと、規則にも取得にも同時に効く。</b>逆に、述語で書けないものは規則にも書けない ── "
         "この一致は、論点6 で引いた線がそのまま取得の線になることを意味する",
         "<b>いまの実装は、すでに引く操作を1つへ寄せてある</b>（<code>query_path</code> 1つ）── "
         "変わるのは渡すものが式か述語かであって、操作の数ではない"],
  pick=("C", "「引く」動詞に意味の述語を渡す。<b>述語の語彙は abstract が持ち、その型の引き方は concrete が宣言する。</b>"
             "構造の式（JMESPath）でも引けるようにするが、<b>どの引き方でも、当たった節の読み方を必ず添えて返す</b> ── "
             "<b>読み方が付かない取得を作らない</b>"),
  extras=[("引き方は、型ごとに宣言する", """<p class="lead"><b>schema が形を固定しているなら、その型をどう引くかも型ごとに決まっている。</b>
毎回そこから組み立てるのではなく、<b>よく引く形に名前を付けて concrete が宣言する</b>。</p>
<table><tr><th>層</th><th>持つもの</th><th>例</th></tr>
<tr><td><b>abstract</b></td><td>述語の語彙　＋　引き方を宣言する仕方</td><td>型＝… ／ 状態＝… ／ 印が指す先＝…</td></tr>
<tr><td><b>concrete（spec）</b></td><td>その型でよく引く形</td><td>シナリオの一覧 ／ 受け入れ基準 ／ 未検のもの</td></tr>
<tr><td><b>concrete（ADR）</b></td><td>同上</td><td>決定の一覧 ／ 落とした案 ／ この節を縛る決定</td></tr>
<tr><td>どちらでもない</td><td><b>構造の式（JMESPath）</b></td><td>その場かぎりの取り出し ── <b>当たった節の読み方を添えて返す</b></td></tr></table>
<p class="lead"><b>式でも引ける。ただし読み方は必ず添える。</b>
式が1つの節に解決するなら、その節の <code>x-prompt-query</code> を引いて返せる ──
<b>読み方が付かない取得を作らない</b>のが、ここでの規律である。
複数の節に散る式や、値を組み立てる式は添えられないので、<b>「添えられない」と返す</b>（黙って落とさない）。</p>
<p class="lead"><b>これは論点6 の名簿と同じ2段である。</b>
共通のものは abstract が持ち、型ごとのものは concrete が宣言する ──
<b>型が増えても abstract は太らない。</b></p>"""),
   ("引いた結果に、何が付いてくるか", """<p class="lead"><b>論点10 で「塊で引いたら、その塊の <code>x-prompt-query</code> を渡す」と決めた。</b>
つまり<b>引く単位が決まると、渡すものも決まる</b> ── 引き方の選択は、取得だけの話ではない。</p>
<table><tr><th></th><th>引く単位をどう指すか</th><th>返るものに、読み方が付くか</th></tr>
<tr><td><b>A</b>　構造の式</td><td>式が辿り着いた場所 ── <b>塊なのか欄なのかは、式しだい</b></td>
<td><b>付けられない。</b>どの階層を引いたのかが、返り値からは決まらない</td></tr>
<tr><td><b>C</b>　意味の述語</td><td><b>「型＝シナリオ」のように、意味で指す</b></td>
<td><b>付く。</b>指した階層が分かっているので、その階層の読み方を合成して返せる</td></tr></table>
<p class="lead"><b>式で引くと、返ってきたものが何なのかを、受け取った側が判断することになる。</b>
述語で引けば、<b>何を引いたかが引く側の宣言として残る</b> ── だから読み方を添えられる。</p>""")],

  grounds=[
   ("述語で引くこと", "abstract は「何が違反かを書く語彙」を持つと決めた ── その語彙は、取得の絞り込みにもそのまま使える", "決まり", "論点6 の決まり（述語 ＋ 検めることの名前）"),
   ("引き方を concrete が宣言すること", "schema が形を固定している以上、その型をどう引くかも型ごとに決まっている", "前提", "構造化した時点で、取得の形も決まるという置き方"),
   ("2段に分けること", "共通のものは abstract、型ごとのものは concrete ── 型が増えても abstract は太らない", "決まり", "論点6 の決まり（名簿は2段）"),
   ("構造の式をやめること", "式は JSON の構造に対するもので、schema が持つ意味を知らない", "前提", "JMESPath が何に対する式かという性質"),
   ("動詞を増やさないこと", "引き方が増えるたびに動詞が増えるなら、型が増えるたびに動詞が増えるのと同じことが起きる", "決まり", "論点5 の決まり（動詞は abstract が固定する）"),
   ("操作を1つに保つこと", "引く操作は、いまも1つに寄せてある", "実測", "waffle query の操作は query_path 1つ（CLI で確認）"),
  ],
  costs=["<b>読み方を添えられない式は、「添えられない」と返すことになる</b> ── 値だけ返して済ませない",
         "述語の語彙が判定と取得の両方に効くので、語彙を変えるときは両方を確かめることになる",
         "<b>型を足すたびに、その型の引き方を宣言する手間が増える</b>（宣言しなければ、述語で引くだけになる）"],
  decision=[
   ("決定", "<b>「引く」動詞に意味の述語を渡す。</b>述語の語彙は abstract が持ち、規則の判定と同じものを使う"),
   ("2段に分ける", "<b>その型でよく引く形は、concrete が名前を付けて宣言する</b> ── "
                "論点6 の名簿と同じ2段になり、型が増えても abstract は太らない"),
   ("式も残す", "<b>構造の式（JMESPath）でも引けるようにする。</b>式そのものが悪いのではない"),
   ("読み方を必ず添える", "<b>どの引き方でも、当たった節の <code>x-prompt-query</code> を添えて返す</b> ── "
                    "<b>読み方が付かない取得を作らない</b>。添えられない式は「添えられない」と返し、黙って落とさない"),
   ("引く単位が渡すものを決める", "塊で引けば塊の読み方、欄で引けば欄の読み方が付く（論点10）── "
                          "<b>何を引いたかが分かっていることが、添えられる条件である</b>"),
   ("次", "<b>12論点すべてが決着した。</b>次は、この決まりを段1・段2 の spec へ落とす"),
  ],
  weaknesses=["<b>述語で書けない取得が実際にどれだけあるかは数えていない。</b>いまの式の使われ方を洗い出していない",
              "<b>式がどこまで「1つの節に解決する」と言えるかは決めていない</b> ── 並びの射影や組み立てを含む式で、境目が動く",
              "<b>既存の式を、どう移すかは決めていない</b> ── 式のまま残す期間を置くのか、一度に切り替えるのか"])


t14 = Topic(14, "型を立てる基準", "何を1つの型として立てるか", "新規",
  answer="推しは D（軸の集合が違えば別の型。種は、その層で決まることの単位）",
  note="<b>論点15 と 16 を決めたあとで、この基準を書き直した。</b>"
       "どちらも<b>欄の集合を見比べて決めたのではない</b> ── 「何に依存して変わるか」を宣言し、"
       "そこから層を導出して切った。<b>欄の違いは、その結果として現れたものだった。</b>",
  tables=[Table("15 と 16 は、実際どう決めたか",
    ["何で切ったか", "種は何になったか", "欄の違いは"],
    {"論点16（Coding）": ["<b>軸</b>（言語 ・ アーキ ・ 用途 ・ 実行環境）",
                     "<b>その層で決まることの単位</b>（雛形15本）", "結果として違う"],
     "論点15（Spec）": ["<b>軸</b>（業務 ・ 接点 ・ 基盤）",
                    "<b>その層で決まることの単位</b>", "結果として違う"]},
    lead="<b>2つとも同じ形で決まっていた。</b>基準を後から書くなら、この形に合わせるしかない。", plain=True),
   Table("この選択が置くもの",
    ["「1欄だけ違う」ときに何を問うか", "型と種の関係", "新しい領域が来たとき"],
    {"B　欄の集合": ["<b>欄を見比べる</b> ── 閾値を決めることになる", "欄が違えば型、似ていれば種", "欄を並べて比べる"],
     "D　軸の集合": ["<b>その言明は、どの軸に依存して真か</b>と問う", "<b>軸の集合が型、層で決まる単位が種</b>",
                 "<b>まず軸を立てる</b>（独立に動くかで判定できる）"]})],
  kept=[
   Option("宣言する欄の集合が違えば、別の型にする",
          "同じ欄で書けるなら同じ型とし、違いは種で表す",
          "<b>「ほぼ同じだが1欄だけ違う」で必ず迷う。</b>閾値を決めることになり、その閾値に根拠が無い",
          before="<b>これを推していた</b>（論点15・16 を決める前）",
          why="<b>15 と 16 は、欄を見比べて決めていなかった</b> ── どちらも軸を宣言して層を導出した。"
              "欄の違いは結果なので、<b>結果を基準に据えていた</b>ことになる"),
   Option("<b>軸の集合が違えば別の型。種は、その層で決まることの単位</b>",
          "その領域が<b>何に依存して変わるか</b>を軸として立て、軸の集合が違えば別の型にする。"
          "層は軸の組み合わせから導出し、<b>種はその層で決まることの単位</b>にする",
          "<b>軸を立てる判断が先に来る。</b>独立に動くかで判定できるが、"
          "<b>新しい領域では、軸そのものを見つける手間がかかる</b>"),
   Option("読み手と用途が違えば、別の型にする",
          "誰が何のために読むかで切る",
          "<b>同じ構造の文書が、読み手の数だけ増える。</b>読み手は射影で分けられる（論点8）"),
   Option("射影先（できあがる成果物）が違えば、別の型にする",
          "SKILL.md になるもの、HTML になるもの…で切る",
          "<b>形で切ることになり、論点8 と正面から衝突する</b>"),
  ],
  dropped=[
   ("すべてを1つの型にし、種だけで並べる",
    "軸を立てずに種を並べると、<b>種ごとに構造がまるで違うものが1つの型に入る</b> ── "
    "論点16 で「層で切る」と決めたのと同じ理由で成り立たない"),
  ],
  found=["<b>基準を、実際の決め方に合わせて書き直した。</b>15 と 16 は欄を見比べていない ── "
         "<b>軸を宣言し、層を導出して切った</b>。欄の違いは結果である",
         "<b>「1欄だけ違う」の迷いは、軸で問えば消える</b> ── 見比べるのではなく、"
         "<b>その言明はどの軸に依存して真か</b>を問えばよい",
         "<b>型と種の役割が、これではっきりする</b> ── <b>型は軸の集合、種はその層で決まることの単位</b>。"
         "Coding も Spec も、この形で説明できる"],
  path=[
   "最初は<b>「欄の集合が違えば別の型」</b>を推していた ── 型は意味の分類で、欄がその意味を表しているから",
   "しかし<b>「1欄だけ違うときどうするか」に答えられなかった</b>。閾値を決めることになり、その閾値に根拠が無い",
   "論点16（Coding）と論点15（Spec）を決めたあとで、<b>自分が実際に何で切ったかを見直した</b> ── どちらも欄を見比べていない",
   "<b>2つとも「何に依存して変わるか」を軸として立て、そこから層を導出して切っていた。</b>欄の違いは、その結果として現れていた",
   "そこで基準を<b>結果ではなく、切った理由の側へ</b>置き直した ── 型は軸の集合、種はその層で決まることの単位",
   "置き直すと、<b>「1欄だけ違う」の迷いが消える</b> ── 見比べるのをやめて「その言明はどの軸に依存して真か」を問えばよい",
  ],
  pick=("D", "<b>軸の集合が違えば別の型にする。</b>層は軸の組み合わせから導出し、"
             "<b>種は、その層で決まることの単位</b>にする ── 欄の集合の違いは、その結果として現れる"),
  grounds=[
   ("軸で切ること", "型が増えても abstract が太らないのは、軸の値が増えるだけだからである", "決まり", "論点5 の決まり（型は横に増える）"),
   ("実際にそう決めたこと", "Coding も Spec も、軸を宣言して層を導出する形で決着した", "決まり", "論点16 ・ 論点15 の決まり"),
   ("軸を独立性で判定できること", "1つを固定して他が動くなら、それは軸である", "決まり", "CodingSkills 論点2 の決まり"),
   ("読み手や形で切らないこと", "読み手の違いは射影で吸収でき、形で切ると意味が1つなのに型が増える", "決まり", "論点8 の決まり"),
  ],
  costs=["<b>軸を立てる判断が先に来る</b> ── 新しい領域では、軸そのものを見つける手間がかかる",
         "既存の型を、この基準で見直すことになる ── 軸を宣言していない型が出る"],
  weaknesses=["<b>この基準を、Coding と Spec 以外に当てていない</b> ── ADR ・ Knowledge ・ Skill ・ Agent ・ Hook の軸は、まだ言えていない",
              "<b>軸が1つしか無い領域では、型と種の区別が薄くなる</b> ── そのとき何を種にするかは決めていない"])

t15 = Topic(15, "Spec の種類", "spec を、どの単位で型にするか", "決着",
  answer="<b>B ── 軸は業務（主題）・接点・基盤。層はどれも業務を含む</b>",
  note="<b>根拠にできるのは、ここまでで決まったことだけである。</b>"
       "いま在る schema は再定義の対象そのものなので、それを根拠にすると循環する ── "
       "<b>一度その誤りをやって、案を作り直した。</b>",
  figures=[SPECLAYER],
  tables=[Table("2つの関係を、分けて見る",
    ["変わり方（軸として独立か）", "存在（それだけで成り立つか）"],
    {"業務": ["<b>独立に動く</b>", "<b>成り立つ</b> ── これが主題である"],
     "接点": ["独立に動く（同じ業務を画面でも API でも）", "<b>成り立たない</b> ── 何を差し出すかが無い"],
     "基盤": ["独立に動く（同じ業務を別の基盤で）", "<b>成り立たない</b> ── 何を動かすかが無い"]},
    lead="<b>「独立に動く」と「それだけで成り立つ」は別の関係である。</b>"
         "軸としては3つとも独立に動くが、<b>存在としては接点も基盤も業務に掛かる</b> ── "
         "だから並列ではない。", plain=True),
   Table("この選択が置くもの",
    ["接点が1つ増えたとき", "業務 × 接点をどこに書くか", "同じ問題に何通りの形を持つか"],
    {"A": ["<b>型が1つ増える</b>", "<b>置き場所が無い</b>（どちらかへ押し込む）", "spec と Coding で別の形（2通り）"],
     "B": ["<b>軸の値が1つ増える</b>", "<b>掛けた層に書く</b>", "<b>1通り（Coding と同じ形）</b>"],
     "C": ["規約側の用途が増える", "規約に書く", "1通り（ただし約束が規約に混ざる）"]})],
  extras=[("軸ごとの原典（2026-09-06 に落として照合）", """<p class="lead"><b>3つの軸それぞれに、支える原典があるか</b>を確かめた。
落とした原文から<b>文字列で照合できたものだけ</b>を「照合済み」とし、できなかったものはそう書く。</p>
<table><tr><th>軸</th><th>原典</th><th>照合した文字列</th></tr>
<tr><td><b>業務</b></td><td><b><code>.waffle/skills/ddd-advisor</code> の knowledge 22本</b> ── 原典から引いて作ってある<br>
（<code>bounded-context</code> ・ <code>ubiquitous-language</code> ・ <code>domain-model</code> ・ <code>subdomain</code> ・
<code>context-integration</code> ほか）<br>
補助：Fowler「Bounded Context」</td>
<td>手元の knowledge をそのまま引ける<br>
<code>Bounded Context</code> ・ <code>Ubiquitous Language</code> ・ <code>context map</code>（Fowler 側で照合）</td></tr>
<tr><td><b>接点</b></td><td>Fowler「PresentationDomainDataLayering」https://martinfowler.com/bliki/PresentationDomainDataLayering.html<br>
Cockburn「Hexagonal Architecture（Ports and Adapters）」<br>
Robinson「Consumer-Driven Contracts」</td>
<td><code>Multiple presentations could be separate pages in a web app, having a web app plus mobile native apps, an API for scripting purposes, or even an old fashioned command line interface</code><br>
<code>Allow an application to equally be driven by users, programs, automated test or batch scripts</code><br>
<code>Consumer-Driven Contracts</code> ・ <code>provider</code> ・ <code>consumer</code></td></tr>
<tr><td><b>基盤</b></td><td>Bottcher「What I Talk About When I Talk About Platforms」https://martinfowler.com/articles/talk-about-platforms.html<br>
CNCF Platforms White Paper　／　Google SRE Book</td>
<td><code>self-service APIs, tools, services, knowledge and support which are arranged as a compelling internal product</code><br>
<code>platform engineering</code> ・ <code>Capabilities of platforms</code><br><code>service level indicators (SLIs), objectives (SLOs)</code></td></tr></table>
<p class="lead"><b>基盤には、確立した呼称がある</b> ── <b>Platform Engineering</b>。
そして「満たすべき要件」を書く言語も在る（<b>SLI ・ SLO</b>）── 論点15 の「基盤の層は要件を書く」と、そのまま噛み合う。</p>
<p class="lead"><b>接点の原典は DDD ではない。</b>DDD は業務が主題で、接点は「ドメインの外へ出す」としか言わない ──
<b>接点そのものを扱うのは、別の2つである。</b></p>
<table><tr><th>原典</th><th>それが言っていること</th><th>接点のどこを覆うか</th></tr>
<tr><td>Fowler　presentation ／ domain ／ data の層分け</td>
<td><b>同じ業務の上に、複数の presentation を載せられる</b> ── web の画面、モバイルの native、
スクリプト用の API、<b>昔ながらのコマンドラインまで</b></td><td><b>接点が複数あることそのもの</b></td></tr>
<tr><td>Cockburn　Ports and Adapters</td>
<td><b>人 ・ プログラム ・ 自動テスト ・ バッチのどれからも、等しく駆動できるようにする</b></td>
<td><b>接点を差し替えられること</b>（UI も API も CLI も同格）</td></tr>
<tr><td>Robinson　Consumer-Driven Contracts</td><td>提供側と利用側のあいだの約束を、利用側から定める</td>
<td>機械どうしの接点の<b>約束の確かめ方</b></td></tr></table>
<p class="lead"><b>あなたの言う接点は、Fowler の言う presentation そのものである</b> ──
UI ・ API ・ CLI ・ MCP は、<b>同じ業務の上に載る別の presentation</b>。
そして Ports and Adapters が、それらを<b>同格に扱ってよい</b>根拠になる。</p>
<p class="lead"><b>「境界」という語で2つを混ぜない。</b>
DDD が扱う<b>文脈どうしの連係</b>（上流と下流、モデル変換装置）は、
<b>業務の側の話</b>である ── 誰に差し出すかではなく、2つの業務モデルをどうつなぐかを決めている。
<code>ddd-advisor</code> の <code>context-integration</code> がそれに当たる。</p>
<table><tr><th>まだ照合していないもの</th><th>なぜ挙げるか</th></tr>
<tr><td>画面という接点の中身をどう設計するか（Interaction Design ・ HCI）</td>
<td>上の3本は「接点が複数あること」「差し替えられること」「約束の確かめ方」を扱うが、<b>画面の設計論そのものは扱っていない</b></td></tr>
<tr><td>人向けの接点の設計論（Interaction Design ・ HCI）</td><td>画面という接点<b>の中身をどう設計するか</b>は、上の3つが扱っていない ── 原文を落としていない</td></tr></table>""")],
  kept=[
   Option("系ごとに型を並べる（業務 ・ 画面 ・ 基盤 …）",
          "「何についての spec か」で型を分け、系が増えたら型を足す",
          "<b>系が増えるたびに型が増える。</b>そして<b>掛かり合い（この業務を、この接点でどう差し出すか）を置く場所が無い</b>",
          before="いまの3系（Domain ・ Presentation ・ Platform）を保つ",
          why="<b>いま在る schema を根拠にしていた</b> ── それは再定義の対象そのものなので、循環する。"
              "決まったことだけから立て直したところ、<b>問いは「いまの系でよいか」ではなく「どの向きで切るか」</b>だった"),
   Option("軸を宣言し、層から導出する（Coding と同じ形）",
          "業務 ・ 接点 ・ 基盤 を軸として宣言し、<b>層は軸の組み合わせから導出</b>する。"
          "種は、その層で決まることの単位。<b>画面か API かは、接点という軸の値になる</b>",
          "<b>「業務の spec」という単独の型が無くなる</b> ── 引くときは軸で指す（論点13）。"
          "また<b>軸の値をどこまで認めるかの判断が要る</b>",
          before="「何を決めるか」で切り直す（業務 ・ 画面 ・ 基盤 ・ 契約）",
          why="切り直しても、<b>系を並べる形は変わらない</b> ── 掛かり合いの置き場所が無いままである。"
              "論点16 で Coding について決めた形を、そのまま当てるほうが筋が通る"),
   Option("業務だけを spec とし、接点と基盤は規約へ寄せる",
          "「何が正しいか」は業務だけとし、接点と基盤は「どう作るか」の側で扱う",
          "<b>約束（誰に何を返すか）が規約に混ざる。</b>"
          "承認の境目で見るのは spec なので、<b>約束が承認の外に出る</b>"),
  ],
  dropped=[
   ("すべてを1つの型にし、種だけで並べる",
    "層を導出せずに種だけを並べると、<b>種ごとに構造がまるで違うものが1つの型に入る</b> ── "
    "論点16 で「層で切る」と決めたのと、同じ理由で成り立たない"),
   ("出力の形（画面 ・ 帳票 ・ API）で型を分ける",
    "それは<b>形で切ること</b>であり、論点8 の決まり（型は意味、形は射影）と正面から衝突する"),
  ],
  found=["<b>業務は主題であって、接点や基盤と並列ではない。</b>"
         "業務が無ければ、接点も基盤も<b>何についての言明にもならない</b> ── "
         "「独立に動く」ことと「それだけで成り立つ」ことは、別の関係である",
         "<b>だから spec の層は、どれも業務を含む</b> ── 業務 ／ 業務 × 接点 ／ 業務 × 基盤。"
         "<b>接点と基盤は、それぞれ独立に業務へ掛かる</b>（互いを知らない）",
         "<b>ここに、spec と規約の線が出た。</b>"
         "業務を含まない「接点だけ」（MCP や HTTP の約束の形）「基盤だけ」（その基盤の使い方）は、"
         "<b>何を約束するかではなく、どう作るかの話</b> ── だから規約の側にある（CodingSkills の用途の層）。"
         "<b>前に「外部との契約の spec が無い」と言ったが、それは業務 × 接点の層に入る</b>",
         "<b>「画面向け」と「API 向け」は、形の違いではなく約束の違いである。</b>"
         "同じ節を Markdown と HTML で出すのは射影だが、<b>人に差し出すか機械に差し出すかは、約束そのものが違う</b> ── "
         "だから接点は形ではなく軸である",
         "<b>掛かり合いの置き場所が、系を並べる形では無い。</b>"
         "「この業務を、この接点でどう差し出すか」は業務にも接点にも属さない ── <b>掛けた層にしか書けない</b>",
         "<b>Coding で決めた形が、そのまま spec にも当たる。</b>"
         "同じ問題に2つの形を持たないほうが、引き方も検査も1通りで済む（論点13 ・ 論点6）"],
  pick=("B", "<b>spec も、軸を宣言して層から導出する形にする。</b>"
             "軸は 業務（主題）・ 接点 ・ 基盤 で、<b>層はどれも業務を含む</b>（業務 ／ 業務 × 接点 ／ 業務 × 基盤）。"
             "<b>業務を含まない層は spec ではなく、規約の側にある</b>"),
  grounds=[
   ("軸で切ること", "業務 ・ 接点 ・ 基盤 は、1つを固定しても他が動く", "決まり", "CodingSkills 論点2 の決まり（独立に動くなら軸である）"),
   ("業務を主題に置くこと", "業務が無ければ、接点も基盤も何についての言明にもならない", "前提", "spec が「何が正しいか」を述べる文書であること（論点7 の切り分け）"),
   ("業務を含まない層を外すこと", "業務を含まない接点や基盤の話は、約束ではなく作り方である", "決まり", "論点7 の決まり（spec は何が正しいか、規約はどう作るか）"),
   ("層を導出すること", "層はあらかじめ並べるものではなく、宣言された軸から出てくる", "決まり", "論点16 の決まり（層は軸から導出、種は雛形の単位）"),
   ("形で切らないこと", "出力の形で型を分けると、意味が1つなのに型が増える", "決まり", "論点8 の決まり（型は意味、形は射影）"),
   ("約束を spec に残すこと", "承認の境目で人が見るのは spec である ── 約束を規約へ移すと、承認の外に出る", "決まり", "論点7 の決まり（承認は意思決定の境目でだけ）"),
  ],
  costs=["<b>「業務の spec」という単独の型が無くなる</b> ── 引くときは軸で指すことになる（論点13）",
         "<b>軸の値をどこまで認めるかの判断が要る</b>（接点に AI を値として立てるか、など）",
         "既存の spec を、この形へ移すことになる ── 再定義の一部として受け入れる（論点11）"],
  weaknesses=["<b>接点の軸を支える呼称が、1つに定まっていない</b> ── 照合できたのは機械どうしの約束と境界の置き方だけで、人向けの接点を同じ語で括る原典を当てていない",
              "<b>業務 × 接点 × 基盤 の層が要るかは分からない</b> ── 3つ掛かる言明が出たら、そのとき足す",
              "<b>基盤の軸が、本当に spec 側かは詰めていない</b> ── 要件は spec、採用は規約、という線は引けるが、当ててはいない",
              "<b>接点の値をどう列挙するかは決めていない</b> ── 人 ／ 別のシステム ／ AI で足りるのか、もっと細かいのか",
              "<b>層ごとに置く種の一覧は、まだ書いていない</b> ── Coding では15本を書き切ったが、spec では未着手である"])

t16 = Topic(16, "Coding の種類", "コーディング系の型を、どの向きで切るか", "決着",
  answer="<b>B ── 種は雛形の単位15。層は軸から導出し、種別は欄。古い4種は捨てる</b>",
  note="<b>ここに実際のずれがある。</b>CodingSchema は<b>種別</b>（architecture ・ coding-standard ・ "
       "tech-stack ・ test-standard）で切っているが、CodingSkills は12論点の末に<b>層</b>"
       "（言語 ・ アーキテクチャ ・ 言語×アーキ ・ 用途）で切ると決めた ── <b>切り方が90度ずれている。</b>",
  figures=[CODLAYER, RULECORE, CODCUT],
  tables=[Table("種は、雛形の単位になる（15）",
    ["種別", "層（軸から導出される）"],
    {"style ・ failure ・ concurrency": ["コーディング", "言語"],
     "test-mechanism": ["テスト", "言語"],
     "toolchain": ["技術スタック", "言語"],
     "layers ・ dependency": ["アーキテクチャ", "アーキテクチャ"],
     "test-boundaries": ["テスト", "アーキテクチャ"],
     "layer-mapping": ["アーキテクチャ", "言語 × アーキテクチャ"],
     "test-placement": ["テスト", "言語 × アーキテクチャ"],
     "contract ・ lifecycle": ["アーキテクチャ", "用途"],
     "acceptance ・ test-strategy": ["テスト", "用途"],
     "stack": ["技術スタック", "用途"]},
    lead="<b>CodingSkills の規約の種類15が、そのまま種15になる。</b>"
         "種別と層は、どちらも欄として持ち、層は宣言された軸から導出される。"
         "<b>雛形のファイルは16本あるが、<code>rule-core.md</code> は写して使う雛形ではなく、"
         "規則の塊の共通の形（芯）を定めるものである</b>（2026-09-06 の更新で入った）。", plain=True),
   Table("規則の塊は、芯 ＋ 種類ごとの欄でできている",
    ["どこに書くか", "誰が定めるか"],
    {"ID": ["塊の見出しの前半", "<b>芯</b>（全種類で同じ）"],
     "言明": ["塊の見出しの後半", "<b>芯</b>"],
     "検証方法": ["塊の表の欄", "<b>芯</b>"],
     "出典": ["<code>出典</code> の表の、同じ ID の行", "<b>芯</b>（塊の外に置く）"],
     "そのほかの欄": ["塊の表の続き", "<b>種別の雛形</b>（種類ごとに違う）"]},
    lead="<b>これが「15の種それぞれに必須の欄をどう決めるか」の答えになる</b> ── "
         "芯が共通の必須欄で、残りが種ごとの欄である。", plain=True),
   Table("同じ「テスト」でも、層が違えば構造が違う",
    ["層", "宣言する中身"],
    {"言語": ["言語", "テストが走る仕組みと、書き方"],
     "アーキテクチャ": ["アーキテクチャ", "確かめる単位の境界（何を単体とし、何を結合とするか）"],
     "言語 × アーキ": ["言語 × アーキ", "テストの置き場所"],
     "用途": ["用途", "何を保証するか ・ テストの重心と観点の採否"]},
    lead="<b>種別で切ると、この4つが同じ「テスト規約」という名前の文書に入る。</b>"
         "読み手は、開けるまでどれなのか分からない。", plain=True)],
  kept=[
   Option("いまの種別（4つ）を保ち、軸は欄で持つ　── <b>もともとの想定と違う</b>",
          "architecture ・ coding-standard ・ tech-stack ・ test-standard のまま、言語や用途は前置きの欄にする",
          "<b>同じ名前の文書に、構造の違うものが入り続ける。</b>"
          "「テスト規約」を引いても、言語の話か用途の話かは開けるまで分からない"),
   Option("<b>種は雛形の単位（15）。層は軸から導出し、種別は欄</b>",
          "CodingSkills の雛形1つが、そのまま CodingSchema の種1つになる ── "
          "<code>style</code> ・ <code>failure</code> ・ <code>contract</code> …。"
          "<b>層は宣言された軸から導出され、種別（コーディング ・ テスト …）は前置きの欄で横断する</b>",
          "<b>種が4つから15へ増える。</b>既存の coding 系 document 12本を作り直すことになり、"
          "<b>層をまたいで同じ名前を探せない</b> ── 横断は種別の欄で引く",
          before="層で切り、種別は欄にする（CodingSkills へ寄せる）",
          why="「層で切る」だけでは、1つの層に何本置くかが決まらない ── "
              "<b>切る単位は雛形そのものである</b>。雛形1つ＝規約の種類1つ＝種1つで、"
              "層はその種が宣言する軸から<b>導出される</b>（あらかじめ並べるものではない）"),
   Option("種別 × 層の両方でファイルを切る",
          "coding/lang.rust ・ test/lang.rust … のように掛け合わせる",
          "<b>空のマスができる。</b>言語の層に「層と依存の向き」は無いので、掛けた分だけ存在しない組が並ぶ"),
  ],
  dropped=[
   ("CodingSkills 側を、CodingSchema の切り方へ寄せる",
    "CodingSkills の12論点は<b>層で切ることを根拠つきで決着させた</b>（軸の独立性から導いた）。"
    "寄せ直すには、その根拠を覆す必要がある ── 覆す材料は無い"),
  ],
  found=["<b>ずれは名前ではなく、切る向きである。</b>種別で切ると、構造の違うものが同じ名前に入る ── "
         "<b>同じ「テスト規約」でも、言語の層では「走る仕組み」、用途の層では「何を保証するか」である</b>",
         "<b>論点14 の基準が、そのまま答えを出す</b> ── 欄の集合が違えば別の型。"
         "層が違えば宣言する中身の構造が違うのだから、<b>層で切るのが基準に合う</b>",
         "<b>いまが再定義の最中なので、作り直しは受け入れられる</b>（論点11）── "
         "定常状態に入ってからでは、これは壊す変更として重い",
         "<b>規則の一覧は、導出物になっている</b>（<code>relist.py</code> が詳細の塊から書き出す）── "
         "<b>schema 側でも一覧を持たず、導出にできる</b>。索引を導出にしたのと同じ形である",
         "<b>もともと CodingSkills に合わせる想定だった。</b>いまの4種は、その前の形が残っているだけである ── "
         "<b>4種の表現で schema を作り直すことはしない</b>"],
  pick=("B", "<b>CodingSchema の種は、CodingSkills の雛形の単位（15）にする。古い4種は捨てる。</b>"
             "層は宣言された軸から導出し、種別は前置きの欄で横断する。"
             "既存の coding 系 document は、再定義の一部として作り直す"),
  grounds=[
   ("層で切ること", "層が違えば、同じ種別でも宣言する中身の構造が違う", "決まり", "CodingSkills 論点10 の決まり（ファイル名は層が決められることの単位）"),
   ("欄の集合で判断すること", "構造が違うなら、別の型として立てる", "決まり", "論点14 の基準"),
   ("種別を欄にすること", "横断で引くための分類なので、前置きに持てば絞れる", "決まり", "CodingSkills 論点10 の決まり（種別は欄）"),
   ("雛形を単位にすること", "規約の種類1つに雛形1つが対応し、その種類ごとに必要な欄が違う", "実測", "CodingSkills の規約37本を、検査16件で検証している（雛形が定める節を持つか、を含む）"),
   ("芯を共通の必須欄にすること", "規則の塊は、種類によらず ID ・ 言明 ・ 検証方法 ・ 出典を持つ", "決まり", "CodingSkills の <code>rule-core</code>（規則の芯 ── 全種類で同じ形）"),
   ("いま作り直すこと", "再定義の最中なので、壊す変更を一度で行う", "決まり", "論点11 の決まり（適用開始点は再定義のあと）"),
  ],
  costs=["<b>種が4つから15へ増える</b> ── 種ごとに欄の集合を設計することになる",
         "<b>既存の coding 系 document 12本を作り直す</b> ── いまが再定義の最中なので受け入れる",
         "層をまたいで同じ名前を探せない。横断は種別の欄で引く（論点13 の引き方が要る）"],
  decision=[
   ("決定", "<b>CodingSchema の種は、CodingSkills の雛形の単位（15）にする。</b>"
            "style ・ failure ・ concurrency ・ test-mechanism ・ toolchain ・ layers ・ dependency ・ "
            "test-boundaries ・ layer-mapping ・ test-placement ・ contract ・ lifecycle ・ acceptance ・ "
            "test-strategy ・ stack"),
   ("捨てるもの", "<b>いまの4種（architecture ・ coding-standard ・ tech-stack ・ test-standard）は捨てる。</b>"
                "もともと CodingSkills に合わせる想定で、いまの形はその前のものが残っていただけである"),
   ("層の扱い", "<b>層は、宣言された軸から導出する。</b>あらかじめ並べない ── 軸の組み合わせが現れた数だけ在る"),
   ("種別の扱い", "<b>種別（コーディング ・ テスト ・ アーキテクチャ ・ 技術スタック）は前置きの欄</b>にし、"
                "横断はその欄で引く（論点13）"),
   ("いつ変えるか", "<b>再定義の一部として、既存の coding 系 document 12本を作り直す</b>（論点11）"),
   ("次", "論点14（型を立てる基準）・論点15（Spec の種類）へ戻る"),
  ],
  weaknesses=["<b>雛形の節が、そのまま schema の欄になるかは確かめていない</b> ── "
              "雛形は人が写すもの、schema は機械が検証するもので、粒度が合わない箇所が出うる",
              "<b>芯の4欄が、そのまま schema の必須欄になるかは確かめていない</b> ── "
              "CodingSkills 側は文書の形、schema 側は検証の形で、粒度が合うとは限らない",
              "<b>芯を abstract に上げるか、concrete に置くかは決めていない</b> ── "
              "spec の規則にも同じ芯が要るなら abstract、Coding だけなら concrete である",
              "<b>用途の層と、spec の「外部との契約」が重なる</b>（論点15 の弱点と同じ）── どちらが正かを決めていない"])


t17 = Topic(17, "schema で持つもの", "ADR 以外を、そもそも schema で持つべきか", "新規",
  answer="推しは B（仕分けの問いに通ったものだけを型にする）",
  note="<b>「どう型にするか」の前に、「そもそも型にするか」がある。</b>"
       "いま schema を持っているものは、<b>再定義の対象そのもの</b>なので、"
       "在ることを根拠にできない ── <b>決まったことから問いを作り、当て直す。</b>",
  figures=[BUCKETS],
  tables=[Table("仕分けの問い（決まりから導く）",
    ["問い", "どの決まりから来るか"],
    {"① 軸を持つか": ["何に依存して変わるかを言えるか", "論点14（型は軸の集合）"],
     "② 意思決定を運ぶか": ["人の承認を要するものか", "論点7（承認は意思決定の境目でだけ）"],
     "③ 形のためだけでないか": ["出す先の成果物のために型になっていないか", "論点8（型は意味、形は射影）"],
     "④ 変更の記録が要るか": ["承認と承認のあいだの差分を見せる必要があるか", "論点9 ・ 12"],
     "⑤ 宣言する規則があるか": ["規則と引き方を、宣言として持つ必要があるか", "論点6 ・ 13"]},
    lead="<b>①と③は要件</b>（満たさなければ型にしない）。<b>②④⑤は、型として持つ値打ちを測る。</b>", plain=True),
   Table("いまの候補を、当ててみる（暫定）",
    ["① 軸", "② 意思決定", "③ 形のためだけでないか", "行き先の見立て"],
    {"spec": ["業務 ・ 接点 ・ 基盤", "承認①の対象", "○", "<b>文書</b>（決着済み）"],
     "coding": ["言語 ・ アーキ ・ 用途 ・ 実行環境", "承認②の材料", "○", "<b>文書</b>（決着済み）"],
     "ADR": ["決定が起きる場面（3種）", "<b>決定そのもの</b>", "○", "<b>文書</b>（決着済み）"],
     "knowledge": ["宣言する欄が定まる", "採否は人が決める（審査がある）", "○", "<b>文書</b>"],
     "skill": ["宣言する欄が定まる（目的 ・ 手順 ・ ガードレール）", "手順は「何が正しいか」である", "<b>○ ── SKILL.md は射影であって、型の理由ではない</b>", "<b>文書</b>"],
     "agent": ["同上", "同上", "○", "<b>文書</b>"],
     "hook": ["<b>1つではない</b>", "── 性質によって違う", "<b>中身によって変わる</b>", "<b>混ざっている ── 仕分けが要る</b>"],
     "template": ["<b>言えていない</b>", "承認を通る場面が無い", "中身は<b>判断の手順</b>だった", "<b>型にしない</b> ── 手順は skill と同じ格へ寄せる"]},
    lead="<b>ADR ・ knowledge ・ skill ・ agent は、いずれも宣言する欄が定まる。</b>"
         "SKILL.md や CLAUDE.md へ出るのは射影であって、型の理由ではない ── <b>手順そのものが「何が正しいか」である</b>。", plain=True),
   Table("hook 15本を開くと、4つの性質に分かれる",
    ["何をしているか", "行き先", "本数"],
    {"検査を走らせる": ["ずれを見つけて、見つかったときだけ返す", "<b>検査の宣言へ</b>（論点6 の名簿 ＋ いつ走らせるか）", "2"],
     "操作を止める": ["禁じられた書き込み・読みを、その場で拒む", "<b>規則へ</b> ── 述語で書ける「〜してはならない」＋ 実行の仕掛け", "6"],
     "気づかせる": ["飛ばした工程を、あとから見える形で残す", "<b>承認の材料へ</b>（未検の一覧と同じ場所）", "3"],
     "道具の都合": ["起動を1つにまとめる ・ 文脈を入れる", "<b>設定</b> ── 何が正しいかを述べていない", "3"]},
    lead="<b>「hook は型か設定か」と問うこと自体が誤りだった。</b>"
         "15本のうち<b>設定に当たるのは3本だけ</b>で、残りは既に決めた仕組みへ入る（1本は止めてある）。", plain=True)],
  kept=[
   Option("いま schema を持っているものは、そのまま持ち続ける",
          "spec ・ coding ・ ADR ・ knowledge ・ skill ・ agent ・ hook を、すべて型として置く",
          "<b>在ることを理由に在り続ける。</b>形のために型になっているものが混ざっていても、"
          "<b>誰も気づけない</b> ── 論点8 の決まりが効かない場所が残る"),
   Option("仕分けの問いに通ったものだけを型にする",
          "① 軸を持つか ③ 形のためだけでないか を要件とし、通らないものは"
          "<b>投影</b>（出し方だけを言う）か<b>設定</b>（道具の動かし方）へ移す",
          "<b>当てる作業が要る。</b>そして通らなかったものを移すと、"
          "<b>いま schema が与えている検証と描画を、別の形で用意することになる</b>"),
   Option("業務に関わるものだけを型にし、道具に関わるものは全部外す",
          "spec ・ coding ・ ADR ・ knowledge だけを schema で持ち、skill ・ agent ・ hook は道具側へ出す",
          "<b>skill と agent は、手順という「何が正しいか」を持っている</b> ── 見ずに外すと、それを失う"),
   Option("<b>型ごとに問わず、hook は中身で仕分ける</b>",
          "ADR ・ knowledge ・ skill ・ agent は文書として持つ。"
          "<b>hook だけは1つの型として扱わず</b>、検査は検査の宣言へ、禁止は規則へ、気づかせは承認の材料へ、"
          "残る道具の都合だけを設定として外す",
          "<b>hook という型が消える。</b>いま1か所に集まっているものが、3つの仕組みへ散る ── "
          "<b>「いつ走らせるか」を、それぞれの側で持つことになる</b>"),
  ],
  dropped=[
   ("いま schema を持っていることを、持ち続ける根拠にする",
    "<b>いまの schema は再定義の対象そのものである。</b>それを根拠にすると循環する ── "
    "この盤面で一度やって、取り下げた誤りである"),
  ],
  found=["<b>template は、名前と中身が食い違っていた</b> ── 中身は空の骨格ではなく<b>判断の骨格</b>"
         "（いつ使うか ・ 前提 ・ 診断のフロー）で、<b>skill が持つ手順と同じ格</b>である。"
         "そして<b>本当の意味の空の骨格は、schema から描ける</b>（<code>render-blank-template</code>）── "
         "どちらの読みでも、型として立てる理由が無い",
         "<b>「型にするか」の前に「schema で持つか」がある。</b>"
         "論点14 は型の切り方を決めるが、<b>そもそも持つかどうかは決めていない</b>",
         "<b>行き先は3つある</b> ── 文書（承認を通り、変更の記録を持ち、軸で引ける）／"
         "投影（出す先の形だけを言う）／設定（道具の動かし方）。"
         "<b>いま確かなのは spec ・ coding ・ ADR だけで、残る4つは当てていない</b>",
         "<b>skill と agent は、形のために型になっている疑いがある</b> ── "
         "SKILL.md や CLAUDE.md を出すことが型の理由なら、それは論点8 に触れる。"
         "<b>ただし手順そのものが「何が正しいか」なら、文書である</b> ── 開いて見るまで言えない"],
  path=[
   "論点14 で型の切り方を決めたので、残る型（ADR ・ knowledge ・ skill ・ agent ・ hook）へ当てようとした",
   "<b>skill と agent を「投影の疑い」と書いたが、これは誤りだった</b> ── "
   "SKILL.md へ出ることは射影であって、型の理由ではない。<b>手順そのものが「何が正しいか」を宣言している</b>",
   "当てる前に気づいた ── <b>これらが schema を持っていること自体を、まだ問うていない</b>",
   "しかも<b>いま schema を持っていることは根拠にならない</b>。それは再定義の対象そのもので、循環する（論点15 で一度やった誤り）",
   "そこで<b>決まりから問いを作った</b> ── 軸を持つか（論点14）、形のためだけでないか（論点8）を要件にし、意思決定・変更の記録・宣言する規則で値打ちを測る",
   "<b>hook を開いたら、15本に4つの性質が混ざっていた</b> ── 検査を走らせる2本、操作を止める6本、"
   "気づかせる3本、道具の都合3本（1本は止めてある）",
   "<b>「hook は型か設定か」と問うこと自体が誤りだった。</b>丸ごと1つに決めるのではなく、"
   "<b>中身ごとに、既に決めた仕組みへ入れる</b> ── 検査は検査の宣言へ、禁止は規則へ、気づかせは承認の材料へ",
   "残ったのは<b>道具の都合の3本だけ</b>で、そこが設定である",
  ],
  pick=("D", "<b>ADR ・ knowledge ・ skill ・ agent は、文書として schema で持つ</b>（いずれも宣言する欄が定まる）。"
             "<b>template は型にしない</b> ── 中身は判断の手順なので skill と同じ格へ寄せ、空の骨格は schema から描く。"
             "<b>hook は1つの型として扱わず、中身で仕分ける</b> ── "
             "検査は検査の宣言へ、禁止は規則へ、気づかせは承認の材料へ、残る道具の都合だけを設定として外す"),
  grounds=[
   ("軸を要件にすること", "型は軸の集合で決まる ── 軸が言えないなら、型として立たない", "決まり", "論点14 の基準"),
   ("形で型にしないこと", "出す先のために型を作ると、意味が1つなのに型が増える", "決まり", "論点8 の決まり"),
   ("在ることを根拠にしないこと", "いまの schema は再定義の対象そのものである", "決まり", "この盤面で取り下げた誤り（論点15 の立て直し）"),
   ("承認を測りに使うこと", "承認を通らないものを文書として置くと、承認の列に意思決定の無い文書が並ぶ", "決まり", "論点7 の決まり"),
   ("空の骨格を型にしないこと", "schema から空の骨格を描く能力が、すでに在る", "実測", "<code>waffle render-blank-template</code>（CLI に在ることを確認）"),
   ("hook を丸ごと決めないこと", "15本のうち、検査が2本 ・ 禁止が6本 ・ 気づかせが3本 ・ 道具の都合が3本で、性質が混ざっている", "実測", "<code>.waffle/documents/hooks/</code> の15本を開いて数えた（2026-09-06）"),
   ("検査の入口を1つに保つこと", "検査は宣言された名前でしか指せない ── hook が別系統で検査を呼ぶと、入口が2つになる", "決まり", "論点6 の決まり（名簿に無い名前を指せない）"),
  ],
  costs=["<b>hook という型が消える</b> ── いま1か所に集まっているものが、3つの仕組みへ散る",
         "<b>「いつ走らせるか」を、散った先のそれぞれで持つことになる</b>",
         "設定として外した3本に、検証と描画を別の形で用意することになる"],
  weaknesses=["<b>template の中身（判断の骨格6本）を、skill のどこへ寄せるかは決めていない</b> ── skill の中の1ブロックにするのか、skill の種を足すのか",
              "<b>「いつ走らせるか」をどこが持つかは決めていない</b> ── 検査の宣言に足すのか、別の仕組みにするのか",
              "<b>禁止を規則へ移すと、規則が「止める力」を持つことになる</b> ── "
              "いまの規則は判定だけで、止める仕掛けは持っていない。"
              "<b>止めることを abstract の動詞にすると、動詞が増える（論点5 に反する）</b> ── "
              "接点の側の振る舞いとして持つのが筋だが、当てていない",
              "<b>ADR ・ knowledge ・ skill ・ agent の軸は、まだ言えていない</b> ── 文書として持つことは決まったが、型の切り方（論点14）は当てていない"])


t18 = Topic(18, "道具と処理の置き場所", "Hook や Skill の中の処理を、どこに紐づけて作るか", "新規",
  answer="推しは B（能力は業務の層、差し出し方は業務 × 接点の層。script は呼ぶだけ）",
  note="<b>Hook も Skill も、中で script を使う。</b>その処理をどこに紐づけるか ── "
       "そして<b>Hook が何をきっかけに起きるかの仕様は、どこに書くのか</b>。"
       "論点15（軸は業務 ・ 接点 ・ 基盤）を Waffle 自身へ当てると、割れる。",
  figures=[TOOLS],
  path=[
   "Hook の script に処理を書くと、<b>その処理は Hook からしか使えない</b>と気づいた",
   "では処理はどこに属するのか ── <b>「ずれを検める」「骨格を作る」は Waffle ができることそのもの</b>で、"
   "接点を変えても変わらない。つまり<b>業務の層</b>である",
   "CLI ・ MCP ・ Hook は、その能力を<b>差し出す3つの接点</b>になる ── 論点15 の軸がそのまま当たる",
   "<b>すると「どの事象で起きるか」は Hook という接点の仕様である</b> ── "
   "能力の側には書けない（CLI には事象が無い）し、実装にだけ書くと承認を通らない",
   "だから<b>トリガーの仕様は 業務 × 接点 の層に書く</b> ── この能力を、Hook という接点でどう差し出すか",
   "<b>いまの hook が usecaseRef と scriptRef を分けて持っているのは、すでにこの形に近い</b>",
  ],
  tables=[Table("3つの接点は、何を宣言するか",
    ["宣言する中身", "どこに書くか"],
    {"CLI": ["命令の名前 ・ 引数 ・ 終了コード", "業務 × 接点の層"],
     "MCP": ["道具の名前 ・ 入力の形 ・ 返す形", "業務 × 接点の層"],
     "Hook": ["<b>どの事象で起きるか</b> ・ 止めるか通すか ・ 何を返すか", "<b>業務 × 接点の層</b>"],
     "（能力そのもの）": ["何を検め、何を作り、何を描くか", "<b>業務の層</b>"]},
    lead="<b>同じ能力を、3つの接点で差し出す。</b>接点ごとに違うのは差し出し方だけである。", plain=True),
   Table("この選択が置くもの",
    ["同じ処理を別の接点から呼べるか", "トリガーの仕様はどこに在るか", "承認を通るか"],
    {"A": ["<b>呼べない</b>（script の中にある）", "script のコメント", "<b>通らない</b>"],
     "B": ["<b>呼べる</b>（能力は1つ）", "<b>業務 × 接点の spec</b>", "<b>通る</b>"],
     "C": ["呼べる", "Hook ごとの独立した spec", "通る（ただし接点ごとに文書が増える）"]})],
  kept=[
   Option("script の中に処理を書く",
          "Hook の script が、検める処理そのものを持つ",
          "<b>Hook からしか使えない能力ができる。</b>同じことを CLI からも MCP からも呼べず、"
          "<b>その処理は承認も検証も通らない</b>"),
   Option("能力は業務の層、差し出し方は業務 × 接点の層。<b>script は呼ぶだけ</b>",
          "「ずれを検める」は能力として業務の層に置き、"
          "「どの事象で起きるか」は Hook という接点の宣言として業務 × 接点の層に置く。"
          "<b>script は事象を受けて、その能力を呼ぶだけにする</b>",
          "<b>接点を1つ足すたびに、業務 × 接点の宣言が1つ増える</b>。"
          "また<b>能力の側は事象を知らない</b>ので、事象に依る振る舞い（止める・通す）は接点の側で書き切ることになる"),
   Option("Hook ごとに、独立した spec を作る",
          "Hook 1本につき1つの spec を持ち、そこに処理も事象も書く",
          "<b>同じ能力の spec が、接点の数だけ増える</b> ── CLI 用・MCP 用・Hook 用が並び、"
          "<b>どれが正かを言う仕組みがまた要る</b>"),
  ],
  dropped=[
   ("Hook は spec を持たない（実装だけにする）",
    "<b>Hook は Waffle ができることの一部である。</b>できることを spec に持たないなら、"
    "何を約束しているのか誰も言えない ── 承認の境目でも見えない"),
   ("能力の側に、事象を持たせる",
    "CLI にも MCP にも事象は無い ── <b>能力が特定の接点の言葉を持つことになり、接点を差し替えられなくなる</b>"),
  ],
  found=["<b>CLI ・ MCP ・ Hook は、同じ能力を差し出す3つの接点である。</b>"
         "だから論点15 の軸がそのまま当たり、<b>トリガーの仕様は 業務 × 接点 の層に落ちる</b>",
         "<b>script が処理を持つと、そこでしか使えない能力ができる。</b>"
         "呼ぶだけにすれば、能力は1つのまま差し出し口だけが増える",
         "<b>いまの hook は、すでにこの形に近い</b> ── <code>usecaseRef</code>（呼ぶ先）と "
         "<code>scriptRef</code>（呼ぶ道具）を分けて持っている",
         "<b>接点が規約寄りに見えるのは、2つが混ざっているからである</b> ── "
         "<b>この能力を何という名前で差し出すか</b>は約束（spec）、"
         "<b>CLI というものをどう作るか</b>は作法（規約）。論点15 の線がそのまま効く"],
  pick=("B", "<b>能力は業務の層に置き、差し出し方（CLI ・ MCP ・ Hook）は業務 × 接点の層に置く。</b>"
             "<b>Hook が何をきっかけに起きるかは、そこに書く。</b>script は事象を受けて能力を呼ぶだけにする"),
  grounds=[
   ("接点として扱うこと", "CLI ・ MCP ・ Hook は、同じ能力を差し出す先が違うだけである", "決まり", "論点15 の決まり（接点は軸。UI ・ API ・ CLI ・ MCP は値）"),
   ("トリガーを接点の側に置くこと", "能力の側に事象を持たせると、接点を差し替えられなくなる", "決まり", "論点15 の決まり（層はどれも業務を含み、掛かることは掛けた層に書く）"),
   ("script を呼ぶだけにすること", "宣言の外に実体を置き、名前で結ぶ ── 実体が宣言を持たないと、検証も承認も通らない", "決まり", "論点6 の決まり（検査の実体は宣言の外、名前で指す）"),
   ("いまの形がすでに近いこと", "hook が usecaseRef と scriptRef を分けて持っている", "実測", "<code>.waffle/documents/hooks/</code> の15本（2026-09-06 に確認）"),
  ],
  extras=[("接点の話は、2つに割れる", """<p class="lead"><b>接点が規約寄りに見えるのは、2つが混ざって見えているからである。</b>
論点15 で引いた線 ──<b>業務を含むなら spec、含まないなら規約</b>── が、そのまま効く。</p>
<table><tr><th>同じ「CLI」でも</th><th>どちらか</th><th>置き場所</th></tr>
<tr><td>この能力を <code>waffle validate --path &lt;p&gt;</code> という名前と引数で差し出す</td>
<td><b>約束</b> ── 破れば利用者が壊れる</td><td><b>spec</b>（業務 × 接点）</td></tr>
<tr><td>この命令が終了コード 2 を返すのは、どんなときか</td><td><b>約束</b></td><td><b>spec</b>（業務 × 接点）</td></tr>
<tr><td>CLI の引数は、どう名づけるか</td><td><b>作法</b> ── どの命令でも同じ</td><td><b>規約</b>（用途＝CLI）</td></tr>
<tr><td>終了コードは何種類に限るか ・ 標準出力に何を出すか</td><td><b>作法</b></td><td><b>規約</b>（用途＝CLI）</td></tr>
<tr><td>Hook が Edit/Write の直後に起き、拒否はしない</td><td><b>約束</b></td><td><b>spec</b>（業務 × 接点）</td></tr>
<tr><td>Hook は 100ms 以内に応答を書き始める ・ 状態を残さない</td><td><b>作法</b></td><td><b>規約</b>（用途＝Hook）</td></tr></table>
<p class="lead"><b>逆に置くと、どちらも壊れる。</b>
固有の約束を規約へ置くと、<b>約束が変わっても spec の差分に出ない</b> ── 承認の境目で見えなくなる。
技術の作法を spec へ置くと、<b>同じことが 能力の数 × 接点の数 だけ複製される</b>。</p>
<h3>それぞれの欄（下書き）</h3>
<table><tr><th></th><th>持つ欄</th></tr>
<tr><td><b>spec（業務 × 接点）</b></td><td>対象の能力（印で指す）／接点の値（CLI ・ MCP ・ Hook ・ UI）／
差し出し方（名前 ・ 入力 ・ 返り、Hook なら事象）／<b>破れたとき、利用者に何が起きるか</b>／受け入れ基準</td></tr>
<tr><td><b>規約（用途＝その接点）</b></td><td>外部との契約の形 ／ 起動と終了 ／ 採用する依存 ／ 保証する振る舞い ── 
<b>CodingSkills の用途の層が、そのまま当たる</b></td></tr></table>
<p class="lead"><b>もとの PresentationSpec ・ PlatformSpec は、この形では型として立たない。</b>
画面も基盤も<b>軸の値</b>になり、spec の型は1つ（種は層の単位）になる ── 論点15 の決着そのものである。</p>""")],
  costs=["<b>接点を1つ足すたびに、業務 × 接点の宣言が1つ増える</b>",
         "<b>能力の側は事象を知らない</b> ── 事象に依る振る舞い（止める ・ 通す）は、接点の側で書き切ることになる"],
  weaknesses=["<b>止める（拒否する）ことを、どの層が持つかは決めていない</b> ── "
              "論点17 で「禁止は規則へ」と置いたが、規則は判定だけで止める力を持っていない",
              "<b>Skill の中の script も同じ形でよいかは、当てていない</b> ── "
              "Skill は人と AI が読む手順で、Hook のように事象で起きるものではない",
              "<b>接点ごとに振る舞いが違うとき（CLI は終了コード、Hook は止める）、能力の返り値をどう揃えるかは決めていない</b>"])


t19 = Topic(19, "Spec の種", "層ごとに、何を1つの文書の単位にするか", "新規",
  answer="推しは B（業務8〈過去の検討〉・ 業務×接点2 ・ 業務×基盤1）",
  note="<b>ここを粗く決めると、抽象と具体の境目がそこでずれる。</b>"
       "論点15 で層は決まったが、<b>その層で何を1つの文書にするか</b>は決めていない ── "
       "CodingSkills では、ここを15種まで切り下げてから形が定まった。"
       "<b>そして業務の層は、すでに過去に切ってあった</b> ── "
       "<code>docs/adr/tier2-domain.html</code>（未承認）が8種別を立てている。",
  figures=[ONEWAY, SIZE],
  path=[
   "層は3つ（業務 ／ 業務 × 接点 ／ 業務 × 基盤）と決まったが、<b>1つの層に何本置くかが決まっていない</b>",
   "CodingSkills で同じ場面を通っている ── <b>「層で切る」だけでは足りず、雛形の単位まで下ろして15種になった</b>",
   "そこで同じ問いを立てた ── <b>その層で決まることのうち、1つの文書として閉じるものは何か</b>",
   "業務の層は、原典（<code>ddd-advisor</code> の knowledge）から拾った ── "
   "領域 ・ 文脈 ・ モデル ・ 操作 ・ 連係の5つが、それぞれ別の単位で閉じる",
   "業務 × 接点は2つに割れた ── <b>差し出しの約束</b>（能力 × 接点の組ごと）と、"
   "<b>つながりの順序</b>（複数の差し出しを、どの順で通るか）",
   "業務 × 基盤は1つに見える ── <b>満たすべき要件</b>（指標と目標の組で書ける）",
  ],
  tables=[Table("文脈の地図で、Waffle は何をするか",
    ["文書として持つか", "誰が出すか"],
    {"連係の取り決め": ["<b>持つ</b>（1件＝2つの文脈のつなぎ方）", "人が決めて書く"],
     "文脈の地図": ["<b>持たない</b>", "<b>Waffle が描く</b>（取り決めを集めて1枚に）"],
     "同じ相手へ変換装置が集まっている": ["持たない", "<b>Waffle が数えて出す</b> ── 相手側のモデルに問題があるという合図"],
     "どこともつながっていない文脈がある": ["持たない", "<b>Waffle が数えて出す</b> ── 孤立が妥当かを問い直す材料"],
     "その合図が何を意味するか": ["── ", "<b>人が読む</b>（組織の課題として）"]},
    lead="<b>描くのも、読み取りの材料を出すのも Waffle の仕事である。</b>"
         "人に残るのは、その材料が何を意味するかの判断だけ ── 図に載せきれないことは、原典もそう扱っている。", plain=True),
   Table("業務の層 ── 過去の検討が立てた8種別",
    ["1本が何を言うか", "いま何本 → 変更後"],
    {"事業領域": ["業務領域を分類するときの基準になる、事業のまとまり", "0 → 1"],
     "業務領域": ["事業活動の1単位。中核 ・ 一般 ・ 補完の分類と、その根拠", "7 → 7"],
     "区切られた文脈": ["この文脈で言葉が一貫する範囲", "1 → 1"],
     "集約": ["一貫性の境界。1つのトランザクションで一緒に変わる範囲", "2 → 2"],
     "エンティティ": ["同一性を持ち、状態が変わるもの", "0 → 2"],
     "値オブジェクト": ["値で等しさが決まり、変わらないもの", "0 → 10"],
     "業務ユースケース": ["外から呼ばれる、業務としてひとまとまりの用事", "30 → 30"],
     "業務サービス": ["どの集約にも属さない、集約をまたぐ判断", "4 → 4"]},
    lead="<b>切る基準は「1本の文書として描かれるかどうか」</b> ── "
         "私がこの盤面で導いた基準（1つの文書として閉じるか）と同じものが、すでに使われていた。"
         "出どころは <code>docs/adr/tier2-domain.html</code>（未承認・bc-waffle を見たもの）。", plain=True),
   Table("Waffle は、対応をどう表すか ── 作りは1通りにする",
    ["作りの数", "① 1対1", "② 1領域に複数の文脈", "③ 1文脈に複数の領域", "しんどさ"],
    {"文脈を上に入れ子": ["1", "書ける", "<b>書けない</b>（領域が2つの文脈に現れる）", "書ける", "低いが、②で詰まる"],
     "領域を上に入れ子": ["1", "書ける", "書ける", "<b>書けない</b>", "低いが、③で詰まる ── 初期はたいてい③である"],
     "3つを場合分けする": ["<b>3</b>", "書ける", "書ける", "書ける", "<b>高い</b> ── 引き方も検査も3通りになる"],
     "<b>並べて、対応を宣言で持つ</b>": ["<b>1</b>", "書ける", "書ける", "書ける",
        "<b>低い</b> ── 構造に分岐が無い。対応は文書の欄で、引くのは述語1つ"]},
    lead="<b>「3つに対応する」は、3通りの作りを持つことではない。</b>"
         "並べて対応を宣言すれば、<b>作りは1通りのまま、結果として3つの形が表せる</b> ── "
         "入れ子にすると作りは1通りでも、<b>表せない形が出る</b>。", plain=True),
   Table("過去の検討を、どこまで採るか",
    ["採るか", "なぜ"],
    {"業務の層の8種別": ["<b>採る</b>", "切る基準が同じ（1本の文書として描かれるか）。中身も新しい決まりと噛み合う"],
     "文書への参照は鍵1つ、相手の値を写さない": ["<b>採る</b>", "論点3 の決まり（節に印。参照の鍵を名前から印へ）と同じ"],
     "索引を保存せず、取り出すときに組み立てる": ["<b>採る</b>", "論点13 ・ 論点11 の「導出する」と同じ"],
     "図の宣言を、主張を軸にした形で持つ": ["<b>採る</b>", "論点8 の決まり（型は意味、形は射影）と同じ思想"],
     "段2 に8つの型を立てる（domain ・ knowledge ・ skill ・ hook ・ coding ・ template ・ agent ・ ADR）":
       ["<b>採らない</b>", "<b>型は軸の集合で決まる</b>（論点14）。"
        "spec は1つの型で、種は層の単位になる（論点15）── 過去の「Domain の型」は、その業務の層に当たる"],
     "Presentation ・ Platform を別の型にする": ["<b>採らない</b>", "論点15 で軸の値へ移した。"
        "<b>過去の計画も Platform を「型そのものを考え直す」として範囲外にしていた</b> ── その保留への答えになる"],
     "knowledge ・ skill ・ hook ・ agent ・ template を型として持つ": ["<b>保留</b>", "論点17 で問い直している最中である"]},
    lead="<b>中身は採れるが、外側の枠は採れない。</b>過去の検討は、型の一覧を「いまある schema」から取っていた ── "
         "そこは論点14 ・ 15 ・ 17 で置き換わっている。", plain=True),
   Table("残る2層 ── ここは過去の検討に無い",
    ["単位", "なぜ、そこで切れるか"],
    {"業務 × 接点": ["<b>差し出しの約束</b>　／　<b>つながりの順序</b>",
                 "約束は 能力 × 接点 の組ごとに1つ。順序は複数の差し出しをまたぐので、どの約束にも属さない"],
     "業務 × 基盤": ["<b>満たすべき要件</b>", "指標と目標の組で書ける（SLI ・ SLO）── 1つの業務に1本で足りる"]},
    lead="過去の検討は Domain の型だけを見ており、接点と基盤は<b>別の型</b>（PresentationSpec ・ PlatformSpec）だった ── "
         "論点15 でそれらを軸へ移したので、この2層はここで新しく立てる。", plain=True),
   Table("この選択が置くもの",
    ["種の数", "1つの文書の大きさ", "抽象と具体の境目"],
    {"A　層をそのまま種にする": ["3", "<b>大きい</b> ── 領域も操作も1本に入る", "<b>ぼやける</b>（同じ文書に別の粒度が同居）"],
     "B　描かれる単位まで切る": ["<b>業務8 ＋ 接点2 ＋ 基盤1</b>", "1つの判断で閉じる", "<b>単位が判断の粒度と一致する</b>"],
     "C　集約の内側を畳む": ["業務6 ＋ …", "集約だけ大きい", "<b>参照が相手の値を写す</b>"]})],
  kept=[
   Option("層をそのまま種にする（3種）",
          "業務 ／ 業務 × 接点 ／ 業務 × 基盤 の3つを、そのまま文書の単位にする",
          "<b>1つの文書に、領域の話と操作の話が同居する。</b>"
          "粒度が違うものが同じ欄に並び、<b>抽象と具体の境目が文書の中でぼやける</b>"),
   Option("<b>1本の文書として描かれる単位まで切る</b>",
          "業務は<b>過去の検討が立てた8種別</b>（事業領域 ・ 業務領域 ・ 区切られた文脈 ・ 集約 ・ エンティティ ・ "
          "値オブジェクト ・ 業務ユースケース ・ 業務サービス）。"
          "業務 × 接点は2つ、業務 × 基盤は1つ",
          "<b>文書の数が増える</b>（bc-waffle で 44本 → 57本）。"
          "そして<b>どの単位に書くかの判断が、書くたびに1回増える</b>",
          before="業務は5つ（領域 ・ 文脈 ・ モデル ・ 操作 ・ 連係）",
          why="<b>過去に、同じ問いをより細かく通した検討があった</b>（<code>tier2-domain.html</code>）── "
              "私の5つは<b>事業領域を落とし、集約 ・ エンティティ ・ 値オブジェクトを「モデル」1つに畳んでいた</b>。"
              "畳むと、集約の内側から出てくる12本の置き場所が無くなる"),
   Option("集約の内側を、集約の文書の中だけに置く（畳む）",
          "エンティティと値オブジェクトを独立した文書にせず、集約の中にだけ書く",
          "<b>文書どうしの参照が、相手の値を写して持つことになる</b> ── "
          "過去の検討が、まさにこれを理由に2つを新設している"),
  ],
  dropped=[
   ("接点の値ごとに種を分ける（CLI 用 ・ MCP 用 ・ UI 用）",
    "接点は<b>軸の値</b>であって種ではない（論点15）── 値ごとに種を作ると、"
    "<b>値が増えるたびに種が増える</b>"),
   ("いまの DomainSpecSchema の種を、そのまま使う",
    "<b>それは再定義の対象そのものである。</b>結果として近い形になることはあっても、"
    "<b>根拠にはできない</b>（論点15 で取り下げた誤り）"),
  ],
  found=["<b>文脈の地図は、Waffle が描く。</b>持たないのは<b>文書として保存しないという意味</b>で、"
         "取り決めを集めて<b>描く側の成果物</b>になる ── 索引を保存せず取り出すときに組み立てるのと、同じ形である",
         "<b>読み取りまで出せる。</b>原典が挙げる観点は<b>線の集まり方</b>にあり、"
         "「1つのチームが関わるすべての相手にモデル変換装置を作っている」「どこともつながっていない文脈がある」は、"
         "<b>取り決めを数えれば分かる</b> ── <b>述語で書ける規則になり、承認の材料に出せる</b>（論点6 ・ 論点7）",
         "<b>切り方の違いは、好みではなく事情の違いである。</b>"
         "<b>上限は言葉で決まる</b> ── 同じ言葉が一貫するもっとも広い範囲で、"
         "それを超えると同じ語が2つの意味を持ちはじめる。"
         "<b>その内側をどこまで割るかが設計判断</b>で、原典が挙げる理由は3つだけである ── "
         "開発チームを増やす ／ 非機能要件で開発単位を分ける ／ スケールさせる機能を切り離す",
         "<b>だから「どちらが正しい切り方か」は決まらない</b> ── 問えるのは"
         "<b>上限を超えていないか</b>と<b>割る理由があるか</b>の2つだけである（大きさ自体は基準にならない）",
         "<b>対応の形は、どれか1つを選ぶものではない。</b>①②③ はどれも起こるので、"
         "<b>構造はどれが来ても壊れない形にする</b> ── これが、この図から出る唯一の設計判断である",
         "<b>いま何本あるかは、根拠にならない。</b>それは再定義の対象そのものである ── "
         "この盤面で3回、いまの姿や過去の数え方を根拠にして答えかけた",
         "<b>Waffle が提供する考え方は、1通りである</b> ── 領域と文脈を並べ、対応を宣言で持つ。"
         "<b>①②③ は、その宣言に現れる結果であって、作りの分岐ではない</b> ── "
         "だから対応の形が増えても、引き方も検査も増えない",
         "<b>業務の層は、すでに切ってあった。</b><code>docs/adr/tier2-domain.html</code>（未承認）が8種別を立てており、"
         "<b>切る基準も同じだった</b>（1本の文書として描かれるかどうか）── "
         "私はそれを知らずに5つへ畳み、<b>事業領域を落としていた</b>",
         "<b>エンティティと値オブジェクトを独立させる理由は、参照にある</b> ── "
         "集約の内側にしか置けないと、文書どうしの参照が<b>相手の値を写して持つ</b>ことになる",
         "<b>事業領域を立てる理由は、置き場所にある</b> ── "
         "業務領域を文脈の外へ出したとき、分類の基準だけが宙に浮く",
         "<b>業務領域と文脈は、入れ子ではない。</b>原典は両方向の例を挙げている ── "
         "<b>一つの業務領域でも課題が複数あれば、課題ごとに別のモデルを作るほうがよい</b>（1つの領域に複数の文脈）、"
         "<b>小さなシステムなら単一の文脈で開発できる</b>（1つの文脈に複数の領域）。"
         "どちらを外側にしても、もう一方が入らない場合が出る",
         "<b>領域は見つけるもの、文脈は決めるものである。</b>"
         "領域は事業方針によって決まる構造で、<b>文脈はプロジェクトの目的と制約に合わせて技術者が決定できる</b> ── "
         "だから<b>文脈がソフトウェアの設計境界だという読みは正しい</b>。ただしそれは「ある領域の中の境界」とは限らない",
         "<b>1つの文脈が複数の領域を含むとき、領域の境界は名前空間 ・ モジュール ・ パッケージとして現れる</b> ── "
         "<b>実装の側の話になる</b>（物理的には単一の境界、論理的には複数の境界）",
         "<b>「層で切る」だけでは足りない。</b>CodingSkills も同じ場面を通り、"
         "<b>雛形の単位まで下ろして初めて形が定まった</b> ── spec でも同じ段が要る",
         "<b>切る基準は「1つの文書として閉じるか」である。</b>"
         "閉じない単位を種にすると、中身が他の文書へこぼれる ── "
         "<b>一貫性の境界より小さく切れないのは、そのためである</b>",
         "<b>業務 × 接点が2つに割れた。</b>約束は 能力 × 接点 の組ごとだが、"
         "<b>順序は複数の約束をまたぐ</b> ── 画面の遷移や対話の流れが、どの約束にも属さない"],
  pick=("B", "<b>業務の層は、過去の検討が立てた8種別を採る</b>（事業領域 ・ 業務領域 ・ 区切られた文脈 ・ 集約 ・ "
             "エンティティ ・ 値オブジェクト ・ 業務ユースケース ・ 業務サービス）。"
             "<b>業務 × 接点は2つ、業務 × 基盤は1つ</b>を、この盤面で新しく立てる"),
  grounds=[
   ("閉じる単位で切ること", "閉じない単位を種にすると、中身が他の文書へこぼれる", "前提", "種は「その層で決まることの単位」である、という論点16 の置き方"),
   ("一貫性の境界より小さく切らないこと", "境界の内側は実体で持ち、外は識別子で指す ── 割ると守れない", "原典", "<code>ddd-advisor</code> の knowledge（一貫性の境界）"),
   ("地図を導出にすること", "選んだ結果はシステム全体の地図として1枚に載せ、組織上の課題を読み取る材料にする ── 載せるのは、集めた結果である", "原典", "<code>ddd-advisor</code> の knowledge（context-integration ── 文脈の地図）"),
   ("上限を言葉に置くこと", "同じ言葉の一貫性に注目すると、その言葉が通用するもっとも広い範囲を特定できる ── それ以上広げると一貫性が失われる", "原典", "<code>ddd-advisor</code> の knowledge（bounded-context ── 区切られた文脈の大きさ）"),
   ("割る理由を限ること", "大きな文脈を小さく切り分ける理由は、開発チームを増やす場合 ・ 非機能要件に対応するために開発単位を分ける場合 ・ スケールアウトが必要な機能を切り離す場合", "原典", "同上"),
   ("作りを1通りにすること", "入れ子にすると表せない形が出て、場合分けすると引き方も検査も3通りになる", "前提", "この論点の表（対応をどう表すか）"),
   ("領域と文脈を分けること", "一つの業務領域でも課題が複数あれば課題ごとに別のモデルを作るほうがよく、逆に小さなシステムなら単一の文脈で複数の領域を開発できる", "原典", "<code>ddd-advisor</code> の knowledge（bounded-context ── 業務領域と区切られた文脈の対応）"),
   ("文脈を設計の側に置くこと", "業務領域は事業方針によって決まる構造だが、区切られた文脈はプロジェクトの目的と制約条件に合わせて技術者が決定できる", "原典", "<code>ddd-advisor</code> の knowledge（bounded-context ── 区切られた文脈は設計）"),
   ("業務の5つを原典から取ること", "領域 ・ 文脈 ・ モデル ・ 言葉 ・ 連係は、それぞれ別の判断として扱われている", "原典", "<code>ddd-advisor</code> の knowledge（subdomain ・ bounded-context ・ domain-model ・ ubiquitous-language ・ context-integration）"),
   ("接点の値で種を作らないこと", "接点は軸の値であり、値が増えても種は増えない", "決まり", "論点15 の決まり"),
  ],
  costs=["<b>文書の数が増える</b> ── 1つの業務を書くのに、複数の文書へ分けて書くことになる",
         "<b>どの単位に書くかの判断が、書くたびに1回増える</b>"],
  weaknesses=["<b>領域と文脈の対応を、どちらの文書が持つかは決めていない</b> ── 領域の側に対応表を置くか、文脈の側が自分の領域を名乗るか",
              "<b>言葉（用語集）を、文脈の中に入れるか別の単位にするかは決めていない</b> ── "
              "文脈の定義そのものが言葉なので、いまは文脈に含めている",
              "<b>「つながりの順序」が、本当に接点をまたぐかは確かめていない</b> ── "
              "画面の遷移は UI の中の話かもしれない",
              "<b>8種それぞれの欄は、まだ書いていない</b> ── CodingSkills では雛形まで書いて形が定まった"])


t20 = Topic(20, "spec の置き方", "文書を、どういうフォルダの形で置くか", "新規",
  answer="推しは B（業務でフォルダを切り、その中に接点と基盤の層を置く）",
  note="<b>業務 × 接点の文書は、実際に作る。</b>数は「その文脈を、その接点で差し出す能力の数」だけである。"
       "<b>問いは、それをどこへ置くかである</b> ── 層でフォルダを切るか、業務でフォルダを切るか。"
       "<b>業務領域は、この入れ子には入らない</b> ── 文脈との対応が1対1とは限らないので、"
       "フォルダの階層にはできない（論点19）。",
  figures=[TREE],
  path=[
   "論点19 で種は決まったが、<b>それが disk の上でどう並ぶかを言っていなかった</b>",
   "CodingSkills は<b>層でフォルダを切っている</b>（<code>lang.rust+arch.hexagonal/</code>）── そのまま当てるのが素直に見えた",
   "しかし当ててみると、<b>同じ業務の文書が3つ4つのフォルダに散る</b> ── "
   "「予約という業務の全部」を見るのに、フォルダを横断することになる",
   "Coding と spec の違いに気づいた ── <b>Coding には主題が無い</b>（言語もアーキも用途も並列）。"
   "<b>spec には業務という主題がある</b>（論点15）",
   "だから<b>主題でフォルダを切り、その中で層を分ける</b>ほうが、決まりを構造へ写すことになる",
   "文脈をまたぐもの（領域 ・ 連係）だけは、どの業務にも属さないので<b>上に置く</b>",
  ],
  tables=[Table("固定されるもの／変わるもの",
    ["中身", "誰が決めるか"],
    {"固定される ── フォルダの骨組み": [
        "<code>business-domains/</code> ・ <code>subdomains/</code> ・ <code>contexts/</code>、"
        "文脈の中の <code>aggregates/</code> ・ <code>usecases/</code> ・ <code>interfaces/</code> ・ <code>platforms/</code>、"
        "そして <code>contexts/integrations/</code>", "<b>Waffle が決める</b>（型が決まれば決まる）"],
     "固定される ── 置き方の規則": [
        "領域は1本ずつ、文脈は1つずつ、種ごとに1ファイル", "<b>Waffle が決める</b>"],
     "変わる ── 数": ["文脈が何本か、領域が何本か", "<b>利用者が決める</b>（言葉の一貫性と、割る理由）"],
     "変わる ── 所属": ["どの集約 ・ ユースケースが、どの文脈に入るか", "<b>利用者が決める</b>"],
     "変わる ── 対応": ["どの領域が、どの文脈に現れるか", "<b>利用者が宣言する</b>（欄に書く）"]},
    lead="<b>骨組みは固定できる。変わるのは、中に何がいくつ入るかと、その所属である。</b>", plain=True),
   Table("この選択が置くもの",
    ["「この業務の全部」を見るとき", "「全部の CLI の約束」を見るとき", "決まりとの関係"],
    {"A　層でフォルダ": ["<b>フォルダを横断する</b>", "1つのフォルダで済む", "Coding と同じ形（主題が無い前提の形）"],
     "B　業務でフォルダ": ["<b>1つのフォルダで済む</b>", "横断する（<b>引き方で解ける</b> ── 論点13）", "<b>業務が主題、という決まりを構造が写す</b>"],
     "C　種でフォルダ": ["横断する", "横断する", "<b>層が見えなくなる</b>"]},
    lead="<b>どちらを選んでも、片方は横断になる。</b>違うのは、横断を人がするか、引き方がするかである。", plain=True)],
  kept=[
   Option("層でフォルダを切る（Coding と同じ形）",
          "<code>domain.予約/</code> ・ <code>domain.予約+interface.cli/</code> のように、層ごとに置く",
          "<b>同じ業務の文書が、フォルダをまたいで散る。</b>"
          "「この業務の全部」を見るのに、人がフォルダを横断することになる"),
   Option("<b>業務でフォルダを切り、その中に層を置く</b>",
          "<code>contexts/予約/</code> の下に <code>context.json</code> と "
          "<code>models/</code> ・ <code>operations/</code> ・ <code>interfaces/</code> ・ <code>platforms/</code> を置く。"
          "<b>フォルダは「何を入れるか」を名乗り、実体の名前はその中に置く</b>。"
          "<b>連係の取り決めは文脈の間にあるので <code>contexts/integrations/</code> に置く</b>"
          "（1件＝2つの文脈のつなぎ方。<b>文脈の地図は保存せず、集めて描く</b>）。"
          "事業領域と業務領域は文脈の外に並べる（<code>business-domains/</code> ・ <code>subdomains/</code>）",
          "<b>「全部の CLI の約束」を見るときに横断する</b> ── "
          "ただしそれは<b>引き方で解ける</b>（種と接点で引く。論点13）"),
   Option("種でフォルダを切る",
          "<code>operations/</code> ・ <code>models/</code> のように、種ごとに集める",
          "<b>層が見えなくなる。</b>どの文書がどの層に属するかが、名前からしか分からない"),
  ],
  dropped=[
   ("接点ごとにフォルダを分け、業務を中に置く",
    "<b>接点が主題になってしまう。</b>業務が主題である（論点15）以上、逆立ちしている"),
  ],
  found=["<b>Coding と spec で、正しい形が違う。</b>Coding には主題が無く（言語もアーキも用途も並列）、"
         "spec には業務という主題がある ── <b>だから同じ「層で切る」でも、フォルダの切り方は変わる</b>",
         "<b>どちらを選んでも、片方は横断になる。</b>違うのは<b>横断を人がするか、引き方がするか</b>である ── "
         "人が読む単位は業務なので、人の側を1か所にまとめ、横断は引き方に任せる",
         "<b>置くのは連係の取り決めであって、文脈の地図ではない</b> ── "
         "取り決めは1件ずつ文書になり（2つの文脈の間にあるので <code>contexts/</code> の下）、"
         "<b>地図はそれを集めて描く</b>（保存しない）。"
         "<b>事業領域と業務領域だけが文脈の外に出る</b> ── 対応が1対1とは限らないからである",
         "<b>過去の検討が、業務の層をすでに8種別へ切っていた</b>（<code>tier2-domain.html</code>）── "
         "文脈の中の並びは、その8種別がそのまま来る",
         "<b>フォルダの名前は、実体の名前ではなく「何を入れるか」である</b> ── "
         "<code>予約/</code> をいきなり置くと、そこに何が入るのかを名前が言わない。"
         "<code>contexts/予約/</code> のように、<b>種類の名前の中に実体の名前を置く</b>"],
  pick=("B", "<b>業務でフォルダを切り、その中に接点と基盤の層を置く。</b>"
             "文脈をまたぐもの（領域 ・ 連係）だけを上に置く ── "
             "横断して見たいときは、種と接点で引く（論点13）"),
  grounds=[
   ("主題でまとめること", "業務は主題であり、接点と基盤は業務に掛かる", "決まり", "論点15 の決まり"),
   ("人の読む単位に合わせること", "人が読む単位は文書であり、AI が引く単位は節である", "決まり", "論点2 の決まり（単位は読み手に従属する）"),
   ("横断を引き方に任せること", "種と接点で引けるので、フォルダで集める必要が無い", "決まり", "論点13 の決まり（意味の述語で引く）"),
   ("Coding と分けて考えること", "Coding の軸は並列で、主題が無い", "決まり", "論点16 の決まり（層は軸から導出）"),
   ("種類の名前を先に置くこと", "フォルダの名前が実体だと、そこに何が入るのかを名前が言わない", "前提", "フォルダは中身の種類を名乗るもの、という置き方"),
  ],
  extras=[
   ("形が変わるとき、何が起きるか", """<p class="lead"><b>同時に別の形が並ぶことは、問題にならない。</b>
対応は文書ごとの宣言なので、<b>ある文脈は2つの領域をまたぎ、別の文脈は1対1</b>、が同じリポジトリで成り立つ。</p>
<p class="lead"><b>問題になるのは、形が変わるときである</b> ── 1つだった文脈を2つに割ると、
その中の集約やユースケースが<b>どちらの文脈に属するかを決め直す</b>ことになる。</p>
<table><tr><th></th><th>文脈を割ったときに起きること</th></tr>
<tr><td><b>並べる形</b></td><td>文書を<b>移し、対応の宣言を書き換える</b>。
フォルダの規則は変わらない（領域は1本ずつ、文脈は1つずつ）</td></tr>
<tr><td>入れ子の形</td><td><b>階層ごと組み替える。</b>領域の下に文脈があるなら、
割れた文脈の片方は<b>別の領域の下へ移る</b> ── 置き場所の規則そのものが動く</td></tr></table>
<p class="lead"><b>そして参照は壊れない</b> ── 文書どうしは<b>印で指している</b>（論点3）。
名前やパスで指していたら、移した瞬間に全部が切れる。
<b>移動が「移動」で済むのは、印で指しているからである。</b></p>
<p class="lead"><b>残るのは、割った結果として中身を分ける作業である。</b>
これは言葉が割れたことの帰結なので、どんな置き方を選んでも避けられない ──
<b>置き方で変えられるのは、それに加えて構造の組み替えまで要るかどうかだけ</b>である。</p>"""),
   ("フォルダは、schema の中に書かない", """<p class="lead"><b>置き方は、schema が宣言することではない。</b>
どの節がどの型かは schema が決めるが、<b>その文書がどこに置かれるかは、人が読むための投影である</b>（論点8）── 
schema に置き場所を書くと、<b>構造の契約に投影が混ざる</b>。</p>
<table><tr><th>何が</th><th>どこに属するか</th></tr>
<tr><td>軸 ・ 種 ・ 欄</td><td><b>concrete schema</b>（構造の契約）</td></tr>
<tr><td>この文書はどのフォルダに置くか</td><td><b>schema の外</b> ── 投影の規則（道具の設定）</td></tr>
<tr><td>置き場所が規則どおりか</td><td><b>検査</b>（名前で指す ── 論点6）</td></tr></table>
<p class="lead"><b>CodingSkills も同じ形を通っている</b> ── ディレクトリ構成は
「アーキテクチャの層」ではなく<b>「言語 × アーキテクチャの層」が決める</b>と切り分けた。
<b>置き場所は、いつも一段具体の側にある。</b></p>"""),],
  costs=["<b>「全部の CLI の約束」を見るときは横断する</b> ── 引き方に頼ることになる",
         "業務フォルダの中に層のフォルダが入るので、<b>深さが1段増える</b>"],
  weaknesses=["<b>文脈を割ったとき、中の文書をどう振り分けるかは決めていない</b> ── 移す作業そのものは避けられないが、道具が助けられる範囲は決めていない",
              "<b>事業領域と業務領域を、同じ階層に並べるかは決めていない</b> ── 業務領域は「属する事業領域」を持つので、入れ子にもできる",
              "<b>領域と文脈の対応を、どちらが持つかは決めていない</b> ── 領域が扱う文脈を並べるのか、文脈が属する領域を名乗るのか。多対多なら、対応そのものが1本になることもある",
              "<b>1つの能力が複数の文脈にまたがるとき、どこへ置くかは決めていない</b>",
              "<b>接点の値が増えたとき、フォルダが横に増える</b> ── 深さは変わらないが、業務ごとに繰り返される",
              "<b>この形を、いまの documents へ当てていない</b> ── 当てると、置き直しが出る",
              "<b>投影の規則をどこに書くかは決めていない</b> ── schema の外だとは言えたが、置き場所そのものは決めていない"])


html = deck("schema 観の三層", [t1, t2, t5, t3, t6, t8, t7, t9, t10, t11, t12, t13, t14, t15, t16, t17, t18, t19, t20],
  intro="<b>論点20のうち14が決着し、6つが開いている（2026-09-06）。</b>後から立った3つは、決まりを spec へ落とす手前で当たったものである ── <b>段2 に何を置くかを、まだ決めていなかった。</b>後から立った4つは、決まった8つを spec へ落とすときに必ず当たるものである ── 書き方の指示 ・ schema の版と移行 ・ 決定と変更の記録 ・ 引く仕様。決着した論点も同じ1枚に置く ── "
        "後の論点が前の決着を前提にしているためである（論点5 は論点6 に、論点6 は論点3 に依存する）。"
        "論点8 は、abstract schema が持つ「お作法」の中身を詰める過程で新しく立った。"
        "<b>これは正本ではない。ブレストの記録である。</b>",
  extras=[overview_extra, ("まとめ", """<table><tr><th>#</th><th>決まり</th></tr><tr><td>1</td><td>schema は <b>abstract ／ concrete ／ 文書エンティティ</b>の三層で捉える</td></tr><tr><td>2</td><td><b>単位は読み手に従属する。</b>人が読む検証・状態・承認は文書単位、AI が行う取得・参照は節単位</td></tr><tr><td>5</td><td><b>型は横に増える。</b>動詞は abstract が固定する</td></tr><tr><td>3</td><td><b>節に、変わらない印を持たせる。</b>参照の鍵を名前から印へ移す</td></tr><tr><td>6</td><td>規則は<b>述語 ＋ 検めることの名前</b>を持つ。名簿は2段 ── 共通の検査は abstract、型固有は concrete</td></tr><tr><td>8</td><td><b>型は意味、形は射影。</b>abstract が既定を持ち、人が書くのは差分だけ。組が持つのは並べ方だけ</td></tr><tr><td>9</td><td>文書は<b>承認済みの値といまの値の2状態</b>だけを持ち、承認したら畳む。履歴は積まない</td></tr><tr><td>7</td><td>承認は<b>意思決定の境目</b>（ブレスト→spec、spec→実装）でだけ行う。間は検査が守る</td></tr><tr><td>10</td><td>書き方の指示は <b><code>x-</code> に構造で保存し、渡すときに <code>description</code> へ合成</b>する。鍵は schema の欄名</td></tr><tr><td>11</td><td><b>schema の版は Waffle の版に従う。</b>版の変更は破壊的だと認め、静かに壊さず、まとめて上げる</td></tr><tr><td>12</td><td><b>変更の記録は abstract の共通契約、ADR は concrete の型</b>（汎用 ／ ブレスト→spec ／ spec→実装 の3種）</td></tr><tr><td>13</td><td>引くのは<b>意味の述語</b>。型ごとの引き方は concrete が宣言し、<b>どの引き方でも読み方を添える</b></td></tr></table><h3>次にすること</h3><ul class="found"><li>この12を、<b>段1（abstract schema）と段2（concrete schema）の spec へ落とす</b></li><li>CodingSkills を、この形（型・射影・検査の宣言）へ寄せる ── いまは Waffle を前提にしない形で置いてある</li><li><b>承認の材料（差分と、未検の一覧）を作る仕組み</b>を決める ── これが無いと論点7 の②が全文読みに戻る</li></ul><h3>保留 ── 決めていないと分かっていること</h3><ul class="found"><li>どの検査が「全ての型に共通」か（論点6）</li>
<li>合成した指示が実際に効くか ── 4通りを測って決める（論点10）</li>
<li>宣言した移行が、実際に当たったかを確かめる方法（論点11）</li>
<li>どの変更が ADR に値するかの線（論点12）</li>
<li>式がどこまで「1つの節に解決する」と言えるか（論点13）</li><li>宣言された名前に実体が在るかを、いつ確かめるか（論点6）</li><li>既定の射影が無難な形に落ち着いたときに、誰が気づくか（論点8）</li><li>承認した契約が、どこまで下流を担保するか（論点7）</li><li>理由が空のまま畳まれたときの扱い（論点9）</li></ul>""")])
write(html, "docs/adr/brainstorm-schema-conception-board.html", "schema 観の三層")
print("ok")
