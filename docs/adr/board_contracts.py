import sys
sys.path.insert(0, '/home/daidaiiro/workspace/waffle/.claude/skills/brainstorm/references')
from build_board import Topic, Option, Table, deck, write, _table
from contractfig import CONTRACT
from algofig import GUARD, READ
from srcfig import FALL_B, ONE_B
from rulefig import SHAPES, SPREAD, VERIFY, ROLES

KNOW = ".waffle/documents/knowledge/knowledge-cand-avoidable-friction-is-not-detection"

t13 = Topic(13, "検査の対象と根拠", "check.py は何を確かめるのか", "決着",
  answer="<b>C ── 対象と根拠を、両方決める</b>",
  note="<b>問いが2つに割れた。</b>「何を対象にするか」と「検査の根拠をどこに置くか」は別の問いで、"
       "片方だけ決めても、もう片方が空くと同じ腐り方をする ── "
       "対象だけ決めれば根拠の無い検査が入り、根拠だけ決めれば原典にまで契約が伸びる。",
  figures=[CONTRACT],
  kept=[
    Option("対象だけを決める",
           "保守するもの（constraints ／ references ／ templates）に限る。検査の中身はその都度決める",
           "根拠が空くので、宣言の無い検査がまた入る"),
    Option("根拠だけを決める",
           "検査は、宣言との照合だけで真偽が決まるものに限る。対象は限らない",
           "原典にも契約を課しうる ── 変えないものに契約を付けると SSOT が壊れる"),
    Option("対象と根拠を、両方決める",
           "対象は保守するものに限り、内容は宣言との照合で真偽が決まるものに限る",
           "宣言の無い性質は、検査の前に宣言を足す工程が要る"),
  ],
  tables=[
    Table("いま在る歪み3つが、どうなるか",
          ["宣言の無い検査（ID の重なり）", "自分で作った写しを守る検査", "機械で裁けない宣言"],
          {"A": ["残る", "残る", "残る"],
           "B": ["宣言を先に足す", "写しを消す", "分けて書く"],
           "C": ["宣言を先に足す", "写しを消す", "分けて書く"]}),
    Table("対象の扱いが、どう決まるか",
          ["scripts", "sources", "SKILL.md ／ README.md"],
          {"A": ["テストが見る", "対象外", "<b>決まらない</b>"],
           "B": ["<b>check.py が見うる</b>", "<b>契約を課しうる</b>", "見うる"],
           "C": ["テストが見る", "対象外", "保守するので対象"]}),
  ],
  dropped=[
    ("壊れると困るものを、全部検査する",
     "何が困るかはその都度なので、判定が人へ戻る ── 規約が腐る経路そのものである"),
    ("scripts も check.py が見る",
     "振る舞いを持つものは、文言の照合では裁けない。テストの仕事である"),
    ("sources にも契約を課す（版が変わっていないか等）",
     "版の追随は constraints 側の出典行の契約である。原典に契約を課すと、原典が保守対象になる"),
  ],
  found=[
    "<b>問いが2つに割れた</b> ── 「何を対象にするか」と「検査の根拠をどこに置くか」。"
    "はじめは1つの問いのつもりで案を作っていた",
    "<b>いまの検査10件のうち、2件が宣言から導かれていない</b> ── "
    "ID の重なり（規約のどこにも一意だと書かれていない）と、"
    "<code>KINDS_BY_SHAPE</code> の写しのずれ（自分で写しを作ってから、その写しを守っている）",
    "<b>雛形と実物で、出典の表の1列目がずれている</b>（雛形は「対象」、実物は「ID」）。"
    "いまの検査は節の見出ししか比べないので、これが見えない",
    "<b>検査する側も同じ穴を持っていた</b> ── <code>figcheck.py</code> は図がタプルでないと"
    "1件も検査せず、それでも「食い違いは無い」と出す。今回この盤面の図で実際に素通りした",
  ],
  pick=("C",
    "<b>対象と根拠を、両方決める。</b>"
    "対象は「保守し続けるもの」に限り、検査の内容は「宣言との照合だけで真偽が決まるもの」に限る。"
    "検査を足すときは、<b>どの文書のどの宣言を守るのかを先に書く</b> ── "
    "宣言が無いなら、検査ではなく宣言を先に足す。"),
  grounds=[
    ("対象を保守するものに限ること", "原典は SSOT なので保守しない。不要になれば消すだけである",
     "決まり", "利用者の判断（2026-09-06）"),
    ("検査の内容を宣言から導くこと", "出典を持たない言明は規則ではない",
     "決まり", "SKILL.md ガードレール ／ references/sources.md"),
    ("宣言の無い検査が実在すること", "「一意」「重複」で全文検索して0件。ID の重なりの検査だけが先に在る",
     "実測", "constraints/ ・ references/ ・ SKILL.md の全文検索（2026-09-06）"),
    ("独自の解釈が原典と衝突しうること",
     "RH-TP-03「偽物を adapter へ置かない」は、Cockburn の "
     "<code>an adapter to a &quot;mock&quot; database</code> と逆だった",
     "原典", "alistair.cockburn.us/hexagonal-architecture/（2026-09-06 取得）"),
    ("その場しのぎの対応を退けること",
     "エラーや不備を塞ぐ対応は、規約と knowledge から導く。"
     "その場で思いついた対応は、困りごとを消すだけで原因を残す",
     "決まり", KNOW),
  ],
  costs=[
    "宣言の無い性質は、検査より先に<b>宣言を足す工程</b>が要る。1件目は「同じ層で ID は一意である」",
    "<code>KINDS_BY_SHAPE</code> の写しを消し、<code>file-catalog.md</code> を直接読むように書き換える",
    "機械で裁けない宣言（同じ層の2本が反対のことを言う等）は、<b>裁けないと明記して人へ渡す</b>",
    "<code>scripts/</code> のテストを書く ── <b>いま1本も無い</b>",
  ],
  weaknesses=[
    "<b>SKILL.md と README.md の契約を、まだ決めていない</b>。保守対象だが、何を確かめるかが空いている",
    "<b>references に書いた「数」をどう扱うかが未決。</b>手で書いていて、今日だけで2回書き直した ── "
    "索引と同じく生成物にできるはずだが、決めていない",
    "<b>sources に、参照されない原文が溜まる。</b>101本のうち出典が指すのは47本で、404 の頁も混じっている。"
    "削除の判断を誰がいつ行うかは決めていない",
  ],
  decision=[
    ("決定", "対象は<b>保守し続けるもの</b>（constraints ／ references ／ templates）に限り、"
             "検査の内容は<b>宣言との照合だけで真偽が決まるもの</b>に限る"),
    ("理由", "対象だけを決めると根拠が空いて宣言の無い検査が入り、"
             "根拠だけを決めると原典にまで契約が伸びて SSOT が壊れる。片方では止まらない"),
    ("効くこと", "検査を足すには、先に宣言を書くことになる ── "
                 "<code>scripts/</code> はテスト、<code>sources/</code> は対象外と決まる"),
  ])


t14 = Topic(14, "検査の担保", "検査が宣言から導かれていることを、何が担保するのか", "決着",
  answer="<b>B ── 検査が、守っている宣言の文字列を持つ</b>",
  note="<b>決めるより先に実装していた。</b>盤面は「新規」のまま、<code>check.py</code> は既に <code>Check</code> が <code>declared_in</code> と <code>needle</code> を必須で持ち、<code>verify_contracts()</code> が検査より先に走る形になっている。決着として記録し直す。論点13 で「検査は宣言から導く」と決めたが、<b>決めただけでは守られない。</b>"
       "いまも <code>check.py</code> のコメントには意図が書いてあるが、"
       "宣言に辿れない検査が2件残っていた。",
  figures=[GUARD],
  kept=[
    Option("コメントで宣言を指す",
           "検査の隣に「この宣言を守る」と書く。いまの形",
           "指したつもりで済む。宣言が消えても誰も気づかない"),
    Option("検査が、宣言の文字列を持つ",
           "<code>Check</code> に <code>declared_in</code> と <code>needle</code> を必須で持たせ、"
           "検査を1つも走らせる前に、その文字列が実在するかを確かめる",
           "中身の無い宣言を書けば通せる ── 機械が裁けるのはここまでで、その先は人が読む"),
    Option("宣言の側にも目印を付ける",
           "上に加えて、参照文書の宣言へ目印を付け、<b>検査の無い宣言</b>も検出する",
           "人が読む文書に機械向けの目印が入る。宣言を書くたびに、その付け方を覚えることになる"),
  ],
  tables=[
    Table("この選択が置くもの",
          ["宣言の無い検査を足せるか", "宣言を消したとき", "検査の漏れ"],
          {"A": ["<b>足せる</b>", "気づかない", "分からない"],
           "B": ["足せない（<code>needle</code> が書けない）", "<b>その検査が落ちる</b>", "分からない"],
           "C": ["足せない", "その検査が落ちる", "<b>検出できる</b>"]}),
  ],
  dropped=[
    ("検査の一覧を、別のファイルに持つ",
     "一覧と実装がずれる。<code>KINDS_BY_SHAPE</code> の写しで今日まさに起きた形である"),
    ("宣言の文をコードへ書き写す",
     "写した側が正になる。要約されたものを出典と認めない、という定めに反する"),
  ],
  found=[
    "<b>これは新しい仕組みではない。</b>規則に出典を持たせる形を、検査へ当てているだけである ── "
    "<code>declared_in</code> が原典、<code>needle</code> が照合する文字列に当たる",
    "<b>いまの「ID の重なり」の検査は、この形では登録できない</b> ── "
    "<code>needle</code> に書ける宣言が存在しないため。"
    "宣言を先に足すことが、意図ではなく構造として強制される",
  ],
  pick=("B",
    "<b>検査が、守っている宣言の文字列を持つ。</b>"
    "<code>check.py</code> は起動時に全 <code>Check</code> の <code>needle</code> が "
    "<code>declared_in</code> に実在するかを確かめ、無ければそこで落ちる。"
    "C の「宣言側の目印」は、検査の漏れが実際に問題になってから足す ── "
    "いまは漏れが起きた実例が無い。"),
  grounds=[
    ("検査に出典を持たせること", "出典を持たない言明は規則ではない。同じ形を検査へ当てる",
     "決まり", "SKILL.md ガードレール ／ references/sources.md"),
    ("コメントでは足りないこと",
     "いまの検査10件のうち2件が宣言に辿れない。コメントには意図が書いてあった",
     "実測", "scripts/check.py の全検査の突き合わせ（2026-09-06）"),
    ("写しを持たないこと", "写しを作ると、写しがずれる。実際に file-catalog.md の写しを守る検査が在る",
     "実測", "scripts/_common.py の KINDS_BY_SHAPE（2026-09-06）"),
    ("C を今は採らないこと",
     "その場で思いついた対応ではなく、実例から導く。検査の漏れが起きた実例をまだ持っていない",
     "決まり", ".waffle knowledge ／ 実例2件以上で抽象化する"),
  ],
  costs=[
    "<b>「同じ層で ID は一意である」を、先に宣言として書く。</b>書くまで、その検査は登録できない",
    "<code>KINDS_BY_SHAPE</code> の写しを消し、<code>file-catalog.md</code> を直接読む",
    "機械で裁けない宣言（同じ層の2本が反対のことを言う等）は、<b>裁けないと明記して人へ渡す</b>",
  ],
  weaknesses=[
    "<b>中身の無い宣言を書けば通る。</b>「規約はきれいであること」のような宣言を足せば、"
    "<code>needle</code> は実在してしまう ── そこは人が読んで止めるしかない",
    "<b>宣言の側からの漏れは見えない。</b>宣言は在るのに検査が無い、という状態を検出できない（C を採らないため）",
  ],
  decision=[
    ("決定",
     "<code>Check</code> は <code>declared_in</code> と <code>needle</code> を必須で持ち、<code>check.py</code> は検査を1つも走らせる前にその文字列が実在するかを確かめる"),
    ("理由",
     "コメントで宣言を指す形では、指したつもりで済む。実際に宣言へ辿れない検査が2件残っていた"),
    ("いまの状態",
     "<b>実装済み。</b>検査14件すべてが宣言を持ち、<code>python3 scripts/check.py --contracts</code> で一覧できる。機械では裁けないもの3件も、そこに並べてある"),
  ])


t15 = Topic(15, "読み取り", "規約の文書を、どう読むか", "決着",
  answer="<b>B ── 列名で読む1つの層を置く</b>",
  note="<b>これも決めるより先に実装していた。</b><code>_common.py</code> が表をヘッダ行の列名で読む層になっており、各検査は正規表現を持たない。決着として記録し直す。論点14 の <code>Check</code> は「何を確かめるか」を持つが、"
       "<b>どう読むか</b>は別に決まる。いまは検査ごとに正規表現を書いていて、"
       "<b>今日それが2か所で静かに壊れていた</b>（出典の照合が1行も走らず、"
       "<code>JSON</code> を規則と数えていた）。",
  figures=[READ],
  kept=[
    Option("いまのまま、行を正規表現で読む",
           "検査ごとに必要なパターンを書き、壊れたら直す",
           "壊れ方が静かで、通ってしまう ── 落ちないので誰も気づけない"),
    Option("表を列名で読む層を、1つ置く",
           "<code>tables()</code> がヘッダ行から列名を取り、行を辞書にする。"
           "検査は「<code>ID</code> 列」「<code>照合する文字列</code> 列」で書く",
           "表のヘッダが正になる。ヘッダを変えると検査が落ちる ── "
           "落ちること自体は望ましいが、書き手には手間になる"),
    Option("規約を、構造化データで持つ",
           "Markdown をやめ、前置きか JSON に規則と出典を持たせる",
           "<b>人が読み書きする形が失われる。</b>この Skill は人が読む文書として置いてある"),
  ],
  tables=[
    Table("今日壊れていた3か所が、どうなるか",
          ["出典の URL を <code>&lt;small&gt;</code> で探していた",
           "<code>JSON</code> を規則と数えた",
           "バッククォートの入れ子で needle が壊れた"],
          {"A": ["直したが、また壊れうる", "ハイフンの数で弾いた ── 形が変われば戻る", "直していない"],
           "B": ["<b>列で取るので起きない</b>", "<b>ID 列に無ければ規則でない</b>", "<b>列で取るので起きない</b>"],
           "C": ["起きない", "起きない", "起きない"]}),
  ],
  dropped=[
    ("Markdown のパーサを外から入れる",
     "<code>README.md</code> が「ディレクトリ1つで完結し、外部の依存も要らない」と宣言している"),
    ("正規表現を1か所へ集める",
     "集めても、位置で読むことは変わらない。列が増えれば同じように壊れる"),
  ],
  found=[
    "<b>壊れ方が「落ちない」ことが問題だった。</b>3か所とも、検査は通ったうえで何も見ていなかった ── "
    "<code>figcheck.py</code> も同じで、図がタプルでないと1件も見ずに「食い違いは無い」と出す",
    "<b>ID の形に依存しなくなる。</b>いまはハイフン2つを数えているので、"
    "<code>N14</code> のような ID を足すと黙って規則から外れる",
  ],
  pick=("B",
    "<b>表を列名で読む層を、1つだけ置く。</b>"
    "検査は位置ではなく列名で書く。C は、人が読み書きする形を捨てることになるので採らない ── "
    "この Skill は<b>人が読む文書として置く</b>と決めてある。"),
  grounds=[
    ("外部の依存を入れないこと", "ディレクトリ1つで完結し、Waffle も外部の依存も要らない",
     "決まり", ".waffle/skills/coding-skills/README.md"),
    ("位置で読むのをやめること",
     "今日3か所が静かに壊れていた ── 出典の照合が0行、JSON を規則と誤検出、needle の入れ子",
     "実測", "scripts/_common.py の source_rows ・ rule_ids（2026-09-06）"),
    ("表にヘッダが揃っていること",
     "規則の表・保証する振る舞いの表・出典の表は、いずれも1行目に列名を持つ",
     "実測", "constraints/ 全37本のヘッダ集計（2026-09-06）"),
  ],
  costs=[
    "表のヘッダが正になる。<b>雛形と実物のヘッダのずれが、その場で落ちる</b>（いま11本ずれている）",
    "<code>local_source</code> の総当たり（4通りのファイル名）も、命名を1つに決めて畳む",
  ],
  weaknesses=[
    "<b>表になっていない宣言は、この層では読めない。</b>散文で書かれた定めは対象外のままである",
    "<b>ヘッダの表記ゆれに弱い。</b>全角空白や表記の違いで列名が一致しなくなる ── 正規化の規則を決めていない",
  ],
  decision=[
    ("決定",
     "表はヘッダ行が宣言する<b>列名</b>で読む。<code>_common.py</code> が唯一の読み取り層で、検査は正規表現を持たない"),
    ("理由",
     "検査ごとに正規表現を書くと、静かに壊れる。実際に2か所で壊れていた ── 出典の照合が1行も走らず、<code>JSON</code> を規則と数えていた"),
    ("いまの状態",
     "<b>実装済み。</b><code>sections()</code> ・ <code>tables()</code> ・ <code>MdTable.col()</code> の3つで読む。列名の表記ゆれ（全角空白）はテストが見ている"),
  ])


t16 = Topic(16, "導出される値", "手で書いた数を、どう扱うか", "決着",
  answer="<b>A ── 数は導出物として書き出す</b>",
  note="<b>これも決めるより先に実装していた。</b><code>index.py</code> が <code>constraints/INDEX.md</code> へ <code>## 数</code> を書き出し、<code>file-catalog.md</code> の手で書いた数は消えている。決着として記録し直す。<code>file-catalog.md</code> は規則の数・出典の行数を本文に書いている。"
       "<b>今日だけで2回書き直した。</b>",
  kept=[
    Option("生成する",
           "<code>index.py</code> が書き出し、<code>file-catalog.md</code> の本文からは消す",
           "人が読む文書に、生成された塊が入る"),
    Option("検査する",
           "手で書いたまま置き、実物とずれたら <code>check.py</code> が落とす",
           "書き直す手間が残る。<b>正しい数を人が数え直すことになる</b>"),
  ],
  tables=[
    Table("この選択が置くもの", ["ずれたとき", "書き手の手間", "検査の数"],
          {"A": ["<b>起きない</b>", "無い", "増えない"],
           "B": ["落ちる", "毎回書き直す", "1つ増える"]}),
  ],
  dropped=[
    ("数を書かない",
     "何本あるかが読めなくなる。規約集の規模は、読み手が最初に知りたいことである"),
  ],
  found=[
    "<b>索引に対しては、既に同じ判断が下りている</b> ── "
    "<code>INDEX.md</code> は「生成物である。手で書き換えない」と宣言されている。"
    "数だけが手書きのまま残っていた",
  ],
  pick=("A",
    "<b>生成する。</b>索引と同じ扱いにする ── "
    "導出される値を手で書くと、必ず腐る。検査を足すより、書く必要をなくすほうが強い。"),
  grounds=[
    ("索引と同じ扱いにすること", "索引は生成物である。手で書かない",
     "決まり", "references/authoring.md Step 7 ／ constraints/INDEX.md"),
    ("手書きが腐ること", "落とした原文41・出典61行という古い数が残っており、今日2回書き直した",
     "実測", "references/file-catalog.md の書き換え（2026-09-06）"),
  ],
  costs=[
    "<code>index.py</code> の出力先を増やす ── いまは <code>INDEX.md</code> だけを書いている",
  ],
  weaknesses=[
    "<b>生成された塊を、人が読む文書のどこへ置くかを決めていない。</b>"
    "<code>file-catalog.md</code> の中か、別ファイルか",
  ],
  decision=[
    ("決定",
     "規約・層・規則・出典の数は、参照文書の本文に書かず、<code>index.py</code> が <code>constraints/INDEX.md</code> へ書き出す"),
    ("理由",
     "手で書いた数は必ず腐る。1日のうちに2回書き直していた"),
    ("いまの状態",
     "<b>実装済み。</b>「参照文書に、手で書いた数が残っていない」検査が<code>check.py</code> に在る。<code>glossary.md</code> に「数は導出物である」と宣言してある"),
  ])



t17 = Topic(17, "出典の書き方", "出典に、何を書くか", "決着",
  answer="<b>照合する文字列という仕組みを持たない。何を裏づけるかを概念で書く</b>",
  note="<b>この論点は一度決着し、2026-09-06 に決め直した。</b>"
       "変更前は「規則1件に原典1本。照合する文字列は、原典がその考えを名づけている箇所から取る」。"
       "<b>なぜ変えたか</b> ── 文字列一致は両方向に外れる。"
       "<code>GH-TP-01</code> の照合する文字列 <code>Short</code> は、"
       "<code>pkg.go.dev</code> の画面部品 <b>Shortcuts Modal</b> に1か所だけ当たっていた ── "
       "画面が変われば落ち、本文が書き換わっても落ちない。"
       "<b>「規則1件に原典1本」は残る。</b>変えたのは照合の仕組みだけである。",
  figures=[ONE_B, FALL_B],
  found=[
    "<b>照合する文字列 103 件のうち 87 件が、原典を1か所に特定できていなかった</b> ── "
    "<code>errors</code> は自分の原典に9か所、他の原典45本にも当たる",
    "<b>当たった数は測定として外していた。</b>1か所しか当たらないのに他の原典にも当たるものと、"
    "多くても特定できているものが、どちらも在る",
    "<b>たまたま当たっていた例が出た</b> ── <code>GH-TP-01</code> の <code>Short</code> は"
    "原典に1か所だけ当たり、その1か所が <b>Shortcuts Modal</b> という画面部品の名前だった。"
    "<b>検査は通っていたが、通っている理由が規則と無関係だった</b>",
    "<b>「原典が2本欲しい」実例が出た</b> ── <code>GO-ERR-01</code> は命題が2つで、"
    "Effective Go の Errors 節と Panic 節が別々に支えている。<b>2本目を足すのではなく、規則を2件に割る</b>",
    "<b>落としてあった原典107本のうち42本は、二重取得か空振りだった</b> ── "
    "<code>.md</code> を先に試した空振りが 14 バイトのファイルとして残っていた",
  ],
  grounds=[
    ("文字列一致が両方向に外れること",
     "<code>Short</code> は画面部品に当たるので原典の見た目が変わるだけで落ち、"
     "<code>errors</code> は広いので本文が書き換わっても落ちない",
     "実測", "sources/pkg.go.dev_testing ・ go.dev_doc_effective_go（2026-09-06）"),
    ("裏づけの判断は機械にできないこと",
     "「照合する文字列が、その規則を実際に裏づけているか」は、"
     "はじめから <b>機械では裁けないもの</b>として人へ渡してあった",
     "決まり", "scripts/check.py の HUMAN_ONLY"),
    ("概念で管理すること",
     "「原典を引いて規則をまとめるときには概念としてまとめたらいい。"
     "識別子みたいなところまで細かく見るとしんどい」",
     "決まり", "利用者の判断（2026-09-06）"),
    ("識別子とコマンドは書いてよいこと",
     "「別に識別子やcommandなどを書いていけないとは言ってない。"
     "原典を正しく読み、正しい識別子とコマンドなどで書けていれば問題ない」",
     "決まり", "利用者の判断（2026-09-06）"),
    ("規則1件に原典1本を残すこと",
     "2本要るなら、どちらも言い切っていない ── "
     "支えているのは2つを突き合わせたこちらの解釈であり、それは認めない出典に当たる",
     "決まり", "この論点の変更前の決着（2026-09-06）"),
  ],
  costs=[
    "<b>出典の列を移した</b> ── <code>照合する文字列</code> → <code>何を裏づけるか</code>（106行）。"
    "雛形15本も同時に移した",
    "<b>機械の検査を落とした</b> ── <code>check.py</code> の「照合する文字列が、落とした原典に在る」、"
    "<code>tests.py</code> の4クラス、<code>_common.py</code> の "
    "<code>source_rows</code> ・ <code>NEEDLE_COLUMN</code> ・ <code>local_source</code>、"
    "<code>check.py --needles</code>",
    "<code>sources.md</code> の「照合の仕方」を「原典の読み方」へ書き換えた",
    "<b>原典を取り直した</b> ── 50本を <code>source.py fetch</code> で取り直し、"
    "<code>sha256</code> と取得日時が付いた。107本 → 54本（二重取得と空振りを整理）",
  ],
  weaknesses=[
    "<b>版が上がったことを、機械は知らせない。</b>取り直しは人が判断して起動する ── "
    "<code>.meta.json</code> に <code>sha256</code> は在るが、比べる仕組みは無い",
    "<b>「原典が規則を裏づけているか」は、いまも機械では裁けない。</b>"
    "<code>check.py --contracts</code> の3件目として人へ渡している",
    "<b>考えを単位として切り出すかは未決。</b><code>contract test</code> の考えは "
    "<code>arch.data-port</code> でも <code>arch.hexagonal</code> でも成り立ち、置ける層が無い",
  ],
  decision=[
    ("決定", "<b>照合する文字列という仕組みを持たない。</b>"
             "出典には <b>何を裏づけるか</b>（その原典のどの考えに寄りかかるか）を書く。"
             "規則の本文に識別子・コマンド・数値を書くことは禁じない ── "
             "原典を正しく読み、原典の綴りのまま書く。<b>規則1件に原典1本</b>は残る"),
    ("理由", "文字列一致は両方向に外れる。落ちるべきときに落ちず、"
             "落ちるべきでないときに落ちる。裏づけの判断は、はじめから機械の外にあった"),
    ("いまの状態", "<b>適用済み。</b>出典106行すべてに「何を裏づけるか」が入り、"
                   "原典54本すべてが出典から指されている。原典を2本持つ規則は0件"),
  ])


t18 = Topic(18, "規則の形", "規則1件の塊の形を、全種類で共通にするか、規約の種類ごとに特化させるか", "決着",
  answer="<b>C ── 芯（ID・言明・検証方法・出典）＋種類ごとの欄。そのうえで一覧を詳細からの導出にする</b>",
  note="<b>規約は規則の集合だけで成り立っていない。</b>宣言（表と図）を持ち、"
       "その構造は種類ごとに違う ── だから雛形が15本ある。"
       "<b>問うているのは、規則の塊の形だけである。</b>"
       "いま規則1件は<b>3か所に散り</b>、同じ欄が2か所にあって<b>36件で値が食い違っている</b>。<b>2026-09-06 に、案C の一部が先に入った</b> ── <code>検証方法</code> を全8種類の雛形が持つようになり、詳細を持たない規則は0件になった。",
  figures=[SPREAD, SHAPES],
  kept=[
    Option("共通1つの形",
           "ID・言明・水準・根拠・検証方法・例外・例・出典を、すべての規約で同じ欄で持つ",
           "保証型に「水準」、規則型に「失敗したときの現れ方」が入る ── "
           "<b>使わない欄が空で並び</b>、「埋まらない欄が残るなら、まだ規約になっていない」"
           "という定めと衝突する"),
    Option("種類ごとの形",
           "雛形が欄を定める。いまの2つの形（規則型・保証型）をそのまま認め、種類ごとに増やす",
           "<b>共通の欄すら揃わない。</b>いま <code>検証方法</code> と <code>実行コマンド</code> で"
           "起きている ── 横断で「この規則はどう確かめるのか」を引けない"),
    Option("芯 ＋ 種類ごとの欄",
           "ID・言明・出典・検証方法は全種類で共通。"
           "水準・前提条件・失敗したときの現れ方などは、雛形が足す",
           "<b>芯に何を入れるかを決めることになる。</b>薄すぎれば種類ごとの形と変わらず、"
           "厚すぎれば共通1つと変わらない"),
  ],
  tables=[
    Table("いま在る食い違いが、どうなるか",
          ["同じ欄が2か所（36件）", "詳細の欄の並びが3通り", "横断で「どう確かめるか」を引けるか"],
          {"A": ["<b>起きない</b>（欄が1つになる）", "1通りへ畳む", "引ける"],
           "B": ["種類ごとに起きうる", "<b>3通りのまま</b>", "<b>引けない</b>"],
           "C": ["芯に入れば起きない", "芯の外だけが分かれる", "芯の範囲で引ける"]}),
    Table("いま在る2つの形",
          ["規則型（style ・ failure ・ concurrency ・ test-placement）",
           "保証型（acceptance ・ test-boundaries ・ test-mechanism ・ test-strategy）"],
          {"A": ["1つの形へ畳む", "1つの形へ畳む"],
           "B": ["そのまま残す", "そのまま残す"],
           "C": ["芯を共有し、水準・根拠・例外・例を足す",
                 "芯を共有し、前提条件・失敗したときの現れ方を足す"]}, plain=True),
  ],
  dropped=[
    ("規約のフォーマットを1つにする",
     "宣言の部分は種類ごとに構造が違う ── 層の一覧と、依存の可否と、ツールの表を、"
     "同じ雛形では表せない。<b>そのために雛形が15本ある</b>"),
    ("規則を規約から切り離し、1件1ファイルにする",
     "人が読んで承認する単位は文書である（論点4 の決着）。"
     "1件1ファイルにすると、承認の単位が消える"),
    ("一覧と詳細の両方を人が書き、食い違いを検査で捕まえる",
     "写しを作ってから、写しのずれを守る検査を足す形になる ── "
     "論点13 で <code>KINDS_BY_SHAPE</code> について同じ判断を下している"),
  ],
  found=[
    "<b>いま既に2つの形が並んでいる</b> ── 規則型4種類・保証型4種類。"
    "同じことを別の語で言っている欄（<code>検証方法</code>／<code>検証の単位</code>＋"
    "<code>実行コマンド</code>、<code>適用範囲</code>／<code>前提条件</code>）と、"
    "片方にしか無い欄（水準・根拠・例外・例 ／ 失敗したときの現れ方）が混ざる。"
    "<b>内容が違うから分かれたのか、別々に書いたから分かれたのかは、表からは決まらない</b>",
    "<b>規則1件は3か所に散っている</b> ── 規則一覧・規則の詳細・出典。"
    "<code>水準</code> と <code>検証方法</code> が一覧と詳細の2か所にあり、"
    "<b>65件のうち27件で値が食い違う</b>（一覧は「静的解析」、詳細は "
    "<code>gofmt -l .</code> が何も出力しない、など）",
    "<b>詳細節を持たない規則が30件ある</b> ── "
    "<code>test-boundaries</code> と <code>test-strategy</code> は表だけで、"
    "根拠も例外も例も持たない",
    "<b>宣言の部分は、3案のどれでも種類ごとに固有のまま。</b>"
    "この論点は規則の塊だけを動かす",
    "<b>案の選択だけでは、保守の問題が残ることが分かった。</b>"
    "A・B・C のどれを採っても、一覧が別に在る限り二重化は消えない ── "
    "<b>効いているのは形の数ではなく、同じ値が2か所に在ることだった</b>",
    "<b>その二重化で、同じ日に自分が5件を食い違わせた</b> ── "
    "<code>BA-AC-02</code> ・ <code>BA-AC-04</code> ・ <code>MC-AC-04</code> の"
    "<code>検証の単位</code>。詳細を書くとき一覧を見なかった",
    "<b>同じことに2つの名前が付いていた</b> ── 一覧の <code>前提条件</code> と"
    "詳細の <code>前提</code>。33件のうち<b>17件で値が違い</b>、誰も気づいていなかった",
  ],
  pick=("C",
    "<b>芯を共通にし、種類ごとの欄を雛形が足す。</b>"
    "どの種類でも同じであるものは既に決まっている ── "
    "ID（同じ層で一意）・言明（検査できる最小）・出どころ（無ければ規則ではない）・"
    "検証方法（書けないなら規則ではない）。<b>この4つは 2026-09-06 に全種類で埋まった</b>。"
    "<b>それ以外は、種類ごとに違っていて構わない。</b>"),
  grounds=[
    ("芯に出典を入れること", "出典を持たないものは規則ではない。種類によらない",
     "決まり", "SKILL.md ガードレール ／ references/sources.md"),
    ("芯に検証方法を入れること",
     "欄は1つ。命令が在るなら書き、その命令で落ちない部分が残るなら見る箇所を続けて書く",
     "決まり", "references/authoring.md Step 5 ／ 論点19"),
    ("芯に ID を入れること", "同じ層の中で、ID は重ならない",
     "決まり", "references/glossary.md 規則の ID"),
    ("種類ごとの欄を残すこと",
     "同じ4種別を全層へ置かない ── 言語のテストは「走る仕組み」、用途のテストは"
     "「何を保証するか」で、構造が別である",
     "決まり", "論点10 の決まり"),
    ("いまの形が壊れていること",
      "規則69件すべてが詳細節を持つが、一覧と詳細の両方に在る欄105件のうち"
     "<b>36件で値が食い違う</b>（一覧は「静的解析」、詳細は "
     "<code>go test -race ./...</code>）",
     "実測", "constraints/ 全37本の表と節の集計（2026-09-06）"),
  ],
  costs=[
    "<b>芯に何を入れるかを決める。</b>候補の ID・言明・出典・検証方法 は<b>既に全種類で埋まっている</b>",
    "<b>一覧を、塊からの導出にする。</b>手で書くと、また食い違う",
    "36件の食い違いを、芯の値へ1つに畳む",
  ],
  weaknesses=[
    "<b>規則型と保証型が、本当に別の内容なのかは決めていない。</b>"
    "書き分けただけかもしれない ── C を採ってもこの問いは残る",
    "<b><code>test-mechanism</code> だけ、詳細に「命令で落ちない部分」が無い。</b>"
    "受け入れ型なのに 2026-09-06 の書き換えから漏れた ── 詳細6か所",
    "<b>一覧の列を減らしたので、一覧だけでは検証方法が分からない。</b>"
    "長い文が並ぶ表は読めないので落としたが、横断で引くには詳細を開くことになる",
    "<b>移行の途中で、19本の前置きを落とした。</b>"
    "読み取り層の <code>body</code> が前置きを含まないことに気づかず書き戻した ── "
    "git から復元したが、<b>正本を直接書き換える移行の危うさが出た</b>",
  ],
  decision=[
    ("決定", "規則1件は<b>詳細の塊1つ</b>である。芯（ID・言明・検証方法・出典）は"
             "全8種類で同じ欄名を持ち、残りは雛形が定める。"
             "<b>一覧は書かない</b> ── <code>scripts/relist.py</code> が詳細から書き出す"),
    ("理由", "案の選択だけでは二重化が残る。一覧と詳細の両方に在る欄105件のうち"
             "<b>36件で値が食い違い</b>、そのうち5件は同じ日に自分で作った。"
             "写しを守る検査を足すより、写しを作らないほうが短い（論点13・16 と同じ判断）"),
    ("いまの状態", "<b>適用済み。</b>両方に在る欄は 105件 → <b>0件</b>。"
                   "<code>実行コマンド</code> と <code>命令で落ちない部分</code> は "
                   "<code>検証方法</code> 1つへ畳み、<code>test-mechanism</code> の"
                   "取りこぼしも消えた。<code>check.py</code> 15件・"
                   "<code>tests.py</code> 27件が通っている"),
  ])

# 盤面は board_merge.py が1枚に畳む。ここは論点だけを持つ

t19 = Topic(19, "検証方法の欄", "規則の検証方法を、機械と人で分けて書くか", "決着",
  answer="<b>分けない ── 欄は1つで、命令と、命令で落ちない部分を並べて書く</b>",
  note="<b>はじめは機械／人の札を立て、排他にしていた。</b>"
       "そのうえ札の判定を「道具の名前が出てくるか」で行っていたため、"
       "その道具にその検査が無くても機械の札が立っていた。",
  figures=[VERIFY],
  grounds=[
    ("排他にしないこと",
     "30件を当て直すと、命令だけで閉じるのは <code>GO-ERR-05</code> の1件だけで、"
     "15件は命令と見る箇所の両方が要る",
     "実測", "docs/adr/verify_draft.py の30件（2026-09-06）"),
    ("機械が落とせるのは形だけであること",
     "<code>ST1020</code> は doc コメントが識別子名で始まることを落とすが、"
     "その doc がその識別子を説明しているかは落とさない",
     "原典", "staticcheck.dev/docs/checks（2026-09-06 取得）"),
    ("道具の名前を機械の根拠にしないこと",
     "<code>go vet</code> の35検査に第1引数を見るものは無く、"
     "<code>staticcheck</code> に捨てた失敗を見る検査も無かった。"
     "確かめずに機械と札を立てた主張が3件あった",
     "原典", "pkg.go.dev/cmd/vet ・ staticcheck.dev/docs/checks（2026-09-06 取得）"),
    ("この欄が機械だけで閉じないこと",
     "検証方法は利用側のコードに向くが、"
     "<b>誰が何を確かめるかの原則は論点20 が決める</b>",
     "決まり", "論点20"),
  ],
  costs=[
    "<code>references/authoring.md</code> Step 5 の分岐表を廃止し、欄1つの形へ書き換えた",
    "雛形6本を書き換えた ── <code>acceptance</code> に「命令で落ちない部分」の行、"
    "<code>test-boundaries</code> と <code>test-strategy</code> に「振る舞いの詳細」の節",
    "<code>scripts/tests.py</code> の検査「使わない語」を落とした ── "
    "規約の本文に「ガイドライン」の語が出たら落ちる検査で、原典の題名で誤検知する",
  ],
  weaknesses=[
    "<b><code>test-mechanism</code> を取りこぼしている。</b>"
    "受け入れ型なのに「命令で落ちない部分」の行が無い。詳細6か所が対象",
    "<b>同じ欄が一覧と詳細の2か所にある。</b>規則型の <code>検証方法</code>（一覧は種類、"
    "詳細は方法）と、受け入れ型の <code>検証の単位</code>。"
    "実際に <code>BA-AC-02</code> などで食い違わせた ── 論点18 が扱う",
  ],
  decision=[
    ("決定", "検証方法の欄は1つ。命令が在るなら書き、"
             "その命令で落ちない部分が残るなら、見る箇所を続けて書く"),
    ("理由", "札を立てると排他になり、命令が在る規則は「もう人が見なくてよい」と読める。"
             "実際には30件のうち15件で両方が要る"),
    ("いまの状態", "<b>適用済み。</b>検証方法を持たなかった30件すべてに書き、"
                   "規約14本を 2026-09-06 に承認した"),
  ])



t20 = Topic(20, "誰が何を確かめるか",
  "CodingSkills という成果物を、機械・AI・人はそれぞれ何で確かめるか", "決着",
  answer="<b>機械は形式だけ。内容の正当性は AI が担保し、人が承認する</b>",
  note="<b>この原則を、論点19 の根拠1行に埋めてしまっていた。</b>"
       "論点19 は「規約が利用側のコードに課す欄の書き方」であって、"
       "<b>CodingSkills 自身を誰が確かめるか</b>とは層が違う。独立した論点として立て直す。",
  figures=[ROLES],
  grounds=[
    ("機械に守らせるのは形式であること",
     "「機械に守らせるべきなのは、出力したフォーマットが正しい契約のフォーマットに"
     "なっているかだと思う。記載内容を機械的にチェックは正直誤検知しか生まない気がします」",
     "決まり", "利用者の判断（2026-09-06）"),
    ("内容は AI が担保し、人が承認すること",
     "「AIが内容の正当性を担保し、人間が承認する。文書のフォーマットや成果物のような"
     "不変なものを機械的にチェックするのがいい」",
     "決まり", "利用者の判断（2026-09-06）"),
    ("内容の機械検査が実際に誤検知を生むこと",
     "規約の本文に「ガイドライン」「ベストプラクティス」の語が出たら落とす検査を持っていた。"
     "<b>原典の題名にその語が入っただけで落ちる</b>",
     "実測", "scripts/tests.py の検査「使わない語」（2026-09-06 に削除）"),
    ("形の検査は誤検知を生まないこと",
     "<code>check.py</code> の14件のうち13件は形の検査で、規約37本に対して食い違い0件。"
     "前置きの欄・節の集合・列名・ID の重なりだけを見ている",
     "実測", "python3 scripts/check.py（2026-09-06）"),
    ("道具の名前を根拠にしないこと",
     "「機械で見ている」と書いた主張のうち3件は、その道具にその検査が無かった。"
     "<code>go vet</code> の35検査に第1引数を見るものは無い",
     "原典", "pkg.go.dev/cmd/vet ・ staticcheck.dev/docs/checks（2026-09-06 取得）"),
  ],
  costs=[
    "<code>check.py</code> に、内容へ踏み込む検査を足さない。"
    "足したくなったら、それは<b>機械では裁けないもの</b>として人へ渡す欄に書く",
    "<code>tests.py</code> の検査「使わない語」を削除した（32→31件）",
    "<b>承認を形骸化させない工夫が要る。</b><code>check.py</code> は欄が埋まっているかしか"
    "見ないので、内容が変わっても日付だけ古いまま通る",
  ],
  weaknesses=[
    "<b>「照合する文字列が、落とした原文に在る」だけが形の検査ではない。</b>"
    "外部の原文に文字列が在るかを見ており、この原則からは外れる。"
    "<b>版が上がったときに落ちるための仕掛けとして残すか、確認が要る</b>",
    "<b>承認の粒度が1日単位である。</b><code>approved_at</code> は日付なので、"
    "同じ日に2回変更すると、承認済みの内容と未承認の内容を区別できない ── "
    "実際に 2026-09-06 で起きている",
    "<b>AI が内容を担保したことを、何も記録していない。</b>"
    "人が承認する前に AI が何を確かめたかは、いまどこにも残らない",
  ],
  decision=[
    ("決定", "<b>機械</b>は、出力した形式が契約どおりかだけを見る（不変なもの）。"
             "<b>AI</b> が内容の正当性を担保し、<b>人</b> が承認する"),
    ("理由", "内容を機械で裁くと誤検知しか生まない。"
             "実際に「ガイドライン」の語を落とす検査が、原典の題名で誤検知する形だった"),
    ("効くこと", "検査を足したくなったとき、"
                 "<b>それが形の検査かどうか</b>が先に問われる。"
                 "形でないなら、機械ではなく人へ渡す欄に書く"),
    ("この原則で落としたもの", "内容を機械で裁こうとしていた検査2つ ── "
                 "<code>tests.py</code> の「使わない語」（原典の題名で誤検知する）と、"
                 "<code>check.py</code> の「照合する文字列が、落とした原典に在る」"
                 "（画面部品の名前に当たっていた）"),
    ("この原則で足したもの", "<code>check.py</code> の"
                 "「出典の種類が、認める3つのいずれかである」 ── "
                 "<b>宣言した語彙に成果物が収まっているかは、形の検査である</b>"),
  ])


def build():
    PREV = _table(["#", "論点"],
      [[str(i + 1), q] for i, q in enumerate([
        "層を並べるか、制約が依存する軸を宣言するか",
        "何をもって、軸と認めるか",
        "抽象側が決めないことを、どう宣言するか",
        "制約1件を、何で数えるか",
        "制約と実装の、どちらが先に在るか",
        "CodingSkill は、何をして何をしないか",
        "このSkillは、何と何を担うか",
        "制約は、何を根拠に書けるか",
        "このSkillは、何を持つか",
        "層と種別を、どう構造で表すか",
        "どの制約が在るかを、どう見えるようにするか",
        "制約1件を、Markdown でどう書くか",
      ])])

    html = deck("CodingSkills は何を確かめるか", [t13, t17, t14, t15, t16],
      intro="<b>論点13〜17。CodingSkills のブレストの続きである（2026-09-06）。</b>"
            "論点13 と 17 が決着し、3つが開いている ── <b>決めただけでは守られない</b>ためである。"
            "論点1〜12 は <code>docs/adr/brainstorm-coding-skills.html</code> に在る ── "
            "あちらは生成元のスクリプトを持たない古い形式なので、同じ1枚へ畳めていない。"
            "<b>畳めていないこと自体が、論点13 の対象である</b>（成果物の保守）。"
            "<b>これは正本ではない。ブレストの記録である。</b>",
      extras=[("関連する成果物",
        "<p class='lead'>いま在るものを並べる。"
        "<b>成果物のスナップショットは、そこに載っている決定の出どころを持たない</b> ── "
        "出どころはこの盤面にある。</p>" + _table(["成果物", "何が在るか", "この盤面との関係"],
        [["<code>docs/adr/needles.html</code>", "出典100件を「その原文を特定できているか」で測った結果",
          "論点17 の実測"],
         ["<code>docs/adr/source-rule.html</code>", "出典の書き方3原則と、実データの実例2件",
          "論点17 の決定"],
         ["CodingSkills 第1版の全文（Artifact <code>24732a71</code>）",
          "SKILL.md ・ 参照文書 ・ 雛形15本 ・ 規約37本のダンプ",
          "<b>決定そのものは載っていない。</b>論点1〜12 の結果として在る"],
         ["<code>docs/adr/brainstorm-coding-skills.html</code>", "論点1〜12",
          "<b>生成元のスクリプトを持たない</b>ので、この1枚へ畳めていない"]])),
        ("これまでの論点（別の盤面に在る）",
        "<p class='lead'>結論はこの盤面へ写していない ── 写すと、写した側が正になってしまう。"
        "原本は <code>docs/adr/brainstorm-coding-skills.html</code> である。</p>" + PREV)])
    write(html, "docs/adr/brainstorm-coding-skills-contracts.html",
          "CodingSkills は何を確かめるか")
    print("ok")


if __name__ == "__main__":
    build()
