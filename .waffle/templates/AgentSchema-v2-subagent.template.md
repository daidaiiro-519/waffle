# {{documentId}}

## プレースフォルダー定義票

| プレースフォルダー | 記入する内容 |
|---|---|
| `{{title.title}}` | documentId をそのまま設定してください。 |
| `{{goal.text}}` | 今回、何を達成するために委譲するのかを、背景（なぜ今これが必要か）とセットで記述してください。「〜を手伝う」のような曖昧な依頼ではなく、「〜という状況で、〜を明らかにするため」のように、委譲元が置かれている文脈まで含めること。 |
| `{{persona.text}}` | 目的を達成するのにふさわしい役割・専門性を2〜3文で定義してください。例:「あなたはシニアセキュリティエンジニアです。」のように、一人称の人格として明示すること。 |
| `{{skillPreloads.items[1]}}` | この役割を果たすために読み込ませるSkillのdocumentIdを列挙してください。無ければこのブロック自体を省略してください。（配列。この形式の行を必要な数だけ繰り返す） |
| `{{skillPreloads.guidance}}` | 列挙した各Skillを、後続のタスクのどの場面で・どう使うべきかを記述してください（例:「tech-lead-advisorは配置判断が必要な場面で参照する」）。 |
| `{{task.items[1]}}` | 今回の委譲で実行してほしい作業を、実行可能な粒度で列挙してください（例:「認証まわりの差分をインジェクション脆弱性の観点でレビューする」）。抽象的な担当領域の説明ではなく、今回やるべき手順・確認項目として書く。（配列。この形式の行を必要な数だけ繰り返す） |
| `{{task.references[1]}}` | この作業の対象となる具体的な参照先（ファイルパス・documentId等）を列挙してください。作業内容を抽象的に説明し尽くすだけで満足せず、必ず実物を確認できる場所を示すこと。対象が`.waffle/documents/`配下のdocument.jsonインスタンスの場合は、直接読まずWaffle CLI（`uv run --project waffle waffle query/validate ...`）経由で確認するよう参照先の記述に含める（Waffle自身がdogfood対象であるため）。ソースコード・スキーマ定義ファイル等、Waffle管理下のdocumentでないものは直接読んでよい。対象となる既存アーティファクトが無い（新規作成のみのタスク等）場合は空配列でよい。（配列。この形式の行を必要な数だけ繰り返す） |
| `{{deliverable.text}}` | 期待する成果物の形を記述してください（例:「具体的な行番号と修正案を示した指摘一覧」「合否と根拠を含むレビューコメント」）。「うまくやる」ではなく、受け取った側がそのまま使える形を指定すること。 |
| `{{acceptanceCriteria.items[1]}}` | 成果物が満たすべき、検証可能な完了条件を列挙してください（例:「指摘の各項目に再現手順が付いている」「該当テストが実行され結果が記録されている」）。「頑張る」「漏れなく確認する」のような検証不能な基準は書かない。（配列。この形式の行を必要な数だけ繰り返す） |
| `{{toolAccess.tools[1]}}` | このSubagentに許可するツール名を列挙してください。読み取り専用の調査タスクならRead/Grep/Glob等に絞り、Write/Editのような変更系ツールは本当に必要な場合だけ含める。制限しない場合はこのブロック自体を省略してください。本文には出力せず、OrchestratorがAgentツールを呼び出す際のパラメータ（例: 既存のsubagent_typeの選択、または個別のツール制限）としてのみ使う。（配列。この形式の行を必要な数だけ繰り返す） |
| `{{toolAccess.disallowedTools[1]}}` | このSubagentに禁止するツール名を列挙してください。無ければ空配列。本文には出力せず、OrchestratorがAgentツールを呼び出す際のパラメータとしてのみ使う。（配列。この形式の行を必要な数だけ繰り返す） |
| `{{runtimeConfig.model}}` | モデルを上書きする場合に指定してください（例: sonnet, opus）。既定を継承する場合はこのブロック自体を省略してください。本文には出力せず、OrchestratorがAgentツールを呼び出す際のmodel等のパラメータとしてのみ使う。 |
| `{{runtimeConfig.permissionMode}}` | 権限モードを指定してください（例: default, plan）。本文には出力せず、OrchestratorがAgentツールを呼び出す際のisolation等のパラメータとしてのみ使う。 |

---

# {{title.title}}

---

## 目的

{{goal.text}}

---

## 役割定義

{{persona.text}}

---

## 読み込むSkill

- {{skillPreloads.items[1]}}

{{skillPreloads.guidance}}

---

## タスク内容

- {{task.items[1]}}

### 参照先

- {{task.references[1]}}

---

## 成果物

{{deliverable.text}}

---

## 受け入れ基準

- {{acceptanceCriteria.items[1]}}
