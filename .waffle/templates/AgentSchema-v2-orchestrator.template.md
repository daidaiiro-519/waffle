# {{documentId}}

## プレースフォルダー定義票

| プレースフォルダー | 記入する内容 |
|---|---|
| `{{タイトル}}` | documentId をそのまま設定してください。（JSON上のフィールド: content.title.title） |
| `{{管轄範囲.管轄ディレクトリ}}` | このOrchestratorが管轄するディレクトリをリポジトリルートからの相対パスで指定してください。ルート自身なら空文字。例: "", "waffle/"（JSON上のフィールド: content.scope.path） |
| `{{管轄範囲.概要}}` | このディレクトリ・プロダクトが何であるかを1〜2文で記述してください。（JSON上のフィールド: content.scope.description） |
| `{{運用ルール.項目1.ルール}}` | 何をすべきかの一文。「〜してください」より「〜する」という明確な指示文にする。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.operatingRules.items[].rule） |
| `{{運用ルール.項目1.なぜ}}` | なぜこのルールが必要か（根拠）。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.operatingRules.items[].why） |
| `{{運用ルール.項目1.適用方法}}` | いつ・どう適用するか（具体的な手順・コマンド・対象範囲）。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.operatingRules.items[].howToApply） |
| `{{主要コマンド.項目1.コマンド}}` | 実行するコマンド。（配列。この形式の行を必要な数だけ繰り返す）（任意ブロック：無ければ主要コマンドブロック自体を省略してよい）（JSON上のフィールド: content.keyCommands.items[].command） |
| `{{主要コマンド.項目1.用途}}` | 何のためのコマンドかを1文で。（配列。この形式の行を必要な数だけ繰り返す）（任意ブロック：無ければ主要コマンドブロック自体を省略してよい）（JSON上のフィールド: content.keyCommands.items[].purpose） |
| `{{委譲先.項目1.対象ディレクトリ}}` | 子Orchestratorが管轄するディレクトリ。例: "waffle/"（配列。この形式の行を必要な数だけ繰り返す。子を持たなければ空配列）（JSON上のフィールド: content.subOrchestratorRefs.items[].scope） |
| `{{委譲先.項目1.参照Orchestrator}}` | 子OrchestratorのdocumentId。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.subOrchestratorRefs.items[].documentId） |
| `{{委譲先.項目1.備考}}` | そのscopeで作業する際に何を確認すべきかの短い説明。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.subOrchestratorRefs.items[].note） |
| `{{Skillフォローアップ.項目1.呼び出した後}}` | どのSkillを呼び出した後の話か（例: advisor全般／特定のSkill名）。（配列。この形式の行を必要な数だけ繰り返す。フォローアップ手順が無ければ空配列）（JSON上のフィールド: content.skillFollowUp.items[].afterSkill） |
| `{{Skillフォローアップ.項目1.次に行うこと}}` | 次に何をするか（検証可能な具体的手順）。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.skillFollowUp.items[].thenAction） |
| `{{Skillフォローアップ.項目1.返却フォーマット1}}` | フォローアップの結果をどんな段階構成で返すべきか、短い見出し語を順番に列挙してください（例: ["各意見", "統合見解", "合意事項", "次のアクション"]）。段落の説明文ではなく、一目で流れが追える短い語の並びにする。（配列の中の配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.skillFollowUp.items[].outputFormat[]） |
| `{{Skillフォローアップ.項目1.テンプレート}}` | outputFormatの各段階を実際に埋めるためのテンプレートファイルへの参照パス（プレースホルダー付きmarkdown）。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.skillFollowUp.items[].template） |
| `{{Skillフォローアップ.項目1.理由}}` | なぜこのフォローアップが必要か。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.skillFollowUp.items[].why） |

---

# {{タイトル}}

---

## 管轄範囲

- **管轄ディレクトリ**: {{管轄範囲.管轄ディレクトリ}}
- **概要**: {{管轄範囲.概要}}

---

## 運用ルール

| ルール | なぜ | 適用方法 |
|---|---|---|
| {{運用ルール.項目1.ルール}} | {{運用ルール.項目1.なぜ}} | {{運用ルール.項目1.適用方法}} |

---

## 主要コマンド

| コマンド | 用途 |
|---|---|
| {{主要コマンド.項目1.コマンド}} | {{主要コマンド.項目1.用途}} |

---

## 委譲先

### 以下のscope配下で作業する場合は、必ず対応するOrchestrator documentのcontentを先に読むこと

| 対象ディレクトリ | 参照Orchestrator | 備考 |
|---|---|---|
| {{委譲先.項目1.対象ディレクトリ}} | {{委譲先.項目1.参照Orchestrator}} | {{委譲先.項目1.備考}} |

---

## Skillフォローアップ

### Skillを呼び出した後に必ず行うフォローアップ

| 呼び出した後 | 次に行うこと | 返却フォーマット | テンプレート | 理由 |
|---|---|---|---|---|
| {{Skillフォローアップ.項目1.呼び出した後}} | {{Skillフォローアップ.項目1.次に行うこと}} | {{Skillフォローアップ.項目1.返却フォーマット1}} | `{{Skillフォローアップ.項目1.テンプレート}}` | {{Skillフォローアップ.項目1.理由}} |
