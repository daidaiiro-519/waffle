---
name: "{{この文書を一意に識別する識別子。}}"
description: "{{このSkillが「何をするか」だけでなく「いつ使うべきか」を1〜2文で記述してください。動詞で始め、判断のトリガーとなる状況・キーワードを含めること（例:「〜を実装・修正する際に必ず使う」「ユーザーが〜と言ったときに使う」）。自動委譲/自動参照の判断根拠として使われる最重要フィールドです。他のSkill名を本文中に直接書かないこと（疎結合原則。受け手が送り手の内部実装・存在を知っている状態を避ける）。前提とする追加の文脈情報があれば精度が上がる場合は、その情報の抽象的な形状のみを記述し、誰が生成したかには触れない。このSkillは単体で完結して回答できることを前提とする。}}"
disable-model-invocation: "{{ユーザーが `/skill-name` で明示的に呼び出す手順型Skill（自動発火させたくない）なら true。ドメイン知識として自動的に参照されてよいリファレンス型Skillなら false。}}"
---

# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのSkillが何をするかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を担うadvisor Skill」を含め、読んだだけでこのSkillがadvisorだとわかるようにしてください。識別子部分はこのSkillの一意な名前をそのまま使う。}}

## 目的

{{このSkillが「何をするか」だけでなく「いつ使うべきか」を1〜2文で記述してください。動詞で始め、判断のトリガーとなる状況・キーワードを含めること（例:「〜を実装・修正する際に必ず使う」「ユーザーが〜と言ったときに使う」）。自動委譲/自動参照の判断根拠として使われる最重要フィールドです。他のSkill名を本文中に直接書かないこと（疎結合原則。受け手が送り手の内部実装・存在を知っている状態を避ける）。前提とする追加の文脈情報があれば精度が上がる場合は、その情報の抽象的な形状のみを記述し、誰が生成したかには触れない。このSkillは単体で完結して回答できることを前提とする。}}

---

## 役割

- {{このSkillが担う責務を列挙してください。各項目は「〜する」「〜を提供する」の形で記述してください。他のSkill名を役割の説明に直接書かないこと（疎結合原則）。前提とする情報がある場合は、その情報の形状のみを抽象的に記述する。}}

---

## 相談種別と回答テンプレート

| 相談種別 | 判定条件 | テンプレート |
|---|---|---|
| {{相談種別の名前（例: 概念質問、判断相談、実装相談）。}} | {{この種別だと判定する条件（例: 「〜とは」という形式の質問）。}} | `{{この相談種別で使う回答テンプレートを特定できる名前や参照を記述してください。例:「概念質問用テンプレート」「判断相談用テンプレート」のように、テンプレートの用途がわかる名前で。}}` |

---

## 入力の想定

| 受け取る情報 | 解釈・既定値 |
|---|---|
| {{受け取ると想定する情報の種類（例: 対象ブランチ、対象ファイル範囲）。}} | {{その情報が明示されなかった場合にどう解釈するか（既定値・確認を挟む等）。}} |

---

## 実行手順

### Step 1: {{このStepで行うことを動詞で始めて簡潔に記述してください。}}

{{この Step でSubAgentが行うドメイン操作の要点を1〜2文で簡潔に。インフラ語彙（document.json・MCP・ファイルパス・Python API 等）は書かない。}}

- {{選択肢・一覧など箇条書きにすべき項目があれば1行ずつ列挙（任意）。}}

#### {{このSubStepで行うことを動詞で始めて簡潔に記述してください。}}

{{この SubStep でSubAgentが行うドメイン操作の要点を1〜2文で簡潔に。インフラ語彙は書かない。}}

- {{箇条書きにすべき項目があれば1行ずつ列挙（任意）。}}

---

## ガードレール

- {{このSkillを実行する際に必ず守るべき制約・禁止事項・前提条件を、1項目1文で行動可能な形（「〜してはならない」「〜する前に必ず〜を確認する」等）で列挙してください。判断がぶれやすい項目は理由も添えてください。例:「検証済みでない前提には着手しない（後で手戻りが生じるため）」。}}

---

## 参照knowledge

- `{{参照するbackbone knowledgeを実行環境で特定できる形で記述してください。例: ファイルパスやドキュメント名（DDD原則集、テスト理論リファレンス等）。}}`: {{このファイルが何を扱うかを1文で（例: DDDの集約設計原則と判断基準）}}
