---
id: "{{この文書を一意に識別する識別子。}}"
title: "{{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるTech Stack仕様」を含め、読んだだけでこの仕様が技術スタックだとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}"
description: "{{この文書が何を定めているかを1〜2文で。}}"
tags: "{{この document を分類するタグ。例: [\"tier:backend\"]}}"
---

# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるTech Stack仕様」を含め、読んだだけでこの仕様が技術スタックだとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}

## 概要

{{この文書が何を定めているかを1〜2文で。}}

---

## スタック概要

- **対象領域（ティア: backend=サーバー側 / frontend=画面側 / platform=基盤側）**: {{このスタックが担うティア（backend=サーバー側/frontend=画面側/platform=基盤側）。下のenumから選ぶ。（選択肢: backend / frontend / platform）}}
- **スタック名**: {{スタック名（kebab-case）。この文書の識別子に含まれるスタック名部分と一致させる。}}

---

## ランタイム

- **実行ターゲット**: {{実行ターゲット（例: CLI / ローカルプロセス、コンテナ、サーバーレス関数）。}}
- **並行モデル**: {{並行モデル（例: 同期主体（境界のみ async））。}}

### 言語

| 言語 | 版 | 拡張子 | 役割 |
|---|---|---|---|
| `{{言語識別子。}}` | {{版の制約（例: >=3.12）。固定不要なら空。}} | `{{この言語の実装ファイルの拡張子（例: [".py"]）。走査でファイルを見つけたとき、拡張子から言語を解決する裏付けになる。}}` | {{このスタックの中での位置づけ。primary=主要な実装 / edge=実行環境が言語を強制する部分（エッジ関数等）/ tooling=補助。}} |

---

## フレームワーク

{{name が空のとき、無い理由を1文で。name があれば空文字。}}

- **フレームワーク**: {{看板フレームワーク名（1つ）。無ければ空文字（例: FastAPI, React）}}
- **選定理由**: {{name が空でなければ、他の候補（習熟度・既存資産・技術要件等）ではなくこれを選んだ理由を1文で（ADR）。name が空なら空文字。}}

---

## 公開インターフェース

### {{様式（例: CLI, MCP, REST）}}

- **実装**: {{実装ライブラリ（例: typer, fastmcp）}}

#### 選定理由

{{他の候補（習熟度・既存資産・技術要件等）ではなくこの実装を選んだ理由を1文で（ADR）。}}

---

## ミドルウェア

{{items が空のとき、無い理由を1文で。items があれば空文字。}}

### {{製品名（例: PostgreSQL, Redis）}}

- **役割**: {{ミドルウェアの役割分類。下のenumから選ぶ。key-value は取り出しの唯一の元になる保管（キャッシュではない）、object-storage は成果物そのものの置き場、identity は認証基盤。}}
- **アクセス手段**: {{アクセスするクライアントライブラリ（例: SQLAlchemy）}}

#### 選定理由

{{他の候補（別のDB製品等・実行環境の制約含む）ではなくこれを選んだ理由を1文で（ADR）。}}

---

## ライブラリ

### {{実装ライブラリ（例: jsonschema）}}

- **分類**: {{能力の分類（AWS カテゴリ相当）（例: validation, observability, security）}}
- **用途**: {{用途名（例: schema-validation, logging）}}
- **バージョン**: {{バージョン。固定不要なら空。}}

#### 選定理由

{{他の候補ではなくこの実装を選んだ理由を1文で（ADR）。}}

---

## 開発ツール

- **パッケージ管理**: {{パッケージ管理ツール（例: uv（.venv / uv.lock 固定））}}
- **lint / format**: {{lint/format ツール。任意なら空。}}
- **選定理由**: {{他の候補ではなくこのパッケージ管理ツールを選んだ理由を1文で（ADR）。}}

---

## 依存方針

| 種別 | 方針 |
|---|---|
| {{この規約の強制力。必須（must、違反は許容しない）／禁止（must not、行ってはならない）／推奨（should、望ましいが例外を許容する）のいずれかを選ぶ。}} | {{依存ライブラリを追加する際に満たすべき方針の内容（例: 標準ライブラリ・既存依存で代替可能なら追加しない）}} |
