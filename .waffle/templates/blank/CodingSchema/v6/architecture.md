---
id: "{{この文書を一意に識別する識別子。}}"
title: "{{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるArchitecture仕様」を含め、読んだだけでこの仕様がアーキテクチャだとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}"
description: "{{この文書が何を定めているかを1〜2文で。}}"
tags: "{{この document を分類するタグ。例: [\"tier:backend\"]}}"
---

# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるArchitecture仕様」を含め、読んだだけでこの仕様がアーキテクチャだとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}

## 概要

{{この文書が何を定めているかを1〜2文で。}}

---

## 実装を受け持つコンテキスト

- {{このアーキテクチャが実装を受け持つ境界づけられたコンテキストのdocumentId（例: bc-waffle）。ドリフト検査はこの宣言から仕様側の走査範囲を決める。}}

---

## レイヤーと依存方向

### 様式

{{技術方式（例: ポートとアダプター（ヘキサゴナル）／オニオン／クリーン／レイヤード）。}}

| レイヤー | 配置 | 責務 | 依存してよい先 |
|---|---|---|---|
| {{レイヤー名。他のブロックはこの名前を指すので、ここが層名の唯一の宣言になる。所有されている要素（ポート等）を層として並べない——代わりに、所有する層の path の 配下へ置く（ports なら application の path の下）。層と所有物のどちらかは 「その要素が独自の依存規則を持つか」で決める。ports は application が外部へ要求する インターフェースであり、application と同じ依存規則に従うので層ではない。}} | `{{このレイヤー自身のディレクトリ（layout の sourceRoot からの相対）。ファイルがどの層に属するかは、この宣言だけで決まる。守ること: (1)その層が所有する要素は、このパスの配下に置く。ports は層ではなく application が所有するので、application の path が …/application なら ports は …/application/ports。所有物を層の外の兄弟ディレクトリに出さない (2)ある層の path を、別の層の path の祖先にしない。祖先にすると配下のファイルが どちらの層かパスから決まらなくなる（入口の層に sourceRoot 直下を与えると、他の全層を飲み込む）。入口も自分のディレクトリを持つ (3)実装をまだ書いていなくてもここには「あるべき配置」を書く。存在しないパスは検査が未実装として報告するので、実態に合わせて後退させない。}}` | {{このレイヤーの責務を1文で。}} | {{依存してよいレイヤー名を列挙。ここに書かれていない依存は違反として検出される。最内層は空。}} |

---

## ディレクトリ構成

```
{{ディレクトリツリーの図（テキスト）。これは宣言ではなく投影——配置を決めているのは layers[].path と conceptPlacement.placements であり、この図はそれを人が一目で掴めるよう 並べ直したもの。書く順序は、先に layers[].path と placements を確定し、そのあとで この図をそこから起こす。逆順で書くと、図にしか存在しないディレクトリが生まれる。守ること: (1)layers[].path のすべてが、この図にノードとして現れる (2)この図にあってどの path にも対応しないディレクトリは、合成ルート（compositionRootPaths）か、rules で層を持たないと明示的に宣言した領域（別ランタイム・配布物・運用スクリプト等）に限る。それ以外は層か概念の宣言漏れ (3)機械が読む決定を図の注釈に書かず、granularity や compositionRootPaths の専用欄へ置く。}}
```

### 1ファイルの粒度

| 概念 | 1ファイルあたり |
|---|---|
| `{{概念の種類（conceptPlacement の concept と同じ語彙）。}}` | {{1ファイルに置く数。1なら1概念1ファイル。複数を許すなら0（上限なし）。}} |

### 合成ルート（結線・DI）

{{合成ルート（結線・DI）の方針を1文で。合成ルートは層のグラフの外にあり、結線のためにすべてを知ってよい唯一の場所。}}

- {{合成ルートの実際の場所（sourceRoot からの相対パス）。依存の検査はここを層の規則から除外し、代わりに『配線専用に保つ』を別に確かめる。}}

---

## 概念 → 実現形

| 概念 | 配置 | 形（決定レベル） |
|---|---|---|
| `{{DDD概念の種類。}}` | {{この配置が担う役割。1か所にしか置かない概念では指定しない（役割という区別が無いため）。interface と implementation は宣言と実装を分ける場合、declaration と definition はヘッダと本体を分ける言語の場合。}} `{{配置（sourceRoot からの相対パス）。}}` | {{この概念の実現方針を決定レベルで（正確な署名は書かない）。}} |

---

## 規約（守るべきルール）

| 種別 | 規約 |
|---|---|
| {{この規約の強制力。必須（must、違反は許容しない）／禁止（must not、行ってはならない）／推奨（should、望ましいが例外を許容する）のいずれかを選ぶ。}} | {{依存方向・DIの受け取り方・エラー伝播（結果型/例外の使い分け）等、横断的な決定ルールの内容（例: 依存は内向きのみ）}} |

---

## サブドメイン別の厚み

| Category | 実装の厚み |
|---|---|
| {{subdomain spec の Category。}} | {{実装の厚み方針を1文で（例: ドメインモデルで厚く実装する）}} |
