---
id: "{{この文書を一意に識別する識別子。}}"
title: "{{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるTest Standard」を含め、読んだだけでこの仕様がテスト規約だとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}"
description: "{{この文書が何を定めているかを1〜2文で。}}"
tags: "{{この document を分類するタグ。例: [\"tier:backend\"]}}"
---

# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるTest Standard」を含め、読んだだけでこの仕様がテスト規約だとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}

## 概要

{{この文書が何を定めているかを1〜2文で。}}

---

## テスト方針

- **重心**: {{テストの重心。（選択肢: pyramid / diamond / inverted）}}

### 種別ごとの重心

| テスト種別 | 重心 |
|---|---|
| `{{テスト種別（testTypes が宣言した種別を指す）。}}` | {{その種別にどれだけ重心を置くか。none はその種別を行わないこと。}} |

### 根拠

{{その重心を選ぶ根拠。業務ロジックの実装方法から導く。}}

---

## テスト計画

| 項目 | 値 |
|---|---|
| {{計画項目名（例: 実行タイミング）}} | {{この計画項目の具体的な内容（例: 全コミット時（pre-commit））}} |

---

## テストタイプ

| テストタイプ | ツール | 対象 | 補足 |
|---|---|---|---|
| `{{共通カタログの固定語彙から選ぶ。}}` | {{使用ツール（例: pytest）。}} | {{対象のレイヤー。architecture の layers が宣言した層名をそのまま使う。複数あれば配列で並べ、1つの欄に区切り文字で詰めない。}} | {{補足（例: 要件のシナリオを見てAIが直接執筆する 等）。無ければ空。}} |

---

## フレームワーク

- **単体テスト**: {{単体テストFW（例: pytest）}}
- **受け入れテスト**: {{受け入れテストFW（仕様(要件)をこのFWでネイティブに実行可能テストとして執筆する）。無ければ空（例: pytest）}}

---

## シナリオの束ね方

### 突き合わせのキー

- **宣言行**: {{仕様のシナリオとテストを突き合わせるキーの形（例: Scenario: {シナリオ名}）。テストの文書コメントに置く。関数名を突き合わせのキーにしない——仕様の語彙をそのまま識別子にできる言語でしか成立しないため。}}
- **一意の範囲**: ['{{宣言行が一意になる範囲。シナリオ名だけでは衝突しうるので、どの組で一意かを宣言する。}}']

### シナリオ種別とテストの対応

| シナリオ種別 | レイヤー | テスト種別 |
|---|---|---|
| `{{仕様のシナリオブロックの種別。}}` | {{対応するテストのレイヤー。architecture の layers が宣言した層名を指す。}} | `{{対応するテスト種別。testTypes が宣言した種別を指す。}}` |

### 規範

| 規範 |
|---|
| {{規範の内容。}} |

---

## シナリオdocstring

- **スタイル**: {{対象言語の文書コメント構文に載せてシナリオを記載するスタイル。先頭にシナリオ宣言行を置き、続けてGiven/When/Thenを書く形を明示すること（例: テスト関数の文書コメント内に、シナリオ名の宣言行とGiven/When/Thenをそのまま記載）。言語によって文書コメントの位置が変わる点にも触れる（関数の内側に置く言語と、関数の直前に置く言語がある）}}
- **対象**: {{この転記規約を必須にする対象（例: 受け入れシナリオ・保証シナリオ・不変条件シナリオ・ドメインサービスシナリオに対応する全テスト関数）}}

### 転記の指針

{{テスト側の文書コメントに、対応する仕様のシナリオ（宣言行とGiven/When/Then）を一字一句そのまま転記する旨。転記が要るのは2つの役割を兼ねるため。宣言行は仕様とテストを突き合わせるキーそのものであり、これが無いテストはどの仕様にも対応していないものとして扱われる。本文は、シナリオ文言の事後編集への追従を検知する材料になる。}}

```
{{シナリオ宣言行とGiven/When/Thenを転記したテスト関数のコード例（対象言語の実際の構文で）。識別子は対象言語の命名慣習に従わせ、仕様の語彙をそのまま識別子にしない。}}
```

---

## テスト対象別の配置

- **置き方**: {{テストファイルの置き方。layer-first=レイヤーを第一階層にする / type-first=テスト種別を第一階層にする / mirrored=ソースの木を鏡写しにする（Java/Mavenの src/test/java 等）/ mirrored-project=別プロジェクトに置く（.NET の Foo.Tests 等）/ colocated=ソースと同じディレクトリに置く（Goの _test.go）/ in-file=実装ファイルの中に置く（Rustの #[cfg(test)]）。言語やビルドツールが形を強制する場合があるので、行ごとに上書きできる。（選択肢: layer-first / type-first / mirrored / mirrored-project / colocated / in-file）}}

| レイヤー | テスト種別 | 配置 | 置き方（個別） | 接尾辞 | 補足 |
|---|---|---|---|---|---|
| {{対象のレイヤー。architecture の layers が宣言した層名をそのまま使う。}} | `{{テスト種別。testTypes が宣言した種別をそのまま使う。}}` | `{{配置パス。説明はここに書かず note へ。strategy が colocated / in-file の行では空でよい。}}` | {{この行だけ全体の strategy と異なる場合に指定（Rustは単体が in-file、統合が別ディレクトリ）。同じなら空。}} | `{{strategy が colocated / in-file の場合のファイル名の決まり（例: _test.go）。それ以外は空。}}` | {{補足。パス欄に説明文を混ぜない。}} |

---

## 決定ルール

| 種別 | 規約 |
|---|---|
| {{この規約の強制力。必須（must、違反は許容しない）／禁止（must not、行ってはならない）／推奨（should、望ましいが例外を許容する）のいずれかを選ぶ。}} | {{実行可能な仕様の扱い・生成物の手書き禁止・テストダブルの使用方針等、テストに関する決定ルールの内容（例: モックは外部I/O境界のみに限定する）}} |

---

## テストファイルの名づけ
