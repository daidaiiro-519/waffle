# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるArchitecture仕様」を含め、読んだだけでこの仕様がアーキテクチャだとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}

## 概要

{{この文書が何を定めているかを1〜2文で。}}

---

## レイヤーと依存方向

### 様式

{{技術方式（例: ポートとアダプター（ヘキサゴナル））}}

| レイヤー | 責務 | 依存してよい先 |
|---|---|---|
| {{レイヤー名（例: domain, application, ports, inbound adapter, outbound adapter）}} | {{このレイヤーの責務を1文で（例: 外部I/O・外部ライブラリをport実装に閉じ込める）}} | {{依存してよいレイヤー名を列挙（内向きのみ）。最内層は空。}} |

---

## ディレクトリ構成

```
{{正典ディレクトリツリー（テキスト）。`{package}` 等プレースホルダを使ってよい（プロダクト非依存）。}}
```

### 合成ルート（結線・DI）

{{結線（DI）を置く場所を1文で（例: inbound adapter の起動点にのみ置く）}}

---

## 概念 → 実現形

| 概念 | 配置 | 形（決定レベル） |
|---|---|---|
| `{{DDD概念の種類を、下のenumに列挙された固定語彙（usecase/aggregate/entity/value-object/domain-service/repository/port/inbound-adapter/outbound-adapter）から選ぶ。対応するドメイン仕様側がこの概念を指すために使っているキー名と表記を一致させること（言い換えたり別名を作ったりしない）。例えば仕様側がusecaseという概念キーで扱っているものは、ここでもusecaseと書く。}}` | {{layout のツリー上の配置（相対パス）（例: application/usecases）}} | {{この概念の実現方針を決定レベルで（正確な署名は書かない）。例: エントリメソッド1つ・ドメインは port 経由で呼ぶ}} |

---

## 規約（守るべきルール）

| 種別 | 規約 |
|---|---|
| {{この規約の強制力。必須（must、違反は許容しない）／禁止（must not、行ってはならない）／推奨（should、望ましいが例外を許容する）のいずれかを選ぶ。}} | {{依存方向・DIの受け取り方・エラー伝播（結果型/例外の使い分け）等、横断的な決定ルールの内容（例: 依存は内向きのみ）}} |

---

## サブドメイン別の厚み

| Category | 実装の厚み |
|---|---|
| {{subdomain spec の Category。}} | {{実装の厚み方針を1文で（例: ドメインモデルで厚く実装する）}} |
