---
id: "{{この文書を一意に識別する識別子。}}"
title: "{{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこの画面・フローが何を示すかを業務語彙で簡潔に表す句（この文書自身の識別子をそのまま繰り返さない）。末尾に種別を表す語（画面なら「〜を示す画面」、フローなら「〜を示すフロー」）を含め、読んだだけでこのSpecがどちらの種別かがわかるようにしてください。識別子部分はこの文書自身の識別子をそのまま使う。}}"
description: "{{この画面/フローが何のために存在するかを1〜2文で（業務ユースケースの内容の繰り返しでなく、プロダクトとしての意図）。}}"
tags: "{{この Spec を分類するタグ。例: [\"context:orders\"]}}"
---

# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこの画面・フローが何を示すかを業務語彙で簡潔に表す句（この文書自身の識別子をそのまま繰り返さない）。末尾に種別を表す語（画面なら「〜を示す画面」、フローなら「〜を示すフロー」）を含め、読んだだけでこのSpecがどちらの種別かがわかるようにしてください。識別子部分はこの文書自身の識別子をそのまま使う。}}

## 概要

{{この画面/フローが何のために存在するかを1〜2文で（業務ユースケースの内容の繰り返しでなく、プロダクトとしての意図）。}}

---

## 業務ユースケースの並び

```mermaid
sequenceDiagram
    {{人間の主体なら actor（棒人間アイコン）、業務ユースケース等のシステム側なら participant。}} {{識別子。人間主体は名前（例:_顧客）、業務ユースケースはその業務ユースケースを指す識別子（例:_uc_place_order）。}} as {{表示名（任意・省略時はidそのまま）。}}
    {{この旅を辿る主体（participantsで宣言したid・例:_顧客）。}}->>{{呼び出す業務ユースケースを指す識別子（participantsで宣言した識別子と同じもの・例:_uc_place_order）。}}: {{この旅の中でこの業務ユースケースが果たす役割を1文で。}}
```

| 主体 | 業務ユースケース | この旅における役割 |
|---|---|---|
| {{この旅を辿る主体（participantsで宣言したid・例: 顧客）。}} | `{{呼び出す業務ユースケースを指す識別子（participantsで宣言した識別子と同じもの・例: uc-place-order）。}}` | {{この旅の中でこの業務ユースケースが果たす役割を1文で。}} |
