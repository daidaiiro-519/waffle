# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのインフラ仕様が何を担うかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を担うsecurity基盤」を含め、読んだだけでこの仕様がsecurityを扱うとわかるようにしてください。識別子部分はこの文書の識別子をそのまま使う。}}

## 概要

{{このコンポーネント/領域が何を担うかを1〜2文で。人の即スキャンとAIの意図把握に使う。}}

---

## コンプライアンス要件

| フレームワーク | 対象範囲 | 要件 |
|---|---|---|
| {{準拠するフレームワーク/規制名（例: PCI DSS、GDPR、SOC2）}} | {{そのフレームワークが適用される範囲（例: 決済処理を行う全コンポーネント）}} | {{満たすべき具体的な要件（例: カード番号は非保持化する）}} |

---

## アクセス制御方針

| 主体 | 許可される操作 | 条件 |
|---|---|---|
| {{主体（誰/何が）（例: CI/CDパイプライン、運用担当者ロール）}} | {{その主体に許可する操作（例: デプロイの実行、ログの閲覧）}} | {{許可の条件（例: 営業時間内のみ、多要素認証済みの場合のみ）。無ければ空。}} |

---

## データ分類ごとの境界

| データ分類 | 境界 | 要件 |
|---|---|---|
| {{データ分類（例: 個人情報、機密情報、公開情報）}} | {{越えてはならない境界（例: 特定リージョン外への転送禁止）}} | {{その境界を守るための要件（例: 保存時暗号化必須）}} |

---

## プラットフォーム操作保証シナリオ

### 背景

{{複数シナリオ共通の前提。無ければ空文字。}}

### {{シナリオ名（概要）。}}

| 分類 | 観点 |
|---|---|
| {{分類: 正常系 / 異常系 / 境界値。}} | {{観点: 何を保証するか＋検証の狙い。}} |

```gherkin
{{Given/When/Then。ドメイン語彙で書き、IaCの実装詳細は書かない。}}
```
