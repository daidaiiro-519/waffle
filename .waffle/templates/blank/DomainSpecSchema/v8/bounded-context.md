---
id: "{{この文書を一意に識別する識別子。}}"
title: "{{「業務エキスパートが理解できる日本語の説明：識別子」の形式で設定してください。説明部分はこの構成要素が何であるかを業務語彙で簡潔に表す句（識別子やクラス名をそのまま繰り返さない）。末尾に「〜を行う境界づけられたコンテキスト」を含め、読んだだけでDDDのどれにあたるかがわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う（識別子は既に別の箇所が正であり、ここでは表示のためだけに引用する。実装と乖離しても他のフィールドのように機械的ドリフト検知の対象にならないため、識別子以外の実装レベルの詳細をここに書き込まない）。}}"
description: "{{論点ごとに列挙する（1〜2項目が目安）。この文脈/業務領域/集約/ユースケースが何かを1〜2文で（ドメイン語彙のみ・技術用語禁止）。}}"
tags: "{{この Spec を分類するタグ。例: [\"context:orders\"]}}"
---

# {{「業務エキスパートが理解できる日本語の説明：識別子」の形式で設定してください。説明部分はこの構成要素が何であるかを業務語彙で簡潔に表す句（識別子やクラス名をそのまま繰り返さない）。末尾に「〜を行う境界づけられたコンテキスト」を含め、読んだだけでDDDのどれにあたるかがわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う（識別子は既に別の箇所が正であり、ここでは表示のためだけに引用する。実装と乖離しても他のフィールドのように機械的ドリフト検知の対象にならないため、識別子以外の実装レベルの詳細をここに書き込まない）。}}

## 名前

{{この境界づけられたコンテキストの名前をドメイン語彙で簡潔に（識別子やタイトルの説明文とは別に、名前だけを独立して機械的に参照できるようにする）。例: スキーマ駆動Document基盤}}

---

## 概要

- {{論点ごとに列挙する（1〜2項目が目安）。この文脈/業務領域/集約/ユースケースが何かを1〜2文で（ドメイン語彙のみ・技術用語禁止）。}}

---

## ユビキタス言語

| 用語 | 定義 |
|---|---|
| `{{用語（この文脈での呼び名）。}}` | {{その用語が指すものを1文で。}} |

---

## 構成要素

### {{メンバーの種別。下のenum（subdomain/aggregate/usecase）から選ぶ。}}

- {{その種別のidを配列で列挙。}}

---

## 文脈マップ

| 相手コンテキスト | 関係 | 内容 |
|---|---|---|
| {{相手の bounded-context 名。}} | {{関係種別（パートナー/顧客・供給/腐敗防止層 等）。}} | {{他bounded-contextと何を連係するかを1文で（例: 注文確定イベントを配送コンテキストへ連携）}} |

---

## ドメインサービス

| 業務サービス | 責務 |
|---|---|
| {{業務サービス名（例: パステンプレート解決 / 整形描画（ドメイン語彙で、他の集約に属さない業務ロジックであることが分かる名前にする））}} | {{何を計算/判定するか・どの集約に跨るか・入力→出力（何を受け何を返すか）を1文で。実装手順は書かない。}} |

---

## 業務サービスシナリオ

### 背景

{{複数シナリオ共通の前提。無ければ空文字。}}

### {{シナリオ名（概要）。}}

| 分類 | 観点 |
|---|---|
| {{分類: 正常系 / 異常系 / 境界値。}} | {{観点: 何を計算/判定するか＋検証の狙い。}} |

```gherkin
{{Given/When/Then。ドメイン語彙で書き、実装詳細は書かない。}}
```
