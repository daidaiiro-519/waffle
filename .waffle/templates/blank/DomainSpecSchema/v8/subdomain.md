---
id: "{{この文書を一意に識別する識別子。}}"
title: "{{「業務エキスパートが理解できる日本語の説明：識別子」の形式で設定してください。説明部分はこの構成要素が何であるかを業務語彙で簡潔に表す句（識別子やクラス名をそのまま繰り返さない）。末尾に「〜を担うサブドメイン」を含め、読んだだけでDDDのどれにあたるかがわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う（識別子は既に別の箇所が正であり、ここでは表示のためだけに引用する。実装と乖離しても他のフィールドのように機械的ドリフト検知の対象にならないため、識別子以外の実装レベルの詳細をここに書き込まない）。}}"
description: "{{論点ごとに列挙する（1〜2項目が目安）。この文脈/業務領域/集約/ユースケースが何かを1〜2文で（ドメイン語彙のみ・技術用語禁止）。}}"
tags: "{{この Spec を分類するタグ。例: [\"context:orders\"]}}"
---

# {{「業務エキスパートが理解できる日本語の説明：識別子」の形式で設定してください。説明部分はこの構成要素が何であるかを業務語彙で簡潔に表す句（識別子やクラス名をそのまま繰り返さない）。末尾に「〜を担うサブドメイン」を含め、読んだだけでDDDのどれにあたるかがわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う（識別子は既に別の箇所が正であり、ここでは表示のためだけに引用する。実装と乖離しても他のフィールドのように機械的ドリフト検知の対象にならないため、識別子以外の実装レベルの詳細をここに書き込まない）。}}

## 名前

{{このサブドメインの名前をドメイン語彙で簡潔に（識別子やタイトルの説明文とは別に、名前だけを独立して機械的に参照できるようにする）。例: フェーズゲート判定}}

---

## 概要

- {{論点ごとに列挙する（1〜2項目が目安）。この文脈/業務領域/集約/ユースケースが何かを1〜2文で（ドメイン語彙のみ・技術用語禁止）。}}

---

## サブドメイン分類

### 分類

{{中核=core / 補完=supporting / 一般=generic。競争優位を生むか・既製品があるかで判定。（選択肢: core / supporting / generic）}}

### 根拠

- {{根拠を独立した論点ごとに列挙する。そのカテゴリーである根拠（差別化の有無）。}}

---

## 業務ユースケース一覧

- {{この業務領域に属する usecase の id を列挙。}}

---

## 詳細設計ガイド

- {{実装方針を論点ごとに列挙する。実装方針を1〜2文で。中核=ドメインモデルで厚く/一般=既製ライブラリを薄く包む/補完=薄いトランザクションスクリプト。}}

---

## 外部解決策

{{採用する既製ライブラリ/サービス名と、それが業務上のどんな能力を代替するかを1文で。中核/補完の業務領域（既製品に頼らず自前で実装する領域）なら不要。}}
