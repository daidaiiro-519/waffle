---
id: "{{この文書を一意に識別する識別子。}}"
title: "{{「業務エキスパートが理解できる日本語の説明：識別子」の形式で設定してください。説明部分はこの構成要素が何であるかを業務語彙で簡潔に表す句（識別子やクラス名をそのまま繰り返さない）。末尾に「〜を守る集約」を含め、読んだだけでDDDのどれにあたるかがわかるようにしてください。識別子部分はその集約のルートを表す正式名と同じ値を使う（識別子は既に別の箇所が正であり、ここでは表示のためだけに引用する。実装と乖離しても他のフィールドのように機械的ドリフト検知の対象にならないため、識別子以外の実装レベルの詳細をここに書き込まない）。}}"
description: "{{論点ごとに列挙する（1〜2項目が目安）。この文脈/業務領域/集約/ユースケースが何かを1〜2文で（ドメイン語彙のみ・技術用語禁止）。}}"
tags: "{{この Spec を分類するタグ。例: [\"context:orders\"]}}"
---

# {{「業務エキスパートが理解できる日本語の説明：識別子」の形式で設定してください。説明部分はこの構成要素が何であるかを業務語彙で簡潔に表す句（識別子やクラス名をそのまま繰り返さない）。末尾に「〜を守る集約」を含め、読んだだけでDDDのどれにあたるかがわかるようにしてください。識別子部分はその集約のルートを表す正式名と同じ値を使う（識別子は既に別の箇所が正であり、ここでは表示のためだけに引用する。実装と乖離しても他のフィールドのように機械的ドリフト検知の対象にならないため、識別子以外の実装レベルの詳細をここに書き込まない）。}}

## 概要

- {{論点ごとに列挙する（1〜2項目が目安）。この文脈/業務領域/集約/ユースケースが何かを1〜2文で（ドメイン語彙のみ・技術用語禁止）。}}

---

## 集約ルート

{{ルートエンティティ名（ドメイン語彙）（例: Order）}}

### 外部参照（ID）

- {{ID で参照する他集約を列挙（オブジェクトは内部に持たない・図6-4）（例: Customer, Product）。無ければ空。}}

---

## エンティティ

### {{エンティティ名（例: Order / OrderLine）}}（集約ルート）

{{このエンティティが集約内で担う役割を1文で（例: 注文全体の整合性を保つ集約ルート）}}

| 属性 | 型 |
|---|---|
| **{{属性名（例: status / orderId）}}**（識別子） | {{型（値オブジェクト名・他エンティティ・ID）（例: OrderStatus / OrderLine[] / customerId）}} |

---

## 値オブジェクト

### {{値オブジェクト名（例: Money）}}

| 表す値 | 振る舞い |
|---|---|
| {{何を表すか。例: 通貨つきの金額}} | {{振る舞い・制約・等価判定（例: 不変）。加算は同一通貨のみ。負値不可。}} |

| 属性 | 型 |
|---|---|
| {{属性名（例: amount）}} | {{型（例: string / number / SchemaId[]）}} |

---

## 不変条件

| ルール | 守り方 | 根拠 |
|---|---|---|
| {{不変条件（「〜は常に〜」の形）（例: 注文明細の数量は常に1以上である）}} | {{静的(値/構造)=schema が守る / 動的(状態遷移など)=guard が守る。}} | - {{その不変条件が必要な業務上の根拠を、独立した論点ごとに配列で列挙}} |

---

## ライフサイクル

```mermaid
stateDiagram-v2
    {{遷移元の状態。statesで宣言済みの値の中から選ぶ。集約インスタンスがまだ存在しない新規生成コマンドの場合のみ、開始疑似状態を表す"[*]"を使う。例:_CREATED}} --> {{遷移先の状態。statesで宣言済みの値の中から選ぶ。例:_VALIDATED}}: {{この遷移を起こすコマンド。commandsで宣言済みのコマンド名の中から選ぶ（新しいコマンド名をここで作らない）。例: validate}}
```

### 遷移

| from | to | command | 条件 |
|---|---|---|---|
| {{遷移元の状態。statesで宣言済みの値の中から選ぶ。集約インスタンスがまだ存在しない新規生成コマンドの場合のみ、開始疑似状態を表す"[*]"を使う。例: CREATED}} | {{遷移先の状態。statesで宣言済みの値の中から選ぶ。例: VALIDATED}} | {{この遷移を起こすコマンド。commandsで宣言済みのコマンド名の中から選ぶ（新しいコマンド名をここで作らない）。例: validate}} | {{遷移の追加条件があれば。無ければ空。}} |

---

## コマンド

### {{コマンド名（命令形）（例: place / 確定する）}}

{{何をするか・どの不変条件を守るかを1文で（実装手順は書かない）。}}

| 前提 | 後 | 発行イベント |
|---|---|---|
| {{実行に必要な前提状態。集約インスタンスがまだ存在しない新規生成コマンドの場合は null（曖昧な文字列プレースホルダーは使わない）。}} | {{成功後の状態。この集約のlifecycle.statesで宣言済みの値の中から選ぶ（新しい状態名をここで作らない）。例: VALIDATED}} | {{発行するドメインイベント名（過去形）。無ければ空。}} |

| 引数 | 意味 |
|---|---|
| {{引数名。ドメイン語彙で（例: quantity）}} | {{この引数が何を表すかを1文で（例: 注文する数量）}} |

---

## ドメインイベント

### {{イベント名（必ず過去形）（例: OrderPlaced）}}

#### 発行契機

{{どのコマンド/契機で発行されるか。commandsで宣言済みのコマンド名の中から選ぶ。例: place}}

#### ペイロード

| 項目 | 意味 |
|---|---|
| {{ペイロード項目名（例: quantity）}} | {{このペイロード項目が何を表すかを1文で（例: 確定した数量）}} |

---

## 不変条件シナリオ

### 背景

{{複数シナリオ共通の前提（Gherkin Background 相当）。無ければ空文字。}}

### {{シナリオ名（概要）。}}

| 分類 | 観点 |
|---|---|
| {{分類: 正常系 / 異常系 / 境界値。}} | {{観点: 側面（状態遷移/計算整合/事前条件 等）＋検証の狙い。}} |

```gherkin
{{このシナリオの Given/When/Then（Scenario: 1つ・実行可能なテストとして書き起こせる形）。ドメイン語彙で書き、実装詳細（クラス名/API/SQL/UI 操作）は書かない。}}
```
