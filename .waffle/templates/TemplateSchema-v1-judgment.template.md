# {{documentId}}

## プレースフォルダー定義票

| プレースフォルダー | 記入する内容 |
|---|---|
| `{{タイトル}}` | documentId をそのまま設定してください。（JSON上のフィールド: content.title.title） |
| `{{使用条件.発動条件}}` | この骨格がいつ使われるべきかを一文で書いてください（例:「『このコードはどこに置くべきか』等の配置・依存方向に関する判断を求める相談」）。実際の相談文ではなく、発動条件の説明であることに注意。（JSON上のフィールド: content.trigger.text） |
| `{{使用条件.冒頭文}}` | 判断フローに入る前に相談対象を一文でスコープする文を{{}}プレースホルダー込みで書いてください（例:「{{対象（ユーザーが提示したシステム・機能・設計）}}について判断します。」）。（JSON上のフィールド: content.trigger.openingLine） |
| `{{前置きセクション.前提1.見出し}}` | 前置きセクションの見出し。例: サブドメイン分類（配列。この形式の行を必要な数だけ繰り返す。判断フローに入る前に確認させたい前提〔分類・階層等〕が無ければ items を空配列にし、このセクション自体を本文から省略する）（JSON上のフィールド: content.contextBlocks.items[].title） |
| `{{前置きセクション.前提1.指示文}}` | そのセクションに何を書くべきかの指示文（{{}}プレースホルダーを使う）。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.contextBlocks.items[].guidance） |
| `{{診断フロー.見出し}}` | このセクションの見出しを書いてください。骨格の性質に応じて『判断フロー』『診断フロー』『計画フロー』等、文脈に合った語を選んでよい（見出し語自体は骨格ごとに変わってよいが、直後のitemsの構造は共通）。（JSON上のフィールド: content.diagnosticFlow.title） |
| `{{診断フロー.問い1.問い文}}` | 問い文（{{}}プレースホルダーを含む）。例:「{{判断の起点となる問い}}」（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.diagnosticFlow.items[].question） |
| `{{診断フロー.問い1.分岐1.条件}}` | 分岐条件（例: YESの条件、NOの条件）。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.diagnosticFlow.items[].branches[].condition） |
| `{{診断フロー.問い1.分岐1.結果}}` | その条件のときどうなるか（次の問いへ進む旨、または結論そのもの）。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.diagnosticFlow.items[].branches[].then） |
| `{{判定}}` | 判定結果をどう提示すべきかの指示を{{}}プレースホルダー込みで書いてください。判定理由（バックボーンの定義・原則に基づく根拠）を必ず示すよう指示に含めること。（JSON上のフィールド: content.verdict.guidance） |
| `{{推奨アクション}}` | 推奨アクションをどう提示すべきかの指示を{{}}プレースホルダー込みで書いてください。動詞で始まる具体的な行動を示すよう指示に含めること。（JSON上のフィールド: content.recommendedAction.guidance） |
| `{{追加セクション.セクション1.見出し}}` | 追加セクションの見出し。例: 診断的差し戻し（該当する場合のみ）（配列。この形式の行を必要な数だけ繰り返す。判定・推奨アクションの後に必要な追加セクションが無ければ items を空配列にし、このセクション自体を本文から省略する）（JSON上のフィールド: content.extraSections.items[].title） |
| `{{追加セクション.セクション1.指示文}}` | そのセクションに何を書くべきかの指示文（{{}}プレースホルダーを使う）。（配列。この形式の行を必要な数だけ繰り返す）（JSON上のフィールド: content.extraSections.items[].guidance） |
| `{{注意}}` | アンチパターンに該当する場合に何を書くべきかの指示を{{}}プレースホルダー込みで書いてください。該当するアンチパターン名とリスクをセットで示すよう指示に含めること。（JSON上のフィールド: content.antiPatternNote.guidance） |

（`{{前置きセクション.空理由}}`〔JSON上のフィールド: content.contextBlocks.emptyReason〕/ `{{追加セクション.空理由}}`〔JSON上のフィールド: content.extraSections.emptyReason〕はitemsが空配列のときにその理由を記録するデータ専用フィールドで、本文には描画されない。items=[]にする場合は必ずこの理由も埋めること。`{{診断フロー.問い1.識別子}}`〔JSON上のフィールド: content.diagnosticFlow.items[].questionId。例: q1〕も本文には描画されない内部識別子。）

---

# {{タイトル}}

---

## 使用条件

{{使用条件.発動条件}}

{{使用条件.冒頭文}}

---

## 前置きセクション

### {{前置きセクション.前提1.見出し}}

{{前置きセクション.前提1.指示文}}

---

## {{診断フロー.見出し}}

### {{診断フロー.問い1.問い文}}

| 条件 | 結果 |
|---|---|
| {{診断フロー.問い1.分岐1.条件}} | {{診断フロー.問い1.分岐1.結果}} |

---

## 判定

{{判定}}

---

## 推奨アクション

{{推奨アクション}}

---

## 追加セクション

### {{追加セクション.セクション1.見出し}}

{{追加セクション.セクション1.指示文}}

---

## 注意（アンチパターンに該当する場合のみ）

{{注意}}
