# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるCoding Standard」を含め、読んだだけでこの仕様がコーディング規約だとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}

## 概要

{{この文書が何を定めているかを1〜2文で。}}

---

## 命名

| 対象 | 規約 |
|---|---|
| {{命名の対象（例: モジュール / ファイル）}} | {{命名規約（例: スネークケース）}} |

---

## スタイル

| 種別 | 規約 |
|---|---|
| {{この規約の強制力。必須（must、違反は許容しない）／禁止（must not、行ってはならない）／推奨（should、望ましいが例外を許容する）のいずれかを選ぶ。}} | {{型注釈・関数の責務範囲・import規律等、コードスタイルに関する規約の内容（例: 全ての公開関数に型注釈を付ける）}} |

---

## docstring

- **スタイル**: {{言語標準の docstring スタイル（例: Google スタイル docstring）}}
- **対象**: {{docstring を必須にする対象（例: 公開要素（module / 公開 class / 公開 function））。private は任意。}}
- **パラメータ等の構文**: {{このdocstringスタイルにおいて、パラメータ・戻り値・例外を表現する構文そのもの（タグ名と書式）。言語ごとに異なる構文を、その言語の書き方でここに明記する。例: Google styleなら「Args: name (type): 説明 / Returns: 説明 / Raises: ExceptionType: 説明」、TSDocなら「@param name - 説明 / @returns 説明 / @throws {ErrorType} 説明」、Javadocなら「@param name 説明 / @return 説明 / @throws ExceptionType 説明」。}}

### 要約行の書き方

{{要約行（1行目）の書き方の指針。「何をするか・いつ使うか」を検索と判断に効く語で、の旨。}}

```
{{このスタイルに従った docstring のコード例（要約行＋本文＋paramSyntaxで明記した構文を使ったパラメータ/戻り値/例外の記載・カスタムタグなし）。}}
```

---

## 決定ルール

| 種別 | 規約 |
|---|---|
| {{この規約の強制力。必須（must、違反は許容しない）／禁止（must not、行ってはならない）／推奨（should、望ましいが例外を許容する）のいずれかを選ぶ。}} | {{docstring必須の範囲・コードから導出できる情報を書かない・仕様と異なる語彙で命名しない等、docstring/コメントに関する規約の内容（例: 公開関数のdocstringは要約行＋Args/Returnsを持つ）}} |

