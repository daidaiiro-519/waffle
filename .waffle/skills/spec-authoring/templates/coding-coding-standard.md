---
id: "{{この文書を一意に識別する識別子。}}"
title: "{{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるCoding Standard」を含め、読んだだけでこの仕様がコーディング規約だとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}"
description: "{{この文書が何を定めているかを1〜2文で。}}"
tags: "{{この document を分類するタグ。例: [\"tier:backend\"]}}"
---

# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるCoding Standard」を含め、読んだだけでこの仕様がコーディング規約だとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}

## 概要

{{この文書が何を定めているかを1〜2文で。}}

---

## 命名

### ファイル名

- **由来**: {{実装ファイル名を何から導くか。type=型名から / module=モジュール名から / none=独立に付ける。Javaのようにファイル名と公開型名の一致が言語仕様で要求されるなら type。（選択肢: none / type / module）}}
- **変換**: {{導出時の変換。derivedFrom=type かつ言語がそのまま一致を要求するなら identity、Pythonのようにスネークへ変換する慣習なら pascal-to-snake。derivedFrom=none なら空。（選択肢:  / identity / pascal-to-snake / pascal-to-kebab / lower）}}
- **拡張子**: {{実装ファイルの拡張子（例: .py / .java / .ts）。}}

### 表記

| 対象 | 可視性 | 表記 | 接頭辞 |
|---|---|---|---|
| `{{名づける対象の種別。}}` | {{可視性で表記が変わる言語（Goは先頭の大小文字が可視性そのもの）でのみ指定。変わらないなら空。}} | {{表記。}} | `{{接頭辞で表す場合のみ（例: Pythonの非公開 _）。}}` |

### 規範

| 適用先 | 規範 |
|---|---|
| {{適用先。層名は architecture の layers が宣言したものをそのまま使い、ここで別途定義しない。層でない対象（語彙 等）はその呼び名を書く。}} | {{規範の内容。}} |

---

## スタイル

| 種別 | 規約 |
|---|---|
| {{この規約の強制力。必須（must、違反は許容しない）／禁止（must not、行ってはならない）／推奨（should、望ましいが例外を許容する）のいずれかを選ぶ。}} | {{型注釈・関数の責務範囲・import規律等、コードスタイルに関する規約の内容（例: 全ての公開関数に型注釈を付ける）}} |

---

## docstring

- **スタイル**: {{言語標準の docstring スタイル名（例: Google スタイル docstring / Javadoc / TSDoc / Go Doc コメント）。}}
- **構文の種類**: {{パラメータ・戻り値・例外の表し方。tagged=タグで区切る（Args:/@param 等）/ prose=タグを持たない平文（Goのドキュメントコメント）。prose の場合は条項を数えられないため、規約由来テストの単位を別に決める。（選択肢: tagged / prose）}}
- **パラメータ**: {{syntaxKind=tagged のとき、パラメータを表すタグ（例: Args: / @param）。prose なら空。}}
- **戻り値**: {{syntaxKind=tagged のとき、戻り値を表すタグ（例: Returns: / @returns）。prose なら空。}}
- **例外**: {{syntaxKind=tagged のとき、送出する例外を表すタグ（例: Raises: / @throws）。prose なら空。}}
- **要約行の決まり**: {{syntaxKind=prose のとき、要約行の書き出しの決まり（例: identifier=識別子名で始める）。tagged なら空。}}

### 必須とする対象

| 対象 | 可視性 | 必須 |
|---|---|---|
| `{{対象の種別。}}` | {{可視性。any は可視性を問わない。}} | {{docstring を必須とするか。}} |

### 要約行の書き方

{{要約行（1行目）の書き方の指針。「何をするか・いつ使うか」を検索と判断に効く語で、の旨。}}

```
{{このスタイルに従った docstring のコード例。架空の汎用業務例にする。}}
```

---

## 決定ルール

| 種別 | 規約 |
|---|---|
| {{この規約の強制力。必須（must、違反は許容しない）／禁止（must not、行ってはならない）／推奨（should、望ましいが例外を許容する）のいずれかを選ぶ。}} | {{docstring必須の範囲・コードから導出できる情報を書かない・仕様と異なる語彙で命名しない等、docstring/コメントに関する規約の内容（例: 公開関数のdocstringは要約行＋Args/Returnsを持つ）}} |
