# {{「業務エキスパートが理解できる日本語の説明：識別子」の形式で設定してください。説明部分はこの構成要素が何であるかを業務語彙で簡潔に表す句（識別子やクラス名をそのまま繰り返さない）。この構成要素の種類が境界づけられたコンテキスト／サブドメイン／集約のいずれかである場合は、説明の末尾にその種類を表す語（境界づけられたコンテキストなら「〜を行う境界づけられたコンテキスト」、サブドメインなら「〜を担うサブドメイン」、集約なら「〜を守る集約」）を含め、読んだだけでこの構成要素がDDDのどれにあたるかがわかるようにしてください（ユースケースの場合はこの種別語を付けない）。識別子部分は構成要素の種類に応じて設定する: ユースケースの場合はその操作を表す正式名と同じ値、集約の場合はその集約のルートを表す正式名と同じ値、境界づけられたコンテキスト／サブドメインの場合はこの文書自身の一意な識別子をそのまま使う（識別子は既に別の箇所が正であり、ここでは表示のためだけに引用する。実装と乖離しても他のフィールドのように機械的ドリフト検知の対象にならないため、識別子以外の実装レベルの詳細をここに書き込まない）。}}

## 名前

{{このサブドメインの名前をドメイン語彙で簡潔に（識別子やタイトルの説明文とは別に、名前だけを独立して機械的に参照できるようにする）。例: フェーズゲート判定}}

---

## 概要

- {{論点ごとに列挙する（1〜2項目が目安）。この文脈/業務領域/集約/ユースケースが何かを1〜2文で（ドメイン語彙のみ・技術用語禁止）。}}

---

## サブドメイン分類

### 分類

{{中核=core / 補完=supporting / 一般=generic。競争優位を生むか・既製品があるかで判定。（選択肢: core / supporting / generic）}}

### 根拠

- {{根拠を独立した論点ごとに列挙する。そのカテゴリーである根拠（差別化の有無）。}}

---

## 業務ユースケース一覧

- {{この業務領域に属する usecase の id を列挙。}}

---

## 詳細設計ガイド

- {{実装方針を論点ごとに列挙する。実装方針を1〜2文で。中核=ドメインモデルで厚く/一般=既製ライブラリを薄く包む/補完=薄いトランザクションスクリプト。}}

---

## 外部解決策

{{採用する既製ライブラリ/サービス名と、それが @stack のどの能力に当たるか。中核/補完なら不要。}}

