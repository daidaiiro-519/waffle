# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのインフラ仕様が何を担うインフラかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に対象インフラ種別を表す語を「〜を担う{種別}基盤」のように含め（例: 種別がcomputeなら「〜を担うcompute基盤」）、読んだだけでこの仕様がどの種別のインフラを扱うかがわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}

## 概要

{{このコンポーネント/領域が何を担うかを1〜2文で。人の即スキャンとAIの意図把握に使う。}}

---

## {{このブロックの見出し。文脈に応じた表示名にする。例: 容量要件、デプロイ先要件、推論サービング目標、クエリ性能目標}}

| 指標 | 目標値 | 根拠 |
|---|---|---|
| {{指標名。例: 同時接続数、スループット、レイテンシp99}} | {{目標値。例: 1000 req/s、p99 200ms以内}} | {{その目標値が必要な事業/業務側の根拠。例: ピーク時のキャンペーン流入に耐えるため}} |

---

## リリース/環境昇格方針

| 環境 | 昇格基準 | 承認要件 |
|---|---|---|
| {{環境名。ReleasePipeline.stagesのlabelと対応させる。例: staging}} | {{次の環境へ昇格するための基準。例: 自動テスト全件成功、カナリアリリースでエラー率が閾値以下}} | {{昇格に必要な承認。例: テックリードの承認必須。不要なら空。}} |

---

## リリースパイプライン

```mermaid
flowchart LR
    {{環境段階の識別子（transitionsのfrom/toで使う）。例:_staging}}["{{環境段階の表示名。例: ステージング環境}}"]
    {{遷移元の環境id（stagesで宣言済みのものを使う）。}} -->|"{{昇格条件の要約。例: 自動テスト成功後}}"| {{遷移先の環境id（stagesで宣言済みのものを使う）。}}
```

---

## プラットフォーム操作保証シナリオ

### 背景

{{複数シナリオ共通の前提。無ければ空文字。}}

### {{シナリオ名（概要）。}}

| 分類 | 観点 |
|---|---|
| {{分類: 正常系 / 異常系 / 境界値。}} | {{観点: 何を保証するか＋検証の狙い。}} |

```gherkin
{{Given/When/Then。ドメイン語彙で書き、IaCの実装詳細は書かない。}}
```

