# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこのコーディング仕様が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に対象種別を表す語（技術スタックなら「〜を定めるTech Stack仕様」、アーキテクチャなら「〜を定めるArchitecture仕様」、コーディング規約なら「〜を定めるCoding Standard」、テスト規約なら「〜を定めるTest Standard」）を含め、読んだだけでこの仕様がどの種別かがわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}

## 概要

{{この文書が何を定めているかを1〜2文で。}}

---

## テスト方針

- **方針**: {{重心の方針。例: ピラミッド形（単体テストを重視・統合はやや軽め・E2E は行わない）}}
- **根拠**: {{その方針を選ぶ根拠。例: 業務ロジックの実装方法＝ドメインモデル}}

---

## テスト計画

| 項目 | 値 |
|---|---|
| {{計画項目名。例: 実行タイミング}} | {{この計画項目の具体的な内容。例: 全コミット時（pre-commit）}} |

---

## テストタイプ

| テストタイプ | ツール | 対象 |
|---|---|---|
| `{{共通カタログの固定語彙から選ぶ。}}` | {{使用ツール。例: pytest}} | {{対象。例: domain / application}} |

---

## フレームワーク

- **単体テスト**: {{単体テストFW。例: pytest}}
- **受け入れテスト**: {{受け入れテストFW（仕様(要件)をこのFWでネイティブに実行可能テストとして執筆する）。無ければ空。例: pytest}}

---

## シナリオの束ね方

| 項目 | 規約 |
|---|---|
| {{項目名。例: 対応関係}} | {{実行可能な仕様とテストの対応関係・ドリフト検知方法に関する規約の内容。例: 1シナリオ=1テスト関数、シナリオ名をsanitizeした名前をtest_接頭辞付きで一致させる}} |

---

## シナリオdocstring

- **スタイル**: {{対象言語のdocstring/コメント構文に載せてGherkinを記載するスタイル。例: pytestの三重引用符docstring内にGiven/When/Thenをそのまま記載}}
- **対象**: {{この転記規約を必須にする対象。例: acceptanceScenarios/guaranteeScenarios/invariantScenarios/domainServiceScenariosに対応する全テスト関数}}

### 転記の指針

{{docstring本文には、対応するspec（DomainSpecSchema）のシナリオ（Given/When/Then）を一字一句そのまま転記する旨。関数名の一致だけでは、シナリオ文言の事後編集への追従を検知できないため。}}

```
{{Given/When/Thenを転記したテスト関数のコード例（対象言語の実際の構文で）。}}
```

---

## テスト対象別の配置

| 対象 | テスト種別 | 配置 |
|---|---|---|
| {{テスト対象。例: domain}} | {{テスト種別。TestTypesBlock.testTypeに対応する種別を書く（人間可読な表記でよい）。例: 単体（unit）}} | `{{配置パス。例: tests/domain/}}` |

---

## 決定ルール

| 種別 | 規約 |
|---|---|
| {{この規約の強制力。必須（must、違反は許容しない）／禁止（must not、行ってはならない）／推奨（should、望ましいが例外を許容する）のいずれかを選ぶ。}} | {{実行可能な仕様の扱い・生成物の手書き禁止・テストダブルの使用方針等、テストに関する決定ルールの内容。例: モックは外部I/O境界のみに限定する}} |

