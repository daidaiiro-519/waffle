---
id: "{{この文書を一意に識別する識別子。}}"
title: "{{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこの契約が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるTool Integration仕様」を含め、読んだだけでこの仕様がツール連携契約だとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}"
description: "{{この文書が何を定めているかを1〜2文で。}}"
tags: "{{この document を分類するタグ。例: [\"tier:backend\"]}}"
---

# {{「業務語彙の説明：識別子」の形式で設定してください。説明部分はこの契約が何を定めるかを業務語彙で簡潔に表す句（識別子をそのまま繰り返さない）。末尾に「〜を定めるTool Integration仕様」を含め、読んだだけでこの仕様がツール連携契約だとわかるようにしてください。識別子部分はこの文書自身の一意な識別子をそのまま使う。}}

## 概要

{{この文書が何を定めているかを1〜2文で。}}

---

## 対象ツールと成果物種別

- **ツール**: {{この契約が対象とするAIコーディングツール名（kebab-case）。例: claude-code, codex, github-copilot, kiro}}
- **成果物種別**: {{このツールが要求する成果物の種別。固定enumではなく自由記述（新しい種別の発見のたびにschema変更を要さないため）。既知の例: Hook, Skill, Agent(subagent), Agent(orchestrator), Instructions, PromptTemplate, ChatMode, Spec, MCPConfig, Plugin（Pluginは複数の下位artifactKindを束ねる複合種別のため、このkindのスコープ外——rulesブロックにその旨を記載する）。}}
- **公式ドキュメント**: {{この契約の一次情報源となる公式ドキュメントのURL。無ければ空文字。}}

---

## 成果物の構造契約

{{成果物全体の構造を1〜3文で。}}

| フィールド | 必須度 | 条件 | 説明 |
|---|---|---|---|
| `{{フィールド名（例: hookSpecificOutput.hookEventName）。}}` | {{必須度。conditional の場合は condition に条件を書く。}} | {{requirement=conditional のときの条件（例: hookEventName == PreToolUse）。それ以外は空。}} | {{このフィールドの意味・値の形。}} |

---

## 配線方法

### {{配置先のパスパターン（例: .claude/settings.json, .kiro/specs/{feature}/requirements.md）}}

- **配線方式**: {{配線方式。directory-convention=規約に沿ったディレクトリ配置のみで自動検出される／registration-array=中央の配列へ明示登録が必要／config-entry=1設定ファイル内に複数エントリとして登録／single-file=リポジトリ内の単一ファイルとして配置（登録不要）。下のenumから選ぶ。}}

#### 補足

{{この配置エントリの補足（例: 複数サーバー定義がこの1ファイルにエントリとして入る、等）。無ければ空文字。}}

---

## 規約・落とし穴

| 種別 | 規約 |
|---|---|
| {{この規約の強制力。必須（must、違反は許容しない）／禁止（must not、行ってはならない）／推奨（should、望ましいが例外を許容する）のいずれかを選ぶ。}} | {{規約・落とし穴の内容。}} |
