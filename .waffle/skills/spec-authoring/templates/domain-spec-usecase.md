---
id: "{{この文書を一意に識別する識別子。}}"
title: "{{「業務エキスパートが理解できる日本語の説明：識別子」の形式で設定してください。説明部分はこの構成要素が何であるかを業務語彙で簡潔に表す句（識別子やクラス名をそのまま繰り返さない）。種別を表す語（〜を守る集約 等）は付けない。識別子部分はその操作を表す正式名と同じ値を使う（識別子は既に別の箇所が正であり、ここでは表示のためだけに引用する。実装と乖離しても他のフィールドのように機械的ドリフト検知の対象にならないため、識別子以外の実装レベルの詳細をここに書き込まない）。}}"
description: "{{論点ごとに列挙する（1〜2項目が目安）。このusecaseが達成する業務上の操作を1〜2文で（ドメイン語彙のみ・技術用語禁止）。actorIntentが「誰が・何を達成したいか」を書く場所であるのに対し、ここはその操作自体を第三者向けに要約する場所（両者を混同しない）。}}"
tags: "{{この Spec を分類するタグ。例: [\"context:orders\"]}}"
---

# {{「業務エキスパートが理解できる日本語の説明：識別子」の形式で設定してください。説明部分はこの構成要素が何であるかを業務語彙で簡潔に表す句（識別子やクラス名をそのまま繰り返さない）。種別を表す語（〜を守る集約 等）は付けない。識別子部分はその操作を表す正式名と同じ値を使う（識別子は既に別の箇所が正であり、ここでは表示のためだけに引用する。実装と乖離しても他のフィールドのように機械的ドリフト検知の対象にならないため、識別子以外の実装レベルの詳細をここに書き込まない）。}}

## 概要

- {{論点ごとに列挙する（1〜2項目が目安）。このusecaseが達成する業務上の操作を1〜2文で（ドメイン語彙のみ・技術用語禁止）。actorIntentが「誰が・何を達成したいか」を書く場所であるのに対し、ここはその操作自体を第三者向けに要約する場所（両者を混同しない）。}}

---

## 存在意義

- {{存在しなかった場合に起きる問題・防いでいるリスクを、独立した論点ごとに列挙する。このusecaseが存在しなかった場合に起きる問題、または防いでいるリスクを1〜2文で。メカニズム（何をするか）ではなく目的（なぜ必要か）を書く。actorIntent.intentが「何を達成したいか」を書く場所であるのに対し、ここは「それがなぜ業務上重要か」を書く場所（両者を混同しない）。}}

---

## 主アクターと意図

### 主アクター

{{主アクター（業務上の役割）（例: 顧客）}}

### 意図

{{何を達成したいか（1文）（例: カートの商品を注文として確定する）}}

---

## 操作一覧

| 操作 | 概要 |
|---|---|
| `{{操作の識別子（例: get_block）。実装のoperation分岐の文字列と完全一致させる。}}` | {{この操作が何をするかの一文説明（業務語彙で）。}} |

---

## 関与する外部

- {{関与する副アクター・隣接コンテキストを列挙（同じ外部システム＝業務領域の境界）。無ければ空。}}

---

## 事前条件

- {{操作開始前に成立している必要がある条件を列挙。無ければ空。}}

---

## 基本フロー

```mermaid
sequenceDiagram
    {{人間/主アクターなら actor、集約等のシステム側なら participant。}} {{識別子。stepsのfrom/toと同じ表記に揃える。}}
    {{送り手（ドメインの役者）。}}->>{{受け手（ドメインの役者）。event_の場合は空でよい。}}: {{メッセージ（コマンド/返り値/イベント名）。}}
```

---

## 事後条件

- {{成功時に成立する結果（集約の状態変化・発行イベント）を列挙。}}

---

## 受け入れ基準

- {{受け入れ条件を EARS で列挙（When/While/If … shall …）。一意・検証可能・ドメイン語彙のみ。}}

---

## 操作保証

- {{保証をEARSで列挙（When/While/If … shall …）。書いてよいのは『何を保証するか』だけ（べき等性・一貫性・提供チャネルの一貫性等）。『どう実現するか』（具体的なDB技法・ロック機構・実装技術）は実装方針の担当領域であり、ここには書かない。判定基準（このusecaseに書くか、対象集約の不変条件に書くか）: 同じ資源を操作する複数usecaseの間で、この保証内容が重複するか？ 重複するなら対象集約側の不変条件として書く。このusecase固有の業務理由でのみ必要な保証だけをここに書く。性能の生数値（応答時間・スループット等）は非機能テストの担当領域であり、ここには書かない。}}

---

## エラー

| コード | 条件 |
|---|---|
| `{{エラーコード（例: OUT_OF_STOCK）}}` | - {{そのエラーコードが返る条件を、独立した論点ごとに配列で列挙。1論点なら1要素でよい。例: ["対象ファイルが存在しない"]}} |

---

## 受け入れシナリオ

### 背景

{{複数シナリオ共通の前提（Gherkin Background 相当）。無ければ空文字。}}

### {{シナリオ名（概要）。}}

| 分類 | 観点 |
|---|---|
| {{分類: 正常系 / 異常系 / 境界値。}} | {{観点: 側面（状態遷移/計算整合/事前条件 等）＋検証の狙い。}} |

```gherkin
{{このシナリオの Given/When/Then（Scenario: 1つ・実行可能なテストとして書き起こせる形）。ドメイン語彙で書き、実装詳細（クラス名/API/SQL/UI 操作）は書かない。}}
```

---

## 操作保証シナリオ

### 背景

{{複数シナリオ共通の前提。無ければ空文字。}}

### {{シナリオ名（概要）。}}

| 分類 | 観点 |
|---|---|
| {{分類: 正常系 / 異常系 / 境界値。}} | {{観点: べき等性/一貫性/提供チャネルの一貫性 等＋検証の狙い。}} |

```gherkin
{{Given/When/Then。ドメイン語彙で書き、実装詳細は書かない。}}
```
