# パステンプレートと値から実パスを導く：ds-resolve-path-template

## 概要

- Schema が宣言するパステンプレートと、Document が持つ値の両方を読んで、実パスへ解決する。
- 逆に、実パスとパステンプレートから変数の値を取り出す逆解析も担う。

---

## 存在意義

- パステンプレートを宣言しているのは Schema 集約で、変数に入る値を持っているのは Document 集約である。どちらか一方だけでは実パスが定まらないため、どちらの内側にも置けない。
- 解決も逆解析も、Schema 集約と Document 集約のどちらの状態も変えない。読み取った時点の値で決まればよく、両者が同時に最新である必要が無いため、強い一貫性を要求しない。
- 実パスが変わるのは、パステンプレートか変数の値のどちらかが変わったときだけで、その変更はそれぞれの集約の内側で完結する。またがるのは読み取りだけである。

---

## 参照する集約

| 集約 | 触れ方 | 何のために |
|---|---|---|
| agg-schema | 参照のみ | パステンプレートと、そこに現れる変数の並びを読む。 |
| agg-document | 参照のみ | パステンプレートの変数に入る値を読む。 |

---

## 入力と出力

### 受け取るもの

| 名前 | 意味 |
|---|---|
| パステンプレート | 値で埋める変数を含んだ、パスの書き方の型。 |
| 変数の値 | パステンプレートの変数それぞれに対応する値。解決のときに与える。 |
| 実パス | 変数がすべて埋まったパス。逆解析のときに与える。 |

### 返すもの

| 名前 | 意味 |
|---|---|
| 実パス | 解決の結果。変数がすべて値に置き換わり、ひとつに定まったパス。 |
| 変数の値 | 逆解析の結果。実パスから取り出した、変数それぞれの値。 |

### 決められない入力

- パステンプレートが要求する変数のうち、値が与えられていないものがあるとき。
- 逆解析しようとした実パスが、パステンプレートの区切り構造と一致しないとき。

---

## 受け入れ基準

| 基準 |
|---|
| When パステンプレートと、その変数すべてに対応する値が与えられたとき、システムは変数を値に置き換えた実パスをひとつ返す shall。 |
| When パステンプレートと、そのパステンプレートから解決された実パスが与えられたとき、システムは解決に使った値と同じ値を逆解析で返す shall。 |
| If 与えられた実パスがパステンプレートの区切り構造と一致しないとき、システムは逆解析を失敗として返す shall。 |

---

## 受け入れシナリオ

### パステンプレートの変数を値に置き換える

| 分類 | 観点 |
|---|---|
| 正常系 | 解決：変数がすべて埋まるとき実パスがひとつに定まるか |

```gherkin
Scenario: パステンプレートの変数を値に置き換える
  Given 変数を含むパステンプレートと、その変数すべてに対応する値
  When 実パスへ解決する
  Then すべての変数が値に置き換わった実パスがひとつ返る
```

### 実パスから変数の値を取り出す

| 分類 | 観点 |
|---|---|
| 正常系 | 逆解析：解決の逆として同じ値を取り出せるか |

```gherkin
Scenario: 実パスから変数の値を取り出す
  Given パステンプレートと、そのパステンプレートから解決された実パス
  When 逆解析する
  Then 解決に使った値と同じ値が返る
```

### 区切り構造の合わない実パスは逆解析できない

| 分類 | 観点 |
|---|---|
| 異常系 | 逆解析：構造の合わない入力を決められないものとして扱えるか |

```gherkin
Scenario: 区切り構造の合わない実パスは逆解析できない
  Given パステンプレートの区切り構造と一致しない実パス
  When 逆解析する
  Then 逆解析は失敗する
```
