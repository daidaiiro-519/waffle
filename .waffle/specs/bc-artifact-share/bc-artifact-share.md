---
id: "bc-artifact-share"
type: "bounded-context"
title: "作った文書を渡した相手に見てもらいコメントを受け取ることを行う境界づけられたコンテキスト：bc-artifact-share"
description: "作った文書を、渡した相手に見てもらい、その場でコメントを受け取るための文脈。文書を作った側は誰に見せるかを決め、見る側は名乗るだけで意見を残せる。 この文脈では、共有される文書の中身を解釈しない。何が書かれているかではなく、誰が見られるか・いつまで見られるか・どんなコメントが集まったかだけを扱う。"
tags: ["context:artifact-share"]
schemaRef: "DomainSpecSchema/v8"
---

# 作った文書を渡した相手に見てもらいコメントを受け取ることを行う境界づけられたコンテキスト：bc-artifact-share

## 概要

- 作った文書を、渡した相手に見てもらい、その場でコメントを受け取るための文脈。文書を作った側は誰に見せるかを決め、見る側は名乗るだけで意見を残せる。
- この文脈では、共有される文書の中身を解釈しない。何が書かれているかではなく、誰が見られるか・いつまで見られるか・どんなコメントが集まったかだけを扱う。

---

## ユビキタス言語

| 用語 | 定義 |
|---|---|
| `文書` | 投稿者が手元で作り、これから公開しようとしているもの。公開されると共有アーティファクトになる。 |
| `共有アーティファクト` | 誰かに見てもらうために公開された、1つの完結した文書。この文脈では中身を解釈せず、公開されている一塊として扱う。 |
| `中身` | 共有アーティファクトとして公開された文書そのもの。公開後は書き換えられず、丸ごと入れ替えることだけができる。 |
| `ArtifactId` | 共有アーティファクト1件を指す識別子。共有URLに現れ、公開してから消えるまで変わらない。 |
| `共有URL` | 閲覧者に渡す、共有アーティファクトを開くための場所。差し替えでも閲覧トークンの入れ替えでも変わらない。 |
| `隔離された枠` | 中身が表示される領域。中身はこの枠の外にあるもの（閲覧トークンや他の共有アーティファクト）に手を伸ばせない。 |
| `アーティファクト閲覧トークン` | 共有アーティファクト1件を開くために閲覧者へ渡すもの。1件につき複数を同時に持てる。発行された瞬間に一度だけ示され、以後は見返せない。それぞれが名前と期限を持ち、個別に無効にできる。期限は必ずあり、発行した時点から最長1ヶ月を超えない。 |
| `プロジェクト` | 複数の共有アーティファクトをまとめて見せるための単位。1つの共有アーティファクトが複数のプロジェクトに属してよい。 |
| `閲覧トークン` | アーティファクト閲覧トークンとプロジェクト閲覧トークンの総称。どちらを指すかが文脈で定まるときに使う。 |
| `プロジェクト閲覧トークン` | プロジェクトに入っている共有アーティファクトをまとめて開くために閲覧者へ渡すもの。アーティファクト閲覧トークンと同じ仕組みで、複数を同時に持て、名前と期限を持ち、個別に無効にできる。異なるのは期限を設けないことも選べる点で、置いておく場として使うため。個々の共有アーティファクトを公開停止すると、そのアーティファクトはこの閲覧トークンでも開けない。 |
| `プロジェクトの持ち主` | そのプロジェクトを作った投稿者。閲覧トークンの発行・無効化、公開停止・再開を行えるのは、この人と管理者だけ。 |
| `共有プロジェクト` | 招かれた投稿者なら誰でも、自分の共有アーティファクトを出し入れできるプロジェクト。作るときに選ぶ。 |
| `個人プロジェクト` | 持ち主だけが共有アーティファクトを出し入れできるプロジェクト。作るときに選ぶ。 |
| `所属` | 共有アーティファクトがプロジェクトに入っている状態。人が明示的に選んだときにだけ成立し、中身に書かれた情報では決して変わらない。 |
| `閲覧者` | 閲覧トークンを渡されて共有アーティファクトを見る人。名乗るだけでよく、あらかじめ登録されている必要はない。 |
| `投稿者` | 共有アーティファクトを公開する人。あらかじめ招かれた者に限られ、自分が公開したものだけを扱える。 |
| `合言葉` | 投稿者が自分であることを示すために使う、本人だけが知るもの。招かれた人が初めて入るときに自分で決め、管理者にも見えない。 |
| `確認コード` | 合言葉を忘れた人の宛先へ送られる、一度きりの短い文字列。これを添えたときだけ新しい合言葉を決められる。 |
| `決め直し` | 合言葉を新しいものへ置き換えること。初めて入るときと、忘れたときに行う。それまでの合言葉は使えなくなる。 |
| `管理者` | 投稿者のうち、全員の共有アーティファクトを一覧・公開停止でき、閲覧トークンを発行・無効化でき、投稿者を招いたり外したりできる者。他人の中身の差し替えはできない（集まったコメントが何に対するコメントかを、投稿者の知らないうちに変えてしまうため）。 |
| `投稿者の名簿` | 誰が招かれているかを持つ、この文脈の外にある仕組み。招待と外すことは、この名簿への操作として行われる。 |
| `招待` | 管理者が投稿者を増やすこと。自分で名乗り出て投稿者になる経路は無い。 |
| `引き継ぎ` | 共有アーティファクトの投稿者を別の投稿者へ移すこと。管理者だけが行える。投稿者が抜けたあとも、残った者が公開停止や閲覧トークンの発行・無効化を行えるようにする。 |
| `コメント` | 閲覧者が共有アーティファクトに書き残すもの。名前・本文・判定を持ち、他のコメントへの返信として残すこともできる。 |
| `判定` | コメントに添える3つの選択肢のうちひとつ。ただの意見か、この方向でよいか、直してほしいか。 |
| `差し替え` | 共有アーティファクトの中身だけを新しいものに入れ替えること。共有URLも閲覧トークンも、それまでに集まったコメントも保たれる。 |
| `区切りの記録` | 差し替えが行われた時点にコメントの並びへ残される印。これより前の指摘が、差し替え前の中身に対するものであることを読み取れるようにする。 |
| `公開停止` | 共有アーティファクトを開けない状態にすること。中身もコメントも消えず、あとから再公開できる。 |
| `再公開` | 公開停止した共有アーティファクトを再び開けるようにすること。止める前の閲覧トークンは、期限内で無効にされていなければそのまま使える。 |
| `識別のための情報` | 共有アーティファクトに添えられる、題名・要約・種別・分類の目印のひとまとまり。文書の中に書かれていれば取り出して控え、書かれていなければ題名だけを投稿者に尋ねる。 |
| `分類の目印` | 共有アーティファクトに付けられる短い語。探しやすくするためだけのもので、誰が見られるかには一切影響しない。 |
| `取り出し` | 共有アーティファクトの中身とコメントを、手元へまとめて持ち出すこと。公開を続けたままでも、公開停止の前でも行える。 |
| `配布先` | 1本の閲覧トークンを渡す相手のひとまとまり。人数は問わない。閲覧トークン1本がこの単位に対応し、配布先ごとに独立して外せる。 |
| `閲覧トークンの名前` | 1本の閲覧トークンがどの配布先のものかを表す短い名前。どれを無効にするかを公開した人が選ぶための手がかりであり、1つの対象の中で有効なものどうしは重ならない。共有アーティファクトの表示名とは別のものを指す。 |
| `表示名` | 共有アーティファクトそのものにつける題名。一覧で見分けるために使う。閲覧トークンの名前とは別のものを指す。 |
| `無効化` | 閲覧トークン1本を使えなくすること。公開停止と違い、同じ対象の他の閲覧トークンには及ばない。一度無効にすると元へは戻らない。 |

---

## 構成要素

### サブドメイン

- sd-artifact-sharing

### 集約

- agg-shared-artifact
- agg-project
- agg-comment

### 業務ユースケース

- uc-publish-artifact
- uc-replace-content
- uc-reissue-view-token
- uc-suspend-artifact
- uc-resume-artifact
- uc-assign-to-project
- uc-post-comment
- uc-invite-publisher
- uc-transfer-artifact
- uc-sign-in
- uc-reset-password
- uc-create-project
- uc-control-project-access
- uc-export-artifact
- uc-read-comments

---

## 文脈マップ

| 相手コンテキスト | 関係 | 内容 |
|---|---|---|
| bc-waffle | 公開された言語 | 共有アーティファクトに添えられた識別子・種別・題名・要約・分類の目印を、双方が同じ形式で読み書きする。この文脈はその値の意味を解釈せず、そのまま保持するだけに留める |

---

## ドメインサービス

| 業務サービス | 責務 |
|---|---|
| 閲覧可否の判定 | ある閲覧トークンで、ある共有アーティファクトを開けるかを判定する。共有アーティファクト自身が公開されていること、渡された閲覧トークンが無効にされておらず期限を過ぎていないこと、そしてその閲覧トークンが対象を開いてよいものであること（その共有アーティファクト自身のものか、その共有アーティファクトが入っているプロジェクトのものか）を、すべて見る。どれか一つだけでは答えを出せないため、集約の内側では判定できない。閲覧トークンと対象を受け取り、開けるかどうかを返す。 |

---

## 業務サービスシナリオ

### 背景

共有アーティファクトAが公開されており、プロジェクトPに所属している。

### 個別の閲覧トークンで開く

| 分類 | 観点 |
|---|---|
| 正常系 | 共有アーティファクト自身の閲覧トークンで開けることを確かめる |

```gherkin
Given 共有アーティファクトAが公開されている
When Aのアーティファクト閲覧トークンで開こうとする
Then 開ける
```

### プロジェクト閲覧トークンで開く

| 分類 | 観点 |
|---|---|
| 正常系 | 所属先のプロジェクトの閲覧トークンでも開けることを確かめる |

```gherkin
Given 共有アーティファクトAが公開されており、プロジェクトPに所属している
When プロジェクトPのプロジェクト閲覧トークンで開こうとする
Then 開ける
```

### 公開停止したものはプロジェクト閲覧トークンでも開けない

| 分類 | 観点 |
|---|---|
| 境界値 | 個別の公開停止が、プロジェクト経由の閲覧にも及ぶことを確かめる。停止した意図を、プロジェクト閲覧トークンが迂回してはならない |

```gherkin
Given 共有アーティファクトAがプロジェクトPに所属している
And 共有アーティファクトAが公開停止されている
When プロジェクトPのプロジェクト閲覧トークンでAを開こうとする
Then 開けない
```

### 所属していないものは開けない

| 分類 | 観点 |
|---|---|
| 異常系 | プロジェクト閲覧トークンの及ぶ範囲が、所属しているものに限られることを確かめる |

```gherkin
Given 共有アーティファクトBがプロジェクトPに所属していない
When プロジェクトPのプロジェクト閲覧トークンでBを開こうとする
Then 開けない
```

### 無効にした閲覧トークンは使えない

| 分類 | 観点 |
|---|---|
| 異常系 | 閲覧トークンの無効化が、その閲覧トークンだけを使えなくすることを確かめる |

```gherkin
Scenario: 無効にした閲覧トークンは使えない
  Given 共有アーティファクトAに2本のアーティファクト閲覧トークンがある
  When 一方を無効にして、その閲覧トークンで開こうとする
  Then 開けない
  And もう一方の閲覧トークンでは開ける
```
